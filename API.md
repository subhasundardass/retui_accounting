<!-- Code generated by gomarkdoc. DO NOT EDIT -->

# retui

```go
import "github.com/subhasundardass/retui/retui"
```

## Index

- [Variables](<#variables>)
- [func Batch\(fn func\(\)\)](<#Batch>)
- [func BeginRender\(\)](<#BeginRender>)
- [func CanPopScreen\(\) bool](<#CanPopScreen>)
- [func CaptureFocus\(id string\)](<#CaptureFocus>)
- [func CapturedFocus\(\) string](<#CapturedFocus>)
- [func CloseLog\(\) error](<#CloseLog>)
- [func CloseOverlay\(id string\)](<#CloseOverlay>)
- [func CurrentFocus\(\) string](<#CurrentFocus>)
- [func CurrentScreen\(\) string](<#CurrentScreen>)
- [func Debug\(args ...interface\{\}\)](<#Debug>)
- [func Debugf\(format string, args ...interface\{\}\)](<#Debugf>)
- [func Error\(args ...interface\{\}\)](<#Error>)
- [func Errorf\(format string, args ...interface\{\}\)](<#Errorf>)
- [func Exit\(\)](<#Exit>)
- [func Fatal\(args ...interface\{\}\)](<#Fatal>)
- [func Fatalf\(format string, args ...interface\{\}\)](<#Fatalf>)
- [func FocusNext\(\)](<#FocusNext>)
- [func FocusPrev\(\)](<#FocusPrev>)
- [func HasFocusCapture\(id string\) bool](<#HasFocusCapture>)
- [func Info\(args ...interface\{\}\)](<#Info>)
- [func Infof\(format string, args ...interface\{\}\)](<#Infof>)
- [func IntrinsicSize\(root \*LayoutNode\) \(width, height int\)](<#IntrinsicSize>)
- [func IsAnyOverlayOpen\(\) bool](<#IsAnyOverlayOpen>)
- [func IsDebugMode\(\) bool](<#IsDebugMode>)
- [func IsFocused\(id string\) bool](<#IsFocused>)
- [func IsPendingRender\(\) bool](<#IsPendingRender>)
- [func LogWithFields\(level LogLevel, fields map\[string\]interface\{\}, message string\)](<#LogWithFields>)
- [func OpenOverlay\(id string\)](<#OpenOverlay>)
- [func PopFocus\(\)](<#PopFocus>)
- [func PopScreen\(\) string](<#PopScreen>)
- [func PruneKeyedState\(\)](<#PruneKeyedState>)
- [func PushFocus\(id string\)](<#PushFocus>)
- [func PushScreen\(id string, params ...ScreenParams\)](<#PushScreen>)
- [func ReleaseFocus\(\)](<#ReleaseFocus>)
- [func ReplaceScreen\(id string, params ...ScreenParams\)](<#ReplaceScreen>)
- [func ResetComponentState\(\)](<#ResetComponentState>)
- [func ResetScreenStack\(rootID string\)](<#ResetScreenStack>)
- [func RunEffects\(\)](<#RunEffects>)
- [func RuneWidth\(r rune\) int](<#RuneWidth>)
- [func ScreenStackSize\(\) int](<#ScreenStackSize>)
- [func ScreenStackSnapshot\(\) \[\]string](<#ScreenStackSnapshot>)
- [func SetDebugMode\(enabled bool\)](<#SetDebugMode>)
- [func SetFocus\(id string\)](<#SetFocus>)
- [func SetFocusOrder\(order \[\]string\) error](<#SetFocusOrder>)
- [func SetInitialScreen\(id string\)](<#SetInitialScreen>)
- [func SetLogLevel\(level LogLevel\)](<#SetLogLevel>)
- [func Success\(args ...interface\{\}\)](<#Success>)
- [func Successf\(format string, args ...interface\{\}\)](<#Successf>)
- [func UseContext\[T any\]\(c \*Context\[T\]\) T](<#UseContext>)
- [func UseEffect\(fn func\(\) func\(\), deps \[\]any\)](<#UseEffect>)
- [func UseFocusedKey\(componentID string, isFocused bool\) \(key Key, isMine bool\)](<#UseFocusedKey>)
- [func UseMemo\[T any\]\(compute func\(\) T, deps \[\]any\) T](<#UseMemo>)
- [func UseReducer\[S any, A any\]\(reducer func\(S, A\) S, initial S\) \(S, func\(A\)\)](<#UseReducer>)
- [func UseState\[T any\]\(initial T\) \(T, func\(T\)\)](<#UseState>)
- [func UseStateKeyed\[T any\]\(key string, initial T\) \(T, func\(T\)\)](<#UseStateKeyed>)
- [func Warn\(args ...interface\{\}\)](<#Warn>)
- [func Warnf\(format string, args ...interface\{\}\)](<#Warnf>)
- [type Alignment](<#Alignment>)
- [type App](<#App>)
  - [func NewApp\(width, height int\) \*App](<#NewApp>)
  - [func \(a \*App\) Render\(fn func\(props Props\) Element, props Props\)](<#App.Render>)
  - [func \(a \*App\) Run\(fn func\(props Props\) Element, props Props\)](<#App.Run>)
- [type Border](<#Border>)
  - [func \(b Border\) Any\(\) bool](<#Border.Any>)
- [type BorderChars](<#BorderChars>)
- [type BorderTitle](<#BorderTitle>)
- [type Cell](<#Cell>)
- [type Color](<#Color>)
  - [func ANSI256\(color uint8\) Color](<#ANSI256>)
  - [func ColorIf\(condition bool, color1, color2 Color\) Color](<#ColorIf>)
  - [func Gray\(n int\) Color](<#Gray>)
  - [func Hex\(color string\) Color](<#Hex>)
  - [func Mix\(a, b Color, t float64\) Color](<#Mix>)
  - [func RGB\(r, g, b uint8\) Color](<#RGB>)
  - [func \(c Color\) Darken\(amount float64\) Color](<#Color.Darken>)
  - [func \(c Color\) Hex\(\) string](<#Color.Hex>)
  - [func \(c Color\) IsZero\(\) bool](<#Color.IsZero>)
  - [func \(c Color\) Lighten\(amount float64\) Color](<#Color.Lighten>)
  - [func \(c Color\) RGB\(\) \(uint8, uint8, uint8\)](<#Color.RGB>)
- [type ColorType](<#ColorType>)
- [type ComponentRenderer](<#ComponentRenderer>)
  - [func NewRenderer\(screen \*Screen\) \*ComponentRenderer](<#NewRenderer>)
  - [func \(r \*ComponentRenderer\) Render\(next Element\)](<#ComponentRenderer.Render>)
- [type Context](<#Context>)
  - [func CreateContext\[T any\]\(defaultValue T\) \*Context\[T\]](<#CreateContext>)
  - [func \(c \*Context\[T\]\) Provide\(value T, render func\(\) Element\) Element](<#Context[T].Provide>)
- [type Direction](<#Direction>)
- [type Effect](<#Effect>)
- [type Element](<#Element>)
  - [func Box\(props Props, style Style, children ...Element\) Element](<#Box>)
  - [func If\(condition bool, choice1 Element, choice2 Element\) Element](<#If>)
  - [func Markdown\(markdown string, style Style\) Element](<#Markdown>)
  - [func MultilineText\(text string, style Style\) Element](<#MultilineText>)
  - [func Overlay\(x, y int, children ...Element\) Element](<#Overlay>)
  - [func Text\(text string, style Style\) Element](<#Text>)
  - [func WrappedText\(text string, style Style\) Element](<#WrappedText>)
- [type ElementType](<#ElementType>)
- [type FocusManager](<#FocusManager>)
  - [func NewFocusManager\(\) \*FocusManager](<#NewFocusManager>)
  - [func \(f \*FocusManager\) CaptureFocus\(id string\)](<#FocusManager.CaptureFocus>)
  - [func \(f \*FocusManager\) CapturedBy\(\) string](<#FocusManager.CapturedBy>)
  - [func \(f \*FocusManager\) Current\(\) string](<#FocusManager.Current>)
  - [func \(f \*FocusManager\) HasCapture\(id string\) bool](<#FocusManager.HasCapture>)
  - [func \(f \*FocusManager\) IsFocused\(id string\) bool](<#FocusManager.IsFocused>)
  - [func \(f \*FocusManager\) Next\(\) string](<#FocusManager.Next>)
  - [func \(f \*FocusManager\) PopFocus\(\) string](<#FocusManager.PopFocus>)
  - [func \(f \*FocusManager\) Prev\(\) string](<#FocusManager.Prev>)
  - [func \(f \*FocusManager\) PushFocus\(id string\)](<#FocusManager.PushFocus>)
  - [func \(f \*FocusManager\) ReleaseFocus\(\)](<#FocusManager.ReleaseFocus>)
  - [func \(f \*FocusManager\) SetFocus\(id string\)](<#FocusManager.SetFocus>)
  - [func \(f \*FocusManager\) SetOrder\(order \[\]string\) error](<#FocusManager.SetOrder>)
- [type Justify](<#Justify>)
- [type Key](<#Key>)
  - [func ParseKey\(b \[\]byte\) Key](<#ParseKey>)
  - [func \(k Key\) IsFunctionKey\(\) bool](<#Key.IsFunctionKey>)
  - [func \(k Key\) IsModifier\(\) bool](<#Key.IsModifier>)
  - [func \(k Key\) IsNavigationKey\(\) bool](<#Key.IsNavigationKey>)
  - [func \(k Key\) IsPrintable\(\) bool](<#Key.IsPrintable>)
  - [func \(k Key\) String\(\) string](<#Key.String>)
- [type KeyCode](<#KeyCode>)
- [type KeyScanner](<#KeyScanner>)
  - [func \(s \*KeyScanner\) Feed\(b \[\]byte\) \[\]Key](<#KeyScanner.Feed>)
- [type LayoutNode](<#LayoutNode>)
  - [func NewLayout\(\) \*LayoutNode](<#NewLayout>)
  - [func \(l \*LayoutNode\) WithAlign\(alignment Alignment\) \*LayoutNode](<#LayoutNode.WithAlign>)
  - [func \(l \*LayoutNode\) WithChildren\(children ...\*LayoutNode\) \*LayoutNode](<#LayoutNode.WithChildren>)
  - [func \(l \*LayoutNode\) WithDirection\(dir Direction\) \*LayoutNode](<#LayoutNode.WithDirection>)
  - [func \(l \*LayoutNode\) WithGap\(value int\) \*LayoutNode](<#LayoutNode.WithGap>)
  - [func \(l \*LayoutNode\) WithHeight\(h Sizing\) \*LayoutNode](<#LayoutNode.WithHeight>)
  - [func \(l \*LayoutNode\) WithJustify\(value Justify\) \*LayoutNode](<#LayoutNode.WithJustify>)
  - [func \(l \*LayoutNode\) WithMargin\(top, right, bottom, left int\) \*LayoutNode](<#LayoutNode.WithMargin>)
  - [func \(l \*LayoutNode\) WithMarginUniform\(margin int\) \*LayoutNode](<#LayoutNode.WithMarginUniform>)
  - [func \(l \*LayoutNode\) WithPadding\(top, right, bottom, left int\) \*LayoutNode](<#LayoutNode.WithPadding>)
  - [func \(l \*LayoutNode\) WithPaddingUniform\(padding int\) \*LayoutNode](<#LayoutNode.WithPaddingUniform>)
  - [func \(l \*LayoutNode\) WithReflow\(fn func\(crossAxisSize int\) int\) \*LayoutNode](<#LayoutNode.WithReflow>)
  - [func \(l \*LayoutNode\) WithSize\(w, h Sizing\) \*LayoutNode](<#LayoutNode.WithSize>)
  - [func \(l \*LayoutNode\) WithWidth\(w Sizing\) \*LayoutNode](<#LayoutNode.WithWidth>)
- [type LayoutProps](<#LayoutProps>)
- [type LogLevel](<#LogLevel>)
  - [func GetLogLevel\(\) LogLevel](<#GetLogLevel>)
  - [func \(l LogLevel\) String\(\) string](<#LogLevel.String>)
- [type Logger](<#Logger>)
  - [func NewLogger\(fields map\[string\]interface\{\}\) \*Logger](<#NewLogger>)
  - [func WithFields\(fields map\[string\]interface\{\}\) \*Logger](<#WithFields>)
  - [func \(l \*Logger\) AddField\(key string, value interface\{\}\) \*Logger](<#Logger.AddField>)
  - [func \(l \*Logger\) Debug\(message string\)](<#Logger.Debug>)
  - [func \(l \*Logger\) Error\(message string\)](<#Logger.Error>)
  - [func \(l \*Logger\) Info\(message string\)](<#Logger.Info>)
  - [func \(l \*Logger\) Warn\(message string\)](<#Logger.Warn>)
- [type MarkdownContent](<#MarkdownContent>)
- [type OverlayManager](<#OverlayManager>)
- [type OverlayRegion](<#OverlayRegion>)
- [type Props](<#Props>)
  - [func NewProps\(\) Props](<#NewProps>)
  - [func \(p Props\) Get\(key string\) any](<#Props.Get>)
- [type Rect](<#Rect>)
  - [func ComputeLayout\(root \*LayoutNode, available Rect\) \[\]Rect](<#ComputeLayout>)
- [type Ref](<#Ref>)
  - [func UseRef\[T any\]\(initial T\) \*Ref\[T\]](<#UseRef>)
  - [func \(r \*Ref\[T\]\) Get\(\) T](<#Ref[T].Get>)
  - [func \(r \*Ref\[T\]\) Set\(v T\)](<#Ref[T].Set>)
- [type Screen](<#Screen>)
  - [func NewScreen\(width, height int, out io.Writer\) \*Screen](<#NewScreen>)
  - [func NewScreenStdout\(\) \*Screen](<#NewScreenStdout>)
  - [func StdOutScreen\(\) \*Screen](<#StdOutScreen>)
  - [func \(s \*Screen\) ApplyOverlay\(region OverlayRegion, x, y int\)](<#Screen.ApplyOverlay>)
  - [func \(s \*Screen\) Clear\(\)](<#Screen.Clear>)
  - [func \(s \*Screen\) EnsureRoom\(contentH int\)](<#Screen.EnsureRoom>)
  - [func \(s \*Screen\) Flush\(\)](<#Screen.Flush>)
  - [func \(s \*Screen\) ForceMarkAllDirty\(\)](<#Screen.ForceMarkAllDirty>)
  - [func \(s \*Screen\) GetCell\(x, y int\) Cell](<#Screen.GetCell>)
  - [func \(s \*Screen\) HandleResize\(\)](<#Screen.HandleResize>)
  - [func \(s \*Screen\) Height\(\) int](<#Screen.Height>)
  - [func \(s \*Screen\) PaintOverlay\(width, height int, paint func\(s \*Screen\)\) OverlayRegion](<#Screen.PaintOverlay>)
  - [func \(s \*Screen\) Resize\(width, height int\)](<#Screen.Resize>)
  - [func \(s \*Screen\) SetCell\(x, y int, value rune, style Style\)](<#Screen.SetCell>)
  - [func \(s \*Screen\) Start\(\)](<#Screen.Start>)
  - [func \(s \*Screen\) Stop\(\)](<#Screen.Stop>)
  - [func \(s \*Screen\) Width\(\) int](<#Screen.Width>)
- [type ScreenParams](<#ScreenParams>)
  - [func CurrentScreenParams\(\) ScreenParams](<#CurrentScreenParams>)
- [type ScreenRoute](<#ScreenRoute>)
- [type ScreenStack](<#ScreenStack>)
  - [func NewScreenStack\(rootScreen string\) \*ScreenStack](<#NewScreenStack>)
  - [func \(n \*ScreenStack\) CanPop\(\) bool](<#ScreenStack.CanPop>)
  - [func \(n \*ScreenStack\) Clear\(\)](<#ScreenStack.Clear>)
  - [func \(n \*ScreenStack\) Current\(\) string](<#ScreenStack.Current>)
  - [func \(n \*ScreenStack\) CurrentParams\(\) ScreenParams](<#ScreenStack.CurrentParams>)
  - [func \(n \*ScreenStack\) IsEmpty\(\) bool](<#ScreenStack.IsEmpty>)
  - [func \(n \*ScreenStack\) PopScreen\(\) string](<#ScreenStack.PopScreen>)
  - [func \(n \*ScreenStack\) PushScreen\(screenID string, params ScreenParams\)](<#ScreenStack.PushScreen>)
  - [func \(n \*ScreenStack\) ReplaceScreen\(screenID string, params ScreenParams\)](<#ScreenStack.ReplaceScreen>)
  - [func \(n \*ScreenStack\) Reset\(rootScreenID string\)](<#ScreenStack.Reset>)
  - [func \(n \*ScreenStack\) Size\(\) int](<#ScreenStack.Size>)
  - [func \(n \*ScreenStack\) Stack\(\) \[\]string](<#ScreenStack.Stack>)
- [type Sizing](<#Sizing>)
  - [func Fit\(\) Sizing](<#Fit>)
  - [func Fixed\(n int\) Sizing](<#Fixed>)
  - [func Grow\(n int\) Sizing](<#Grow>)
  - [func Percent\(n int\) Sizing](<#Percent>)
- [type SizingMode](<#SizingMode>)
- [type Style](<#Style>)
  - [func NewStyle\(\) Style](<#NewStyle>)
  - [func \(s Style\) ANSIPrefix\(\) string](<#Style.ANSIPrefix>)
  - [func \(s Style\) Background\(color Color\) Style](<#Style.Background>)
  - [func \(s Style\) Bold\(bold bool\) Style](<#Style.Bold>)
  - [func \(s Style\) Border\(b Border\) Style](<#Style.Border>)
  - [func \(s Style\) Foreground\(color Color\) Style](<#Style.Foreground>)
  - [func \(s Style\) IsBold\(\) bool](<#Style.IsBold>)
  - [func \(s Style\) Italic\(italic bool\) Style](<#Style.Italic>)
  - [func \(s Style\) Underline\(underline bool\) Style](<#Style.Underline>)


## Variables

<a name="BorderSharp"></a>

```go
var (
    BorderSharp = BorderChars{
        Top: '─', Bottom: '─', Left: '│', Right: '│',
        TopLeft: '┌', TopRight: '┐', BottomLeft: '└', BottomRight: '┘',
    }
    BorderRounded = BorderChars{
        Top: '─', Bottom: '─', Left: '│', Right: '│',
        TopLeft: '╭', TopRight: '╮', BottomLeft: '╰', BottomRight: '╯',
    }
    BorderDouble = BorderChars{
        Top: '═', Bottom: '═', Left: '║', Right: '║',
        TopLeft: '╔', TopRight: '╗', BottomLeft: '╚', BottomRight: '╝',
    }
    BorderThick = BorderChars{
        Top: '━', Bottom: '━', Left: '┃', Right: '┃',
        TopLeft: '┏', TopRight: '┓', BottomLeft: '┗', BottomRight: '┛',
    }
)
```

<a name="Black"></a>Standard 16\-color ANSI palette.

```go
var (
    Black         = Color{Type: ColorANSI16, Code: 0}
    Red           = Color{Type: ColorANSI16, Code: 1}
    Green         = Color{Type: ColorANSI16, Code: 2}
    Yellow        = Color{Type: ColorANSI16, Code: 3}
    Blue          = Color{Type: ColorANSI16, Code: 4}
    Magenta       = Color{Type: ColorANSI16, Code: 5}
    Cyan          = Color{Type: ColorANSI16, Code: 6}
    White         = Color{Type: ColorANSI16, Code: 7}
    BrightBlack   = Color{Type: ColorANSI16, Code: 8}
    BrightRed     = Color{Type: ColorANSI16, Code: 9}
    BrightGreen   = Color{Type: ColorANSI16, Code: 10}
    BrightYellow  = Color{Type: ColorANSI16, Code: 11}
    BrightBlue    = Color{Type: ColorANSI16, Code: 12}
    BrightMagenta = Color{Type: ColorANSI16, Code: 13}
    BrightCyan    = Color{Type: ColorANSI16, Code: 14}
    BrightWhite   = Color{Type: ColorANSI16, Code: 15}
)
```

<a name="Orange"></a>Extended named colors, approximated from the 256\-color palette. These give richer named options than the base 16 ANSI colors while still rendering correctly on terminals that only support 256 colors \(unlike arbitrary Hex\(\)/RGB\(\) truecolor values\).

```go
var (
    Orange      = ANSI256(208)
    Purple      = ANSI256(129)
    Pink        = ANSI256(213)
    Teal        = ANSI256(30)
    Lime        = ANSI256(154)
    Gold        = ANSI256(220)
    Silver      = ANSI256(250)
    Navy        = ANSI256(17)
    Maroon      = ANSI256(52)
    Olive       = ANSI256(58)
    Indigo      = ANSI256(54)
    Turquoise   = ANSI256(80)
    Coral       = ANSI256(203)
    Salmon      = ANSI256(210)
    Violet      = ANSI256(177)
    Chocolate   = ANSI256(166)
    SkyBlue     = ANSI256(117)
    ForestGreen = ANSI256(28)
    Crimson     = ANSI256(161)
    Khaki       = ANSI256(222)
)
```

<a name="CurrentScreenHeight"></a>

```go
var CurrentScreenHeight int
```

<a name="CurrentScreenWidth"></a>

```go
var CurrentScreenWidth int
```

<a name="CurrentTick"></a>CurrentTick is the state of the periodic tick \(alternates every 500ms\). Available to hook functions for reactive effects \(e.g., blinking cursor\).

NOT thread\-safe; read/write from the event loop goroutine only.

```go
var CurrentTick bool
```

<a name="EffectCursor"></a>EffectCursor is the next free index in Effects.

```go
var EffectCursor int
```

<a name="Effects"></a>Effects is the registered side\-effect slice. Indexed by EffectCursor.

```go
var Effects []Effect
```

<a name="IsAnyModalOpenFn"></a>IsAnyModalOpenFn, if set, reports whether any modal is currently open. Used to determine if root content should be blocked from keyboard input.

Nil by default. Set by window package.

```go
var IsAnyModalOpenFn func() bool
```

<a name="KeyedState"></a>KeyedState is state keyed by a stable string identity. stateMu guards all access to this map.

```go
var KeyedState = map[string]any{}
```

<a name="Keys"></a>

```go
var Keys = make(chan Key, 4)
```

<a name="MemosCursor"></a>MemosCursor is the next free index in memos.

```go
var MemosCursor int
```

<a name="NoColor"></a>NoColor explicitly represents "no color set". Equivalent to the zero Color, provided for readability at call sites.

```go
var NoColor = Color{Type: ColorNone}
```

<a name="Renderer"></a>

```go
var Renderer = NewRenderer(screen)
```

<a name="RootRenderWrap"></a>RootRenderWrap, if set, wraps the root element before rendering. Used by overlay/modal packages \(like window\) to composite overlays without creating import cycles.

Called twice per render frame:

1. Pass 1 \(hooks phase\): With realKey for modal/window processing
2. Pass 2 \(render phase\): With cleared key \(already consumed\)

Return value from Pass 1 is discarded \(side effects only\). Return value from Pass 2 is used for rendering.

Nil by default. Set by modal/window layer at init time.

```go
var RootRenderWrap func(Element) Element
```

<a name="State"></a>State is the positional state slice. Indexed by StateCursor. Render\-goroutine\-owned for reads; stateMu for writes from setters.

```go
var State []any
```

<a name="StateCursor"></a>StateCursor is the next free index in State. Must only be written from the render goroutine.

```go
var StateCursor int
```

<a name="WindowKeyDispatch"></a>WindowKeyDispatch, if set, intercepts keyboard input for modal/window layer. Called when a key is pressed to allow modal stack to handle it before the root application.

Returns true if the key was handled by the modal layer \(consumed\); false if the root application should handle it.

Nil by default. Set by window package.

```go
var WindowKeyDispatch func(key Key) bool
```

<a name="Batch"></a>
## func Batch

```go
func Batch(fn func())
```

Batch coalesces multiple setter calls into a single pendingRender signal. Use when an event handler must update several independent state values atomically — without Batch, each setter would trigger its own redraw.

Example:

```
retui.Batch(func() {
    setName("Alice")
    setAge(30)
    setRole("admin")
})
// one redraw, not three
```

<a name="BeginRender"></a>
## func BeginRender

```go
func BeginRender()
```

BeginRender must be called once at the start of every render, before the root component function runs.

It resets all cursor positions and the keyed\-state touch tracker. This is the ONLY place cursors should be reset — doing it elsewhere without the stateMu section below risks a race with async setters.

Example \(in your render loop\):

```
retui.BeginRender()
root := MyApp(props)
retui.RunEffects()
```

<a name="CanPopScreen"></a>
## func CanPopScreen

```go
func CanPopScreen() bool
```

CanPopScreen reports whether Back navigation is possible.

<a name="CaptureFocus"></a>
## func CaptureFocus

```go
func CaptureFocus(id string)
```

CaptureFocus captures keyboard input for a component.

<a name="CapturedFocus"></a>
## func CapturedFocus

```go
func CapturedFocus() string
```

CapturedFocus returns the ID of the component with capture.

<a name="CloseLog"></a>
## func CloseLog

```go
func CloseLog() error
```

CloseLog closes the log file

<a name="CloseOverlay"></a>
## func CloseOverlay

```go
func CloseOverlay(id string)
```



<a name="CurrentFocus"></a>
## func CurrentFocus

```go
func CurrentFocus() string
```

CurrentFocus returns the currently focused component ID.

<a name="CurrentScreen"></a>
## func CurrentScreen

```go
func CurrentScreen() string
```

CurrentScreen returns the current screen ID.

<a name="Debug"></a>
## func Debug

```go
func Debug(args ...interface{})
```

Debug logs debug messages \(only shown in debug mode\)

<a name="Debugf"></a>
## func Debugf

```go
func Debugf(format string, args ...interface{})
```

Debugf logs a formatted debug message

<a name="Error"></a>
## func Error

```go
func Error(args ...interface{})
```

Error logs error messages

<a name="Errorf"></a>
## func Errorf

```go
func Errorf(format string, args ...interface{})
```

Errorf logs a formatted error message

<a name="Exit"></a>
## func Exit

```go
func Exit()
```

Exit requests the running application to stop gracefully. Safe to call from any goroutine; uses buffered channel to avoid blocking. Calling Exit\(\) multiple times is safe.

<a name="Fatal"></a>
## func Fatal

```go
func Fatal(args ...interface{})
```

Fatal logs fatal messages and exits

<a name="Fatalf"></a>
## func Fatalf

```go
func Fatalf(format string, args ...interface{})
```

Fatalf logs a formatted fatal message and exits

<a name="FocusNext"></a>
## func FocusNext

```go
func FocusNext()
```

FocusNext moves focus to the next component in the tab order.

<a name="FocusPrev"></a>
## func FocusPrev

```go
func FocusPrev()
```

FocusPrev moves focus to the previous component in the tab order.

<a name="HasFocusCapture"></a>
## func HasFocusCapture

```go
func HasFocusCapture(id string) bool
```

HasFocusCapture checks if a component has capture.

<a name="Info"></a>
## func Info

```go
func Info(args ...interface{})
```

Info logs info messages

<a name="Infof"></a>
## func Infof

```go
func Infof(format string, args ...interface{})
```

Infof logs a formatted info message

<a name="IntrinsicSize"></a>
## func IntrinsicSize

```go
func IntrinsicSize(root *LayoutNode) (width, height int)
```

IntrinsicSize returns the natural \(unconstrained\) dimensions of a layout tree. The tree is measured bottom\-up without a parent constraint; Grow and Percent sizing modes resolve to 0 during this pass.

Useful for: \- Determining the minimum bounding box for a layout subtree. \- Previewing layout before committing to a parent Rect. \- Implementing responsive containers.

Returns \(width, height\) as if the tree had infinite space and no parent sizing.

<a name="IsAnyOverlayOpen"></a>
## func IsAnyOverlayOpen

```go
func IsAnyOverlayOpen() bool
```



<a name="IsDebugMode"></a>
## func IsDebugMode

```go
func IsDebugMode() bool
```

IsDebugMode returns true if debug mode is enabled

<a name="IsFocused"></a>
## func IsFocused

```go
func IsFocused(id string) bool
```

IsFocused checks if a component has global focus.

<a name="IsPendingRender"></a>
## func IsPendingRender

```go
func IsPendingRender() bool
```

IsPendingRender returns true if any setter has requested a redraw since the last render. Clears the flag atomically.

Example \(in your render loop\):

```
if retui.IsPendingRender() {
    // re-render
}
```

<a name="LogWithFields"></a>
## func LogWithFields

```go
func LogWithFields(level LogLevel, fields map[string]interface{}, message string)
```

LogWithFields logs with additional fields \(like structured logging\)

<a name="OpenOverlay"></a>
## func OpenOverlay

```go
func OpenOverlay(id string)
```



<a name="PopFocus"></a>
## func PopFocus

```go
func PopFocus()
```

PopFocus pops focus from the modal stack.

<a name="PopScreen"></a>
## func PopScreen

```go
func PopScreen() string
```

PopScreen navigates back.

<a name="PruneKeyedState"></a>
## func PruneKeyedState

```go
func PruneKeyedState()
```

PruneKeyedState evicts KeyedState entries that were not touched since the last BeginRender. Call once per render, after the tree has been fully rendered. Prevents KeyedState growing without bound.

Example:

```
retui.BeginRender()
root := MyApp(props)
retui.PruneKeyedState()
retui.RunEffects()
```

<a name="PushFocus"></a>
## func PushFocus

```go
func PushFocus(id string)
```

PushFocus pushes focus onto the modal stack.

<a name="PushScreen"></a>
## func PushScreen

```go
func PushScreen(id string, params ...ScreenParams)
```

PushScreen pushes a new screen onto the global navigation stack.

The params argument is optional.

Example:

```
retui.PushScreen("ledger_list")

retui.PushScreen("ledger_list", retui.ScreenParams{
    "groupId": 5,
})
```

<a name="ReleaseFocus"></a>
## func ReleaseFocus

```go
func ReleaseFocus()
```

ReleaseFocus releases keyboard capture.

<a name="ReplaceScreen"></a>
## func ReplaceScreen

```go
func ReplaceScreen(id string, params ...ScreenParams)
```

ReplaceScreen replaces the current screen.

<a name="ResetComponentState"></a>
## func ResetComponentState

```go
func ResetComponentState()
```

ResetComponentState clears ALL hook state: positional, keyed, memos, and effects. Call on hard screen transitions \(logout, full reset\).

WARNING: this also clears UseStateKeyed state. If you use UseStateKeyed specifically to survive navigation \(tree expand state, table selection\), do NOT call this on routine screen switches — call BeginRender\(\) instead, which only resets cursors.

<a name="ResetScreenStack"></a>
## func ResetScreenStack

```go
func ResetScreenStack(rootID string)
```

ResetScreenStack clears navigation history.

<a name="RunEffects"></a>
## func RunEffects

```go
func RunEffects()
```

RunEffects executes all dirty effects and cleans up removed effects.

Call once per render cycle, after rendering and PruneKeyedState:

```
retui.BeginRender()
root := MyApp(props)
retui.PruneKeyedState()
retui.RunEffects()
```

CRITICAL: stateMu is never held while calling user code \(fn, cleanup\). Effect bodies routinely call setters — holding the lock across that would self\-deadlock. stateMu is held only for short slice/map accesses.

<a name="RuneWidth"></a>
## func RuneWidth

```go
func RuneWidth(r rune) int
```

RuneWidth returns the number of terminal columns a rune occupies. Wraps go\-runewidth for use by layout code without importing the package.

<a name="ScreenStackSize"></a>
## func ScreenStackSize

```go
func ScreenStackSize() int
```

ScreenStackSize returns the number of screens on the stack.

<a name="ScreenStackSnapshot"></a>
## func ScreenStackSnapshot

```go
func ScreenStackSnapshot() []string
```

ScreenStackSnapshot returns a copy of the navigation history.

<a name="SetDebugMode"></a>
## func SetDebugMode

```go
func SetDebugMode(enabled bool)
```

SetDebugMode enables or disables debug mode

<a name="SetFocus"></a>
## func SetFocus

```go
func SetFocus(id string)
```

SetFocus sets the global focus to a component.

<a name="SetFocusOrder"></a>
## func SetFocusOrder

```go
func SetFocusOrder(order []string) error
```

SetFocusOrder sets the global tab navigation order.

<a name="SetInitialScreen"></a>
## func SetInitialScreen

```go
func SetInitialScreen(id string)
```

SetInitialScreen sets the root screen.

<a name="SetLogLevel"></a>
## func SetLogLevel

```go
func SetLogLevel(level LogLevel)
```

SetLogLevel sets the minimum log level

<a name="Success"></a>
## func Success

```go
func Success(args ...interface{})
```

Success logs success messages \(green\)

<a name="Successf"></a>
## func Successf

```go
func Successf(format string, args ...interface{})
```

Successf logs a formatted success message

<a name="UseContext"></a>
## func UseContext

```go
func UseContext[T any](c *Context[T]) T
```

UseContext returns the innermost active Provide value, or the default.

<a name="UseEffect"></a>
## func UseEffect

```go
func UseEffect(fn func() func(), deps []any)
```

UseEffect registers a side\-effect to run after paint when deps change. fn returns an optional cleanup called before the next run or on unmount.

Deps are compared with safeDeepEqual \(recover\-wrapped reflect.DeepEqual\) so slice/map/struct deps are compared by value. Functions and channels in deps are treated as "always changed" rather than panicking.

Example:

```
retui.UseEffect(func() func() {
    ticker := time.NewTicker(time.Second)
    go func() {
        for range ticker.C {
            setTime(time.Now())
        }
    }()
    return ticker.Stop  // cleanup
}, []any{})  // empty deps = run once on mount
```

<a name="UseFocusedKey"></a>
## func UseFocusedKey

```go
func UseFocusedKey(componentID string, isFocused bool) (key Key, isMine bool)
```

UseFocusedKey is a hook for components to check if a keyboard key is theirs.

Usage:

```
In your component render function:

func myComponent(props Element) Element {
    key, isMine := UseFocusedKey("my-component-id", isFocused)
    if isMine && key.Code == KeyEnter {
        handleEnter()
    }
    // ...
}
```

Behavior:

- Returns the current key and whether the component should handle it
- Component only handles the key if: 1. Component has focus \(current\) 2. Key hasn't been consumed by another component \(Key.consumed is false\)
- Marks the key as consumed \(Key.consumed = true\) when claimed
- Prevents key duplication: each key is handled by exactly one component
- Returns empty key and false if component shouldn't handle input

Parameters:

```
componentID: Unique identifier for this component
isFocused:   True if this component is visually focused
```

Returns:

```
key:   The current keyboard input (or empty if not applicable)
isMine: True if this component should handle the key, false otherwise
```

Key Consumption:

- Uses Key.consumed field to track if key was already claimed
- When a component claims a key, Key.consumed is set to true
- Other components see Key.consumed=true and don't claim it
- CurrentKey is cleared after each render frame, resetting all keys

Example:

```
func render(props Props) Element {
    key, isMine := UseFocusedKey("input", currentFocus == "input")
    if isMine {
        switch key.Code {
        case KeyEnter:
            return handleEnter()
        case KeyEscape:
            return handleEscape()
        }
    }
    return Element{...}
}
```

<a name="UseMemo"></a>
## func UseMemo

```go
func UseMemo[T any](compute func() T, deps []any) T
```

UseMemo returns a memoized value recomputed only when deps change. Use for expensive computations \(filtering, sorting, formatting\) that should not run on every render.

The compute function runs synchronously during render. deps comparison uses safeDeepEqual \(recover\-wrapped DeepEqual\).

Example:

```
filtered := retui.UseMemo(func() []Item {
    return filterItems(allItems, query)
}, []any{allItems, query})
```

<a name="UseReducer"></a>
## func UseReducer

```go
func UseReducer[S any, A any](reducer func(S, A) S, initial S) (S, func(A))
```

UseReducer manages state through a reducer function, identical to the React pattern. Prefer over multiple UseState calls when several values must be updated atomically in response to an action.

Example:

```
type Action struct{ Type string; Payload any }

state, dispatch := retui.UseReducer(func(s MyState, a Action) MyState {
    switch a.Type {
    case "increment": return MyState{Count: s.Count + 1}
    case "reset":     return MyState{Count: 0}
    }
    return s
}, MyState{Count: 0})

if focused && key == KeyEnter {
    dispatch(Action{Type: "increment"})
}
```

<a name="UseState"></a>
## func UseState

```go
func UseState[T any](initial T) (T, func(T))
```

UseState returns the current value at the caller's positional slot and a setter that updates it and requests a redraw.

Rules \(same as React hooks\):

- Call only from the render goroutine.
- Call unconditionally and in a fixed order — never inside an if/loop.
- If calls vary across renders \(different screen with different hook count\), use UseStateKeyed instead.

The setter is safe to call from any goroutine \(timer, goroutine, etc\).

Example:

```
count, setCount := retui.UseState(0)
if focused && key == KeyEnter {
    setCount(count + 1)
}
```

<a name="UseStateKeyed"></a>
## func UseStateKeyed

```go
func UseStateKeyed[T any](key string, initial T) (T, func(T))
```

UseStateKeyed is like UseState but keyed by a stable string.

Use this when the number of hook calls in a render can vary — e.g. tree nodes that appear/disappear as subtrees expand or collapse. Positional UseState would corrupt all slots after a disappeared node; UseStateKeyed is immune because it always looks up by key.

key must be globally unique for the lifetime of the app. Prefix it with the component/screen name to avoid collisions:

```
expanded, setExpanded := retui.UseStateKeyed("tree-"+node.ID, false)
```

<a name="Warn"></a>
## func Warn

```go
func Warn(args ...interface{})
```

Warn logs warning messages

<a name="Warnf"></a>
## func Warnf

```go
func Warnf(format string, args ...interface{})
```

Warnf logs a formatted warning message

<a name="Alignment"></a>
## type Alignment

Alignment specifies how children align on the cross axis \(perpendicular to layout direction\).

In a Row \(horizontal layout\), cross axis is vertical \(Y\). In a Column \(vertical layout\), cross axis is horizontal \(X\).

Default \(zero value\) is AlignStretch, which overrides child sizing on the cross axis.

```go
type Alignment int
```

<a name="AlignStretch"></a>

```go
const (
    // AlignStretch: children stretch to fill the full cross-axis size of their parent.
    // Overrides child's cross-axis sizing (Height in Row, Width in Column).
    AlignStretch Alignment = iota
    // AlignStart: align children to the start of the cross axis (top in Row, left in Column).
    AlignStart
    // AlignCenter: center children on the cross axis.
    AlignCenter
    // AlignEnd: align children to the end of the cross axis (bottom in Row, right in Column).
    AlignEnd
)
```

<a name="App"></a>
## type App

App is the root controller for a terminal\-based application.

App manages:

- Screen lifecycle \(initialization, cleanup\)
- Event loop \(keyboard, timer, resize\)
- Rendering pipeline \(three\-pass system with state/effect hooks\)
- Modal/window layer integration

App is not thread\-safe; all calls must come from the same goroutine \(typically the main thread\). Use channels or queues for inter\-goroutine communication.

```go
type App struct {
    // contains filtered or unexported fields
}
```

<a name="NewApp"></a>
### func NewApp

```go
func NewApp(width, height int) *App
```

NewApp creates and initializes a new terminal application.

Bootstrap Process:

1. Creates a Screen that writes to os.Stdout
2. Puts the terminal into raw mode \(no echo, line buffering disabled\)
3. Hides the cursor and enables bracketed paste mode
4. Detects the actual terminal dimensions
5. Creates the root Renderer
6. Returns a fully initialized App instance

Parameters:

```
width:  Fallback width (in columns) if terminal size cannot be detected.
        Used when stdout is redirected or in CI environments where
        terminal.GetSize() fails (e.g., piped output, Docker).

height: Fallback height (in rows) if terminal size cannot be detected.
```

Terminal Size Detection:

In normal interactive terminals, the actual terminal viewport dimensions are detected automatically and used, regardless of width/height arguments. Fallback dimensions are only used if detection fails.

Returns: A fully initialized App ready for Run\(\). NewApp panics if Screen creation fails.

Example:

```
app := NewApp(80, 24)  // 80x24 fallback for piped/CI environments
app.Run(renderFunc, props)
```

Note: NewApp starts the Screen to detect terminal size. The Screen is started again in Run\(\) for proper cleanup \(see Run\(\) for details\).

<a name="App.Render"></a>
### func \(\*App\) Render

```go
func (a *App) Render(fn func(props Props) Element, props Props)
```

Render executes one complete render cycle \(three\-pass system\).

The three\-pass system ensures hooks see updated state and effects can trigger immediate re\-renders without requiring another event loop cycle.

Pass 1 \(Hook Setup\):

- Element tree is built via fn\(props\)
- Hooks run in order \(State, then Effects from prior frame\)
- RootRenderWrap receives the tree for modal pass effects
- Return value from RootRenderWrap is discarded \(side effects only\)
- StateCursor and EffectCursor reset at frame start and advance as hooks run

Pass 2 \(Render\):

- CurrentKey is cleared so root doesn't see keys consumed by modal
- Element tree is built again via fn\(props\) with updated hook state
- RootRenderWrap wraps the tree for rendering \(return value used\)
- Tree is rendered to screen and flushed
- RunEffects\(\) executes effect hooks; may request another render

Pass 3 \(Re\-render on Effect\):

- If RunEffects\(\) set pendingRender, immediately repeat Pass 2
- Allows effects to update state and see the result in the same frame
- Helps keep UI responsive without waiting for next event

Modal Blocking:

- If a modal is open, root's CurrentKey is cleared in Pass 1
- Modal layer handles the key via WindowKeyDispatch
- Root component never sees the key

Parameters:

```
fn:    Render function; builds element tree given props
props: Props passed to fn; typically includes app state
```

<a name="App.Run"></a>
### func \(\*App\) Run

```go
func (a *App) Run(fn func(props Props) Element, props Props)
```

Run starts the application's event loop and renders the UI.

Event Loop:

Run manages a select loop that multiplexes several input sources:

- Keyboard input \(os.Stdin\)
- Timer tick \(500ms interval\)
- Terminal resize \(SIGWINCH\)
- Explicit exit request \(Exit\(\) or Ctrl\+C\)

Each event triggers a render cycle via fn\(props\) and optional hook processing.

Rendering Pipeline \(Three Passes\):

Each render frame executes three passes:

```
Pass 1 (Hook Setup): fn(props) is called with CurrentKey set (if not blocked by modal).
The element tree is built and any hook functions (State, Effect, Render) run.
RootRenderWrap is called for pass effects only (return value discarded).
This pass mutates hook state (StateCursor, EffectCursor).

Pass 2 (Render): fn(props) is called again with CurrentKey cleared.
New element tree is built with updated hook state.
RootRenderWrap wraps the tree (return value used).
Tree is rendered to screen via Renderer.Render() and flushed.
Effect hooks (RunEffects) are executed, may request another render.

Pass 3 (Re-render): If RunEffects set pendingRender, Pass 2 repeats
with CurrentKey and hook cursors reset.
```

This pipeline ensures:

- Hook state is computed before rendering
- Rendering sees the latest state
- Effects can schedule a re\-render in the same frame
- Key input is consumed immediately \(not seen by effects\)

Modal Blocking:

When IsAnyModalOpenFn\(\) returns true:

- Root content's CurrentKey is cleared \(receives no input\)
- Modal layer receives CurrentKey via WindowKeyDispatch
- Tab key has special handling to prevent root interference
- Modal can consume keys and the root never sees them

Shutdown:

Run exits gracefully when:

- Ctrl\+C is pressed \(requestQuit called\)
- os.Stdin read fails \(requestQuit called\)
- Exit\(\) is called \(exitCh signal, requestQuit called\)
- quit channel is closed \(select branch returns\)

Before returning, defer block calls a.screen.Stop\(\) to restore the terminal to its original state.

Parameters:

```
fn:    Render function that builds the element tree given props.
       Called three times per frame (hook setup, render, re-render if needed).

props: Props passed to fn on every call. Not modified by Run.
```

Example:

```
func render(props Props) Element {
    return Element{
        Type:     ElementBox,
        Layout:   LayoutProps{Direction: Row},
        Children: []Element{...},
    }
}

app.Run(render, Props{})
```

<a name="Border"></a>
## type Border



```go
type Border struct {
    Top, Right, Bottom, Left bool
    Chars                    BorderChars
    Color                    Color

    Title *BorderTitle
}
```

<a name="Border.Any"></a>
### func \(Border\) Any

```go
func (b Border) Any() bool
```



<a name="BorderChars"></a>
## type BorderChars



```go
type BorderChars struct {
    Top, Bottom, Left, Right                   rune
    TopLeft, TopRight, BottomLeft, BottomRight rune
}
```

<a name="BorderTitle"></a>
## type BorderTitle



```go
type BorderTitle struct {
    Text  string
    Style Style
    Align Alignment
}
```

<a name="Cell"></a>
## type Cell

Cell represents a single terminal cell.

- Rune: the character to render \(0 → rendered as space\)
- Style: foreground/background/attributes
- Wide: true when the rune occupies two columns \(CJK, emoji\)

```go
type Cell struct {
    Rune  rune
    Style Style
    Wide  bool
}
```

<a name="Color"></a>
## type Color



```go
type Color struct {
    Type    ColorType
    R, G, B uint8 //for RGB color
    Code    uint8 //for ANSI16 and ANSI256 color
}
```

<a name="ANSI256"></a>
### func ANSI256

```go
func ANSI256(color uint8) Color
```

ANSI256 creates a color from the 256\-color palette \(0\-255\).

<a name="ColorIf"></a>
### func ColorIf

```go
func ColorIf(condition bool, color1, color2 Color) Color
```

ColorIf returns color1 if condition is true, otherwise color2

<a name="Gray"></a>
### func Gray

```go
func Gray(n int) Color
```

Gray returns a grayscale color from the 256\-color palette's grayscale ramp. n ranges 0 \(near\-black\) to 23 \(near\-white\), corresponding to ANSI256 codes 232\-255; out\-of\-range values are clamped.

<a name="Hex"></a>
### func Hex

```go
func Hex(color string) Color
```

Hex parses a hex color string such as "\#ff8800" or "ff8800" \(case insensitive\) into a truecolor RGB Color. It returns the zero Color if the input isn't a valid 6\-digit hex string.

<a name="Mix"></a>
### func Mix

```go
func Mix(a, b Color, t float64) Color
```

Mix blends two RGB colors together; t=0 returns a, t=1 returns b. If either color is not RGB, it returns whichever one is RGB, or the zero Color if neither is.

<a name="RGB"></a>
### func RGB

```go
func RGB(r, g, b uint8) Color
```

RGB creates a truecolor color from red, green, and blue components \(0\-255 each\).

<a name="Color.Darken"></a>
### func \(Color\) Darken

```go
func (c Color) Darken(amount float64) Color
```

Darken blends an RGB color toward black by amount \(0\-1, clamped\). Non\-RGB colors are returned unchanged.

<a name="Color.Hex"></a>
### func \(Color\) Hex

```go
func (c Color) Hex() string
```

Hex returns the "\#rrggbb" hex representation of an RGB color. For non\-RGB color types \(ANSI16, ANSI256, or unset\) it returns "".

<a name="Color.IsZero"></a>
### func \(Color\) IsZero

```go
func (c Color) IsZero() bool
```

IsZero reports whether the color is unset \(the zero value / ColorNone\).

<a name="Color.Lighten"></a>
### func \(Color\) Lighten

```go
func (c Color) Lighten(amount float64) Color
```

Lighten blends an RGB color toward white by amount \(0\-1, clamped\). Non\-RGB colors are returned unchanged.

<a name="Color.RGB"></a>
### func \(Color\) RGB

```go
func (c Color) RGB() (uint8, uint8, uint8)
```



<a name="ColorType"></a>
## type ColorType



```go
type ColorType int
```

<a name="ColorNone"></a>

```go
const (
    ColorNone ColorType = iota
    ColorANSI16
    ColorANSI256
    ColorRGB
)
```

<a name="ComponentRenderer"></a>
## type ComponentRenderer

ComponentRenderer owns a Screen and drives layout \+ paint on every render frame. Render\(\) computes layout and paints the element tree to the screen atomically. Cell\-level diffing in Screen ensures only changed cells emit ANSI output.

Renderer is not thread\-safe; coordinate all Render calls via your app's event loop or use explicit synchronization.

```go
type ComponentRenderer struct {
    // contains filtered or unexported fields
}
```

<a name="NewRenderer"></a>
### func NewRenderer

```go
func NewRenderer(screen *Screen) *ComponentRenderer
```

NewRenderer creates a new renderer backed by the given screen. screen must be non\-nil; panics if screen is nil.

Multiple renderers can exist; typically your app uses a single global Renderer.

<a name="ComponentRenderer.Render"></a>
### func \(\*ComponentRenderer\) Render

```go
func (r *ComponentRenderer) Render(next Element)
```

Render computes layout and paints element tree to the screen in one atomic pass.

Steps: 1. Resolve deferred content \(ContentBuilder nodes\) with their measured sizes. 2. Build layout tree from element tree. 3. Measure intrinsic size and adjust screen height if needed. 4. Compute layout \(position and size each node\). 5. Paint main tree, deferring overlays. 6. Paint deferred overlays on top. 7. Ensure scrollback room if content overflows terminal viewport.

Cell\-level diffing \(SetCell\) marks only genuinely changed cells dirty, so Flush emits minimal ANSI output even though paint visits every cell.

If content's wrapped text \(reflow\) causes height to expand after initial layout, Render re\-measures and re\-layouts automatically to propagate the new height to containers.

<a name="Context"></a>
## type Context

Context carries a value down the component tree without prop\-drilling. The zero value is not usable — construct with CreateContext.

Example:

```
var ThemeCtx = retui.CreateContext("light")

func App(...) retui.Element {
    return ThemeCtx.Provide("dark", func() retui.Element {
        return MyPage()
    })
}

func MyPage() retui.Element {
    theme := retui.UseContext(ThemeCtx)  // "dark"
    ...
}
```

```go
type Context[T any] struct {
    // contains filtered or unexported fields
}
```

<a name="CreateContext"></a>
### func CreateContext

```go
func CreateContext[T any](defaultValue T) *Context[T]
```

CreateContext returns a Context with the given default value.

<a name="Context[T].Provide"></a>
### func \(\*Context\[T\]\) Provide

```go
func (c *Context[T]) Provide(value T, render func() Element) Element
```

Provide pushes value, runs render, then pops via defer. A panic in render still unwinds the stack cleanly.

<a name="Direction"></a>
## type Direction

Direction specifies the primary layout axis along which children are arranged.

```go
type Direction int
```

<a name="Row"></a>

```go
const (
    // Row arranges children left-to-right (horizontal main axis).
    Row Direction = iota
    // Column arranges children top-to-bottom (vertical main axis).
    Column
)
```

<a name="Effect"></a>
## type Effect

Effect holds a registered side\-effect.

```go
type Effect struct {
    // contains filtered or unexported fields
}
```

<a name="Element"></a>
## type Element

Element represents a node in the UI element tree.

Field Usage by ElementType:

ElementBox — Container:

- Uses: Type, Children, Layout, Style
- Renders: Background fill, border, arranged children
- Ignores: Text, Markdown, MarkdownText, Wrap, Render, ContentBuilder, OverlayX/Y

ElementText — Single line:

- Uses: Type, Text, Style
- Renders: Text on single line
- Ignores: Children, Markdown, Wrap, Layout, Render, ContentBuilder, OverlayX/Y
- Note: Width determined by text length; height is 1

ElementMultilineText — Multi\-line text:

- Uses: Type, Text, Wrap, Style, Layout
- If Wrap=true: Text wraps at container width via reflow callback
- If Wrap=false: Text split on newlines; no word wrapping
- Renders: Multiple lines of text
- Ignores: Children, Markdown, MarkdownText, Render, ContentBuilder, OverlayX/Y

ElementMarkdown — Rendered markdown:

- Uses: Type, MarkdownText, Markdown, Style, Layout
- MarkdownText: Raw markdown source \(re\-parsed on resize if non\-empty\)
- Markdown: Pre\-parsed output \(used if MarkdownText is empty\)
- Renders: Multi\-line text with per\-character styling
- Ignores: Text, Wrap, Children, Render, ContentBuilder, OverlayX/Y

ElementOverlay — Absolute positioned:

- Uses: Type, OverlayX, OverlayY, Children, Style
- Layout: Zero\-size in flow; children painted at absolute position
- Renders: Children at \(OverlayX, OverlayY\) regardless of parent layout
- Ignores: Text, Markdown, Wrap, Layout, Render, ContentBuilder

Hooks & Callbacks:

- Render: Hook function for component composition \(called during render\)
- ContentBuilder: Size\-aware builder \(called before layout with available dimensions\)
- Props.Values: Custom state accessible to hooks

Example \(Box with children\):

```
Element{
    Type: ElementBox,
    Layout: LayoutProps{Direction: Row, WidthSizing: Grow(1)},
    Children: []Element{...},
}
```

Example \(Text\):

```
Element{
    Type: ElementText,
    Text: "Hello, world!",
    Style: Style{foreground: ColorGreen},
}
```

Example \(Markdown\):

```
Element{
    Type: ElementMarkdown,
    MarkdownText: "# Title\n\n**Bold** text",
    Layout: LayoutProps{WidthSizing: Grow(1)},
}
```

```go
type Element struct {
    // Id is an optional unique identifier for this element.
    // Not required; used for debugging or external tracking.
    Id  string

    // Type identifies the element category (Box, Text, Markdown, etc.).
    // Determines which fields are active and how element behaves.
    Type ElementType

    // Key is an optional hint for element identity across renders.
    // Used by reconciliation to match elements (e.g., for lists).
    // Helps preserve element state (focus, scroll position) during updates.
    Key string

    // Text is the content for ElementText and ElementMultilineText.
    // Ignored by other element types.
    Text string

    // Wrap indicates whether ElementMultilineText should wrap at container width.
    // If true: text wraps with word boundaries, hard-breaks long words
    // If false: text split on newlines only, no word wrapping
    // Ignored by other element types.
    Wrap bool

    // Style carries colors, fonts, borders, background, etc.
    // Applied to this element and inherited by children (merged in render).
    // See Style type for available properties.
    Style Style

    // Layout contains sizing, positioning, and child arrangement properties.
    // Uses Direction (Row/Column), sizing modes (Fixed/Grow/Fit/Percent),
    // padding, gap, alignment, and justify.
    // Mirrors layout.go exactly; this is what the layout algorithm uses.
    Layout LayoutProps

    // Children is the list of child elements for ElementBox and ElementOverlay.
    // Ignored by text elements (ElementText, ElementMultilineText, ElementMarkdown).
    Children []Element

    // Render is an optional hook function for component composition.
    // If set, called during render pass to allow component logic.
    // Receives the Element itself as props.
    // Return value may be used for recursive rendering (implementation-dependent).
    Render func(props Element) Element

    // Props carries layout configuration (legacy) and custom hook state.
    // Prefer Element.Layout for layout properties; Props.Values for hook state.
    // See Props documentation for details.
    Props Props

    // Markdown is the pre-parsed markdown output (lines with styling).
    // Populated by render engine; read-only from outside.
    // Do NOT set directly; set MarkdownText instead.
    Markdown MarkdownContent

    // MarkdownText is the raw markdown source for ElementMarkdown.
    // If non-empty, render engine re-parses it and updates Markdown.
    // If empty, Markdown field is used as-is.
    // Allows content to update markdown dynamically (e.g., on resize).
    MarkdownText string

    // ContentBuilder is an optional size-aware content builder.
    // Called before layout with allocated width/height.
    // Returns an Element that replaces this node for rendering.
    // Allows responsive UI that changes structure based on available space.
    // If set, this element's children are ignored until ContentBuilder returns.
    ContentBuilder func(width, height int) Element

    // OverlayX is the absolute column (left edge) for ElementOverlay.
    // Only used when Type == ElementOverlay.
    // Screen-relative (not parent-relative).
    // Negative values allowed (off-screen overlay renders nothing).
    OverlayX int

    // OverlayY is the absolute row (top edge) for ElementOverlay.
    // Only used when Type == ElementOverlay.
    // Screen-relative (not parent-relative).
    // Negative values allowed (off-screen overlay renders nothing).
    OverlayY int
}
```

<a name="Box"></a>
### func Box

```go
func Box(props Props, style Style, children ...Element) Element
```



<a name="If"></a>
### func If

```go
func If(condition bool, choice1 Element, choice2 Element) Element
```

If returns choice1 when condition is true and choice2 otherwise. It is a ternary\-style helper for composing element trees inline, since both branches are evaluated before the call, use it for picking between already\-constructed elements rather than for guarding expensive work.

<a name="Markdown"></a>
### func Markdown

```go
func Markdown(markdown string, style Style) Element
```

Markdown renders a markdown string with rich formatting including headers, bold, italic, code, links, lists, blockquotes, and tables. The content fills its container's available width and wraps automatically.

<a name="MultilineText"></a>
### func MultilineText

```go
func MultilineText(text string, style Style) Element
```

MultilineText renders text that may contain '\\n' line breaks. Each '\\n' starts a new row at the element's left edge; the intrinsic width is the longest line and the intrinsic height is the line count.

<a name="Overlay"></a>
### func Overlay

```go
func Overlay(x, y int, children ...Element) Element
```

Overlay places children at absolute screen position \(x, y\), completely outside normal flow layout. The node itself is zero\-size so it does not push siblings or affect the parent's dimensions. Children are painted after all flow\-positioned siblings, so they always appear on top.

Use for modal dialogs, tooltips, popups — anything that should float above the rest of the UI at a fixed screen position. Position in the element tree does not matter — it always paints at \(x, y\).

<a name="Text"></a>
### func Text

```go
func Text(text string, style Style) Element
```



<a name="WrappedText"></a>
### func WrappedText

```go
func WrappedText(text string, style Style) Element
```

WrappedText renders text that fills its container's available width and breaks on word boundaries to fit. Existing '\\n' characters still force a new row. Width grows to the parent's cross\-axis size; height adapts to the resulting wrapped line count.

<a name="ElementType"></a>
## type ElementType

ElementType identifies the category and behavior of an Element.

Each ElementType determines:

- Which Element fields are actually used
- How the element is laid out and positioned
- How the element is rendered to the screen
- Which sizing and alignment rules apply

See Element documentation for detailed field usage per type.

```go
type ElementType int
```

<a name="ElementBox"></a>

```go
const (
    // ElementBox is a container for laying out children.
    //
    // Usage:
    //   - Type: ElementBox
    //   - Uses: Children, Layout, Style
    //   - Ignores: Text, Markdown, MarkdownText, Wrap, ContentBuilder, etc.
    //
    // Behavior:
    //   - Renders background fill and optional border
    //   - Arranges children via Layout (direction, gap, alignment, justify)
    //   - Children sized/positioned according to Layout properties
    //   - Supports responsive sizing (Grow, Fit, Percent)
    //
    // Example:
    //   Element{
    //       Type: ElementBox,
    //       Layout: LayoutProps{
    //           Direction: Row,
    //           WidthSizing: Grow(1),
    //           HeightSizing: Fit(),
    //       },
    //       Children: []Element{...},
    //   }
    ElementBox ElementType = iota

    // ElementText is a single line of unformatted text.
    //
    // Usage:
    //   - Type: ElementText
    //   - Uses: Text, Style
    //   - Ignores: Children, Markdown, Wrap, Layout, ContentBuilder, etc.
    //
    // Behavior:
    //   - Renders text on a single line
    //   - Width determined by text length (rune count with RuneWidth)
    //   - Height is always 1 row
    //   - No wrapping or line breaking
    //   - Text exceeding container width is clipped
    //
    // Example:
    //   Element{
    //       Type: ElementText,
    //       Text: "Status: OK",
    //       Style: Style{foreground: ColorGreen},
    //   }
    ElementText

    // ElementMultilineText is text that may span multiple lines.
    //
    // Usage:
    //   - Type: ElementMultilineText
    //   - Uses: Text, Wrap, Style, Layout
    //   - Ignores: Children, Markdown, ContentBuilder, etc.
    //
    // Behavior:
    //   - If Wrap=true: Text wraps at container width using reflow callback
    //   - If Wrap=false: Text split on newlines (\n); no word wrapping
    //   - Wrapping respects word boundaries (space-separated)
    //   - Words longer than width are hard-broken at width boundary
    //   - Height expands to fit all lines
    //
    // Reflow Callback:
    //   - When Wrap=true, layout engine calls reflow callback with allocated width
    //   - Callback computes wrapped height; updates Element.Layout
    //   - Second layout pass propagates height to ancestor containers
    //   - Allows containers around wrapped text to grow to fit content
    //
    // Example (Wrapped):
    //   Element{
    //       Type: ElementMultilineText,
    //       Text: "Lorem ipsum dolor sit amet...",
    //       Wrap: true,
    //       Layout: LayoutProps{WidthSizing: Grow(1)},
    //   }
    //
    // Example (Raw):
    //   Element{
    //       Type: ElementMultilineText,
    //       Text: "Line 1\nLine 2\nLine 3",
    //       Wrap: false,
    //   }
    ElementMultilineText

    // ElementMarkdown is rendered markdown with per-character styling.
    //
    // Usage:
    //   - Type: ElementMarkdown
    //   - Uses: MarkdownText (source), Markdown (rendered), Style, Layout
    //   - Ignores: Text, Wrap, Children, ContentBuilder, etc.
    //
    // Behavior:
    //   - MarkdownText: Raw markdown source (re-parsed on resize if non-empty)
    //   - Markdown: Pre-parsed output (used if MarkdownText is empty)
    //   - Renders multi-line text with inline styling (bold, italic, colors)
    //   - Each character has its own style (color, formatting)
    //   - Automatically wraps at container width during render
    //   - Height expands to fit rendered markdown
    //
    // Reflow:
    //   - Similar to wrapped text: reflow callback computes height from width
    //   - Allows containers around markdown to grow to fit content
    //
    // Example:
    //   Element{
    //       Type: ElementMarkdown,
    //       MarkdownText: "# Title\n\nParagraph with **bold** and *italic*",
    //       Layout: LayoutProps{WidthSizing: Grow(1)},
    //   }
    ElementMarkdown

    // ElementOverlay is positioned at absolute screen coordinates.
    //
    // Usage:
    //   - Type: ElementOverlay
    //   - Uses: OverlayX, OverlayY, Children, Style
    //   - Ignores: Text, Markdown, Wrap, Layout, ContentBuilder, etc.
    //
    // Behavior:
    //   - Children are positioned at absolute coordinates (OverlayX, OverlayY)
    //   - Ignores normal flow layout entirely
    //   - Zero-size in flow (doesn't push siblings or expand parents)
    //   - Painted on top of main tree (deferred to final render pass)
    //   - Used for modals, tooltips, dropdowns
    //   - Negative coordinates allowed (off-screen overlays render nothing)
    //
    // Positioning:
    //   - OverlayX: Column (left edge)
    //   - OverlayY: Row (top edge)
    //   - Both are screen-relative (not parent-relative)
    //
    // Example (Modal):
    //   Element{
    //       Type: ElementOverlay,
    //       OverlayX: 10,
    //       OverlayY: 5,
    //       Children: []Element{
    //           {Type: ElementBox, Text: "Modal content"},
    //       },
    //   }
    ElementOverlay
)
```

<a name="FocusManager"></a>
## type FocusManager

FocusManager manages keyboard focus and event routing for UI components.

Focus System:

- Current: The component that receives keyboard input
- Order: Tab navigation order for cycling through focusable components
- Stack: Modal/dialog stack for nested focus contexts
- Capture: A component that intercepts all keyboard events \(e.g., dropdown\)

Focus Flow:

1. A component is set as current \(focused\)
2. User presses Tab/Shift\+Tab to navigate focus order
3. A component can capture focus to handle all keys \(dropdown, modal\)
4. Modals push/pop focus on the stack
5. UseFocusedKey hook checks if current component should handle the key

Thread Safety:

```
FocusManager is thread-safe via RWMutex. All operations are protected.
Typical use is single-threaded (TUI event loop), but concurrent access
from multiple goroutines is safe.
```

```go
type FocusManager struct {
    // contains filtered or unexported fields
}
```

<a name="NewFocusManager"></a>
### func NewFocusManager

```go
func NewFocusManager() *FocusManager
```

NewFocusManager creates a new focus manager with no initial focus.

Returns: A fully initialized FocusManager ready for use.

<a name="FocusManager.CaptureFocus"></a>
### func \(\*FocusManager\) CaptureFocus

```go
func (f *FocusManager) CaptureFocus(id string)
```

CaptureFocus sets a component as the exclusive keyboard handler.

Used for dropdowns, autocomplete, and other input\-capturing components:

- All keyboard input goes to the capturing component
- Other components don't receive keys
- Tab/Shift\+Tab navigation is blocked
- Typically released when dropdown closes or user presses Escape

Parameters:

```
id: The component ID to capture focus.
```

Only one component can have capture at a time. Calling CaptureFocus again replaces the previous capture.

<a name="FocusManager.CapturedBy"></a>
### func \(\*FocusManager\) CapturedBy

```go
func (f *FocusManager) CapturedBy() string
```

CapturedBy returns the ID of the component with keyboard capture, or empty string.

Returns: The ID of the capturing component, or empty string if no capture.

<a name="FocusManager.Current"></a>
### func \(\*FocusManager\) Current

```go
func (f *FocusManager) Current() string
```

Current returns the ID of the component that currently has focus.

Returns: The ID of the focused component, or empty string if no focus.

<a name="FocusManager.HasCapture"></a>
### func \(\*FocusManager\) HasCapture

```go
func (f *FocusManager) HasCapture(id string) bool
```

HasCapture checks if a specific component has keyboard capture.

Parameters:

```
id: The component ID to check.
```

Returns: True if id has capture, false otherwise.

<a name="FocusManager.IsFocused"></a>
### func \(\*FocusManager\) IsFocused

```go
func (f *FocusManager) IsFocused(id string) bool
```

IsFocused checks if the specified component ID is the current focus.

Parameters:

```
id: The component ID to check.
```

Returns: True if id is currently focused, false otherwise.

<a name="FocusManager.Next"></a>
### func \(\*FocusManager\) Next

```go
func (f *FocusManager) Next() string
```

Next moves focus to the next component in the tab order.

Behavior:

- If current is at the end, wraps to the start \(circular\)
- Does nothing if order is empty
- Does nothing if capture is active \(dropdown/modal has focus lock\)
- Typically called when user presses Tab

Returns: The ID of the new focus, or empty string if no change.

<a name="FocusManager.PopFocus"></a>
### func \(\*FocusManager\) PopFocus

```go
func (f *FocusManager) PopFocus() string
```

PopFocus pops the focus stack and restores the previous focus.

Used when closing a modal dialog:

- Current focus \(modal\) is discarded
- Previous focus is restored from the stack
- Returns to normal focus flow

If the stack is empty, does nothing \(stays at current focus\).

Returns: The ID of the restored focus, or current if stack is empty.

<a name="FocusManager.Prev"></a>
### func \(\*FocusManager\) Prev

```go
func (f *FocusManager) Prev() string
```

Prev moves focus to the previous component in the tab order.

Behavior:

- If current is at the start, wraps to the end \(circular\)
- Does nothing if order is empty
- Does nothing if capture is active
- Typically called when user presses Shift\+Tab

Returns: The ID of the new focus, or empty string if no change.

<a name="FocusManager.PushFocus"></a>
### func \(\*FocusManager\) PushFocus

```go
func (f *FocusManager) PushFocus(id string)
```

PushFocus pushes the current focus onto the stack and sets a new focus.

Used when opening a modal dialog:

- Current focus is saved to the stack
- New focus \(modal\) becomes current
- Focus capture is automatically released \(modal doesn't inherit capture\)

Parameters:

```
id: The component ID for the modal/dialog.
```

When the modal closes, call PopFocus\(\) to restore the previous focus.

<a name="FocusManager.ReleaseFocus"></a>
### func \(\*FocusManager\) ReleaseFocus

```go
func (f *FocusManager) ReleaseFocus()
```

ReleaseFocus releases keyboard capture.

Called when the capturing component \(dropdown, etc.\) closes. Restores normal focus flow to the current component.

If no component has capture, does nothing.

<a name="FocusManager.SetFocus"></a>
### func \(\*FocusManager\) SetFocus

```go
func (f *FocusManager) SetFocus(id string)
```

SetFocus sets the current focus to the specified component ID.

Parameters:

```
id: The component ID to focus. Must be in the focus order.
```

If id is not in the order list, it still becomes current, but Tab navigation won't cycle through it. This is allowed for components that are focusable but not tab\-navigable \(e.g., dynamically added components\).

<a name="FocusManager.SetOrder"></a>
### func \(\*FocusManager\) SetOrder

```go
func (f *FocusManager) SetOrder(order []string) error
```

SetOrder sets the tab navigation order for cycling through components.

Parameters:

```
order: A list of component IDs in the order they should be visited
       when the user presses Tab or Shift+Tab.
```

Validation:

- All IDs must be non\-empty
- No duplicate IDs allowed
- Returns error if validation fails

If SetOrder fails, the previous order remains unchanged.

<a name="Justify"></a>
## type Justify

Justify specifies how children distribute along the main axis when there is extra space.

```go
type Justify int
```

<a name="JustifyStart"></a>

```go
const (
    // JustifyStart: pack children at the start, extra space at the end (default).
    JustifyStart Justify = iota
    // JustifyEnd: pack children at the end, extra space at the start.
    JustifyEnd
    // JustifyCenter: center children, extra space split equally on both sides.
    JustifyCenter
    // JustifySpaceBetween: equal spacing between children, no space before first or after last.
    JustifySpaceBetween
    // JustifySpaceAround: equal space around each child (half-space at edges).
    JustifySpaceAround
)
```

<a name="Key"></a>
## type Key

Key represents a single keyboard event. Either Code or Rune is set. When Code == KeyPaste, Paste holds the full pasted text.

```go
type Key struct {
    Code     KeyCode
    Rune     rune
    Paste    string
    Consumed bool
}
```

<a name="CurrentKey"></a>CurrentKey is the most recent keyboard input, available to all hook functions. Cleared after each render frame so hooks must check it during their frame.

NOT thread\-safe; read/write from the event loop goroutine only.

```go
var CurrentKey Key
```

<a name="ParseKey"></a>
### func ParseKey

```go
func ParseKey(b []byte) Key
```

ParseKey converts raw terminal bytes into a Key.

<a name="Key.IsFunctionKey"></a>
### func \(Key\) IsFunctionKey

```go
func (k Key) IsFunctionKey() bool
```

IsFunctionKey returns true if the key is F1\-F12

<a name="Key.IsModifier"></a>
### func \(Key\) IsModifier

```go
func (k Key) IsModifier() bool
```

IsModifier returns true if the key is a modifier combination

<a name="Key.IsNavigationKey"></a>
### func \(Key\) IsNavigationKey

```go
func (k Key) IsNavigationKey() bool
```

IsNavigationKey returns true for arrow keys, Home, End, PageUp, PageDown

<a name="Key.IsPrintable"></a>
### func \(Key\) IsPrintable

```go
func (k Key) IsPrintable() bool
```

IsPrintable returns true if the key is a printable character

<a name="Key.String"></a>
### func \(Key\) String

```go
func (k Key) String() string
```

String returns a readable name for the key

<a name="KeyCode"></a>
## type KeyCode

KeyCode identifies special \(non\-printable\) keys.

```go
type KeyCode int
```

<a name="KeyNone"></a>

```go
const (
    KeyNone KeyCode = iota

    // ─── Function Keys ──────────────────────────────────────────────────────
    KeyF1
    KeyF2
    KeyF3
    KeyF4
    KeyF5
    KeyF6
    KeyF7
    KeyF8
    KeyF9
    KeyF10
    KeyF11
    KeyF12

    // ─── Navigation Keys ──────────────────────────────────────────────────
    KeyUp
    KeyDown
    KeyLeft
    KeyRight
    KeyHome
    KeyEnd
    KeyPageUp
    KeyPageDown
    KeyInsert
    KeyDelete

    // ─── Special Keys ──────────────────────────────────────────────────────
    KeyEnter
    KeyBackspace
    KeyEscape
    KeyTab
    KeyShiftTab
    KeySpace
    KeyCtrlC
    KeyPaste

    // ─── Ctrl+Letter Keys ──────────────────────────────────────────────────
    KeyCtrlA
    KeyCtrlB
    KeyCtrlD
    KeyCtrlE
    KeyCtrlF
    KeyCtrlG
    KeyCtrlH // 0x08 - same as Backspace
    KeyCtrlI // 0x09 - same as Tab
    KeyCtrlJ // 0x0A - same as Enter
    KeyCtrlK
    KeyCtrlL
    KeyCtrlM // 0x0D - same as Enter
    KeyCtrlN
    KeyCtrlO
    KeyCtrlP
    KeyCtrlQ
    KeyCtrlR
    KeyCtrlS
    KeyCtrlT
    KeyCtrlU
    KeyCtrlV
    KeyCtrlW
    KeyCtrlX
    KeyCtrlY
    KeyCtrlZ

    // ─── Alt+Letter Keys ──────────────────────────────────────────────────
    KeyAltA
    KeyAltB
    KeyAltC
    KeyAltD
    KeyAltE
    KeyAltF
    KeyAltG
    KeyAltH
    KeyAltI
    KeyAltJ
    KeyAltK
    KeyAltL
    KeyAltM
    KeyAltN
    KeyAltO
    KeyAltP
    KeyAltQ
    KeyAltR
    KeyAltS
    KeyAltT
    KeyAltU
    KeyAltV
    KeyAltW
    KeyAltX
    KeyAltY
    KeyAltZ
)
```

<a name="KeyScanner"></a>
## type KeyScanner

KeyScanner is a stateful parser that converts raw stdin reads into Key events.

```go
type KeyScanner struct {
    // contains filtered or unexported fields
}
```

<a name="KeyScanner.Feed"></a>
### func \(\*KeyScanner\) Feed

```go
func (s *KeyScanner) Feed(b []byte) []Key
```

Feed consumes one chunk of stdin bytes and returns any complete Key events.

<a name="LayoutNode"></a>
## type LayoutNode

LayoutNode is a node in the layout tree. It describes sizing, positioning, and child\-arrangement rules for a rectangular region.

Build via builder methods \(WithDirection, WithSize, WithChildren, etc.\) and compute layout via ComputeLayout\(root, bounds\).

Fields are private; use public methods to construct and configure nodes.

```go
type LayoutNode struct {
    // Direction is the primary layout axis (Row or Column).
    Direction Direction
    // WidthSizing describes how width is computed.
    WidthSizing Sizing
    // HeightSizing describes how height is computed.
    HeightSizing Sizing
    // Children is the list of child nodes.
    Children []*LayoutNode
    // contains filtered or unexported fields
}
```

<a name="NewLayout"></a>
### func NewLayout

```go
func NewLayout() *LayoutNode
```

NewLayout creates a new, empty LayoutNode with default settings: \- Direction: Row \- All sizing: SizingFit \(shrink\-wrap\) \- Alignment: AlignStretch \- Justify: JustifyStart \- No padding, gap, or children

<a name="LayoutNode.WithAlign"></a>
### func \(\*LayoutNode\) WithAlign

```go
func (l *LayoutNode) WithAlign(alignment Alignment) *LayoutNode
```

WithAlign sets cross\-axis alignment \(AlignStretch, AlignStart, AlignCenter, AlignEnd\) and returns the node for chaining. Default is AlignStretch, which makes children fill the full cross\-axis space.

<a name="LayoutNode.WithChildren"></a>
### func \(\*LayoutNode\) WithChildren

```go
func (l *LayoutNode) WithChildren(children ...*LayoutNode) *LayoutNode
```

WithChildren sets the child nodes and returns the node for chaining.

<a name="LayoutNode.WithDirection"></a>
### func \(\*LayoutNode\) WithDirection

```go
func (l *LayoutNode) WithDirection(dir Direction) *LayoutNode
```

WithDirection sets the layout direction and returns the node for chaining. Direction is the primary axis along which children are arranged.

<a name="LayoutNode.WithGap"></a>
### func \(\*LayoutNode\) WithGap

```go
func (l *LayoutNode) WithGap(value int) *LayoutNode
```

WithGap sets the minimum spacing between adjacent children on the main axis and returns the node for chaining.

<a name="LayoutNode.WithHeight"></a>
### func \(\*LayoutNode\) WithHeight

```go
func (l *LayoutNode) WithHeight(h Sizing) *LayoutNode
```

WithHeight sets height sizing and returns the node for chaining.

<a name="LayoutNode.WithJustify"></a>
### func \(\*LayoutNode\) WithJustify

```go
func (l *LayoutNode) WithJustify(value Justify) *LayoutNode
```

WithJustify sets main\-axis space distribution \(JustifyStart, JustifyEnd, etc.\) and returns the node for chaining. Default is JustifyStart \(pack at the start\).

<a name="LayoutNode.WithMargin"></a>
### func \(\*LayoutNode\) WithMargin

```go
func (l *LayoutNode) WithMargin(top, right, bottom, left int) *LayoutNode
```

WithMargin sets exterior spacing \(top, right, bottom, left\) and returns the node for chaining. Margin reserves space outside the node's border box, between it and adjacent siblings or the parent's edge — unlike padding, which is interior space. Margin and Gap both add spacing between siblings and are additive, not merged.

<a name="LayoutNode.WithMarginUniform"></a>
### func \(\*LayoutNode\) WithMarginUniform

```go
func (l *LayoutNode) WithMarginUniform(margin int) *LayoutNode
```

WithMarginUniform sets uniform margin on all sides and returns the node for chaining.

<a name="LayoutNode.WithPadding"></a>
### func \(\*LayoutNode\) WithPadding

```go
func (l *LayoutNode) WithPadding(top, right, bottom, left int) *LayoutNode
```

WithPadding sets interior spacing \(top, right, bottom, left\) and returns the node for chaining. Padding reserves space between the node's bounds and its children's layout area.

<a name="LayoutNode.WithPaddingUniform"></a>
### func \(\*LayoutNode\) WithPaddingUniform

```go
func (l *LayoutNode) WithPaddingUniform(padding int) *LayoutNode
```

WithPaddingUniform sets uniform padding on all sides and returns the node for chaining.

<a name="LayoutNode.WithReflow"></a>
### func \(\*LayoutNode\) WithReflow

```go
func (l *LayoutNode) WithReflow(fn func(crossAxisSize int) int) *LayoutNode
```

WithReflow sets a callback for responsive main\-axis sizing based on cross\-axis constraints. Used for content whose height depends on width \(e.g. wrapped text, wrapped flex items\).

The callback receives the node's resolved cross\-axis size \(width in Column, height in Row\) and must return the updated main\-axis size \(height in Column, width in Row\). The returned value should be non\-negative and clamped to the available space; clamping is not automatic.

Reflow is invoked during the layout pass:

- For Column parents: immediately after cross\-axis \(width\) is resolved, before descending into children.
- For Row parents: after width distribution is complete \(after grow sizing\).

If a reflow callback is present anywhere in the tree, ComputeLayout performs a second measure\+layout pass to propagate updated heights to ancestors.

<a name="LayoutNode.WithSize"></a>
### func \(\*LayoutNode\) WithSize

```go
func (l *LayoutNode) WithSize(w, h Sizing) *LayoutNode
```

WithSize sets both width and height sizing and returns the node for chaining.

<a name="LayoutNode.WithWidth"></a>
### func \(\*LayoutNode\) WithWidth

```go
func (l *LayoutNode) WithWidth(w Sizing) *LayoutNode
```

WithWidth sets width sizing and returns the node for chaining.

<a name="LayoutProps"></a>
## type LayoutProps

LayoutProps contains the layout configuration for an element.

LayoutProps mirrors layout.go exactly and is the authoritative source of layout properties used by the layout algorithm. Never modify the layout algorithm or layout.go without updating LayoutProps.

Properties:

- Direction: Row \(left\-to\-right\) or Column \(top\-to\-bottom\)
- WidthSizing, HeightSizing: Sizing modes \(Fixed, Grow, Fit, Percent\)
- Padding: Interior spacing \[top, right, bottom, left\]
- Gap: Spacing between adjacent children
- Align: Cross\-axis alignment \(Start, Center, End, Stretch\)
- Justify: Main\-axis distribution \(Start, End, Center, SpaceBetween, SpaceAround\)

Sizing Modes:

- SizingFixed\(n\): Exactly n pixels/columns
- SizingGrow\(weight\): Claim remaining space \(weight determines share\)
- SizingFit\(\): Shrink\-wrap to children's intrinsic size
- SizingPercent\(n\): Percentage \(0\-100\) of parent's resolved size

Alignment \(Cross\-Axis\):

- AlignStart: Start of cross axis \(top in Row, left in Column\)
- AlignCenter: Centered on cross axis
- AlignEnd: End of cross axis \(bottom in Row, right in Column\)
- AlignStretch: Full cross\-axis size \(default\)

Justify \(Main\-Axis Distribution\):

- JustifyStart: Pack at start, extra space at end \(default\)
- JustifyEnd: Pack at end, extra space at start
- JustifyCenter: Center children, split extra space equally
- JustifySpaceBetween: Equal space between children
- JustifySpaceAround: Equal space around each child

Example:

```
LayoutProps{
    Direction:    Row,
    WidthSizing:  Grow(1),
    HeightSizing: Fixed(3),
    PaddingTop:    1,
    PaddingRight:  2,
    PaddingBottom: 1,
    PaddingLeft:   2,
    Gap:           1,
    Align:         AlignCenter,
    Justify:       JustifySpaceBetween,
}
```

```go
type LayoutProps struct {
    // Direction is the primary layout axis.
    Direction Direction
    // WidthSizing determines how width is computed.
    WidthSizing Sizing
    // HeightSizing determines how height is computed.
    HeightSizing Sizing
    // PaddingTop is interior spacing at top.
    PaddingTop int
    // PaddingRight is interior spacing at right.
    PaddingRight int
    // PaddingBottom is interior spacing at bottom.
    PaddingBottom int
    // PaddingLeft is interior spacing at left.
    PaddingLeft int

    // MarginTop is exterior spacing at top — space between this element and
    // whatever is above it, outside its border box.
    MarginTop int
    // MarginRight is exterior spacing at right.
    MarginRight int
    // MarginBottom is exterior spacing at bottom.
    MarginBottom int
    // MarginLeft is exterior spacing at left.
    MarginLeft int

    // Gap is minimum spacing between adjacent children on main axis.
    Gap int
    // Align controls child alignment on cross axis.
    Align Alignment
    // Justify controls space distribution along main axis.
    Justify Justify
}
```

<a name="LogLevel"></a>
## type LogLevel

LogLevel represents the logging level

```go
type LogLevel int
```

<a name="LevelDebug"></a>

```go
const (
    LevelDebug LogLevel = iota
    LevelSuccess
    LevelInfo
    LevelWarn
    LevelError
    LevelFatal
)
```

<a name="GetLogLevel"></a>
### func GetLogLevel

```go
func GetLogLevel() LogLevel
```

GetLogLevel returns the current log level

<a name="LogLevel.String"></a>
### func \(LogLevel\) String

```go
func (l LogLevel) String() string
```

String returns the string representation of the log level

<a name="Logger"></a>
## type Logger

WithField returns a new logger with a field \(for structured logging\)

```go
type Logger struct {
    // contains filtered or unexported fields
}
```

<a name="NewLogger"></a>
### func NewLogger

```go
func NewLogger(fields map[string]interface{}) *Logger
```

NewLogger creates a new logger with fields

<a name="WithFields"></a>
### func WithFields

```go
func WithFields(fields map[string]interface{}) *Logger
```

WithFields creates a new logger with additional fields

<a name="Logger.AddField"></a>
### func \(\*Logger\) AddField

```go
func (l *Logger) AddField(key string, value interface{}) *Logger
```

AddField adds a field to the logger

<a name="Logger.Debug"></a>
### func \(\*Logger\) Debug

```go
func (l *Logger) Debug(message string)
```

Debug logs a debug message with fields

<a name="Logger.Error"></a>
### func \(\*Logger\) Error

```go
func (l *Logger) Error(message string)
```

Error logs an error message with fields

<a name="Logger.Info"></a>
### func \(\*Logger\) Info

```go
func (l *Logger) Info(message string)
```

Info logs an info message with fields

<a name="Logger.Warn"></a>
### func \(\*Logger\) Warn

```go
func (l *Logger) Warn(message string)
```

Warn logs a warning message with fields

<a name="MarkdownContent"></a>
## type MarkdownContent

MarkdownContent carries pre\-parsed markdown with per\-character styling.

The Lines field contains rendered output: each line is a sequence of styled cells \(character \+ color/formatting\).

Usage:

- Lines is exported for reading by render engine and tests
- Do NOT modify Lines directly; instead set Element.MarkdownText
- To change markdown: set MarkdownText, let render re\-parse it
- MarkdownContent is updated automatically during render

Note: markdownLine is private \(intentional\). Markdown parsing is internal to render.go; external code should not construct lines manually. This ensures rendering consistency and simplifies the API.

Example:

```
elem := Element{
    Type:         ElementMarkdown,
    MarkdownText: "# Title\n\nParagraph",  // Set raw markdown
    // Markdown will be populated by render engine
}
```

```go
type MarkdownContent struct {
    // Lines is the rendered output; read-only from outside render engine.
    // Each line is a sequence of styled cells (character + styling).
    // Updated by render engine when MarkdownText is set.
    Lines []markdownLine
}
```

<a name="OverlayManager"></a>
## type OverlayManager



```go
type OverlayManager struct {
    // contains filtered or unexported fields
}
```

<a name="OverlayRegion"></a>
## type OverlayRegion

OverlayRegion is a pre\-rendered rectangular region of cells. Built by PaintOverlay and written into the screen buffer by ApplyOverlay.

The window system builds one OverlayRegion per window via PaintOverlay, then calls ApplyOverlay to stamp it onto the screen at \(x, y\) AFTER the main layout has been painted. This gives true floating windows with correct height bounds — independent of the element tree.

```go
type OverlayRegion struct {
    // contains filtered or unexported fields
}
```

<a name="Props"></a>
## type Props

Props carries layout configuration and generic hook properties.

Two Purposes:

1. Layout properties \(Direction, Gap, Padding, Align, Justify, Width, Height\) These mirror LayoutProps but with a different API \(Props uses Width/Height, LayoutProps uses WidthSizing/HeightSizing\). Kept for backward compatibility.
2. Generic hook data \(Values map\) Custom application state accessible to hooks via Props.Get\(\)

Sizing Defaults:

- DO NOT rely on Props width/height defaults
- Always use NewProps\(\) to get correct defaults
- Zero value Props produces Fixed\(0\) width/height \(rarely useful\)
- NewProps\(\) sets Width: Fit\(\), Height: Fit\(\) \(sensible defaults\)

Relationship with LayoutProps:

- Element.Layout \(LayoutProps\) is the authoritative layout configuration
- It mirrors layout.go exactly and is what the layout algorithm uses
- Props.Width/Height are shadows for backward compatibility
- New code should set Element.Layout, not Element.Props
- Props.Values is useful for custom hook state

Example \(Correct Usage\):

```
props := NewProps()
props.Values["counter"] = 42
```

Example \(Legacy Usage — still works\):

```
props := Props{Width: Grow(1), Height: Fit(), Values: make(map[string]any)}
```

```go
type Props struct {
    // Direction is the primary layout axis (Row or Column).
    Direction Direction
    // Gap is the minimum spacing between adjacent children on main axis.
    Gap int
    // Padding reserves space inside the element [top, right, bottom, left].
    Padding [4]int
    // Margin reserves space outside the element [top, right, bottom, left].
    Margin [4]int
    // Align controls child alignment on the cross axis.
    Align Alignment
    // Justify controls space distribution along the main axis.
    Justify Justify
    // Width sizing for this element (legacy; prefer Element.Layout.WidthSizing).
    // Zero value is Fixed(0); use NewProps() for sensible defaults.
    Width Sizing
    // Height sizing for this element (legacy; prefer Element.Layout.HeightSizing).
    // Zero value is Fixed(0); use NewProps() for sensible defaults.
    Height Sizing
    // Values is a map for custom hook properties (application state).
    // Accessible via Props.Get(key) or Values[key] directly.
    Values map[string]any
}
```

<a name="NewProps"></a>
### func NewProps

```go
func NewProps() Props
```

NewProps creates a Props with sensible defaults.

Defaults:

- Width: Fit\(\) \(shrink\-wrap to children\)
- Height: Fit\(\) \(shrink\-wrap to children\)
- Values: empty map
- Direction, Gap, Padding, Align, Justify: zero values

Use NewProps\(\) instead of Props\{\} to avoid accidental Fixed\(0\) sizing.

Example:

```
props := NewProps()
props.Width = Grow(1)   // Fill parent width
props.Values["state"] = myState
```

<a name="Props.Get"></a>
### func \(Props\) Get

```go
func (p Props) Get(key string) any
```

Get returns the value associated with key in Props.Values, or nil if not found.

<a name="Rect"></a>
## type Rect

Rect represents a rectangular region with absolute positioning. Used to define bounds, dimensions, and layout output.

```go
type Rect struct {
    X      int // Left edge coordinate
    Y      int // Top edge coordinate
    Width  int // Horizontal extent
    Height int // Vertical extent
}
```

<a name="ComputeLayout"></a>
### func ComputeLayout

```go
func ComputeLayout(root *LayoutNode, available Rect) []Rect
```

ComputeLayout computes the layout of the tree and returns concrete Rects for each node in depth\-first, pre\-order \(parent before children\).

The root node is constrained to the provided available Rect; child layout is derived from parent sizing and positioning rules.

If any node in the tree has a reflow callback, ComputeLayout performs a second measure\+layout pass: the first pass calls reflow once cross\-axis sizes are known; the second measure propagates updated dimensions to ancestors \(e.g. containers around wrapped text grow to fit new heights\).

Returns a flat slice of Rects in depth\-first, pre\-order. Each Rect corresponds to a node; indices align with a depth\-first traversal of the tree.

Nodes with Fit sizing that contain no children may have 0 dimensions if clamped to a parent constraint.

<a name="Ref"></a>
## type Ref

Ref is a mutable container that does NOT trigger a re\-render when its value changes. Use for values that the render needs to read but that should not cause a redraw when updated \(timer IDs, previous values, imperative handles\).

Example:

```
prevCount := retui.UseRef(0)
prevCount.Set(count) // does NOT trigger redraw
delta := count - prevCount.Get()
```

```go
type Ref[T any] struct {
    // contains filtered or unexported fields
}
```

<a name="UseRef"></a>
### func UseRef

```go
func UseRef[T any](initial T) *Ref[T]
```

UseRef returns a Ref stored at the caller's positional slot. The initial value is set only on the first render.

Example:

```
timerID := retui.UseRef[int](0)
```

<a name="Ref[T].Get"></a>
### func \(\*Ref\[T\]\) Get

```go
func (r *Ref[T]) Get() T
```

Get returns the current ref value. Safe from any goroutine.

<a name="Ref[T].Set"></a>
### func \(\*Ref\[T\]\) Set

```go
func (r *Ref[T]) Set(v T)
```

Set updates the ref value without triggering a re\-render. Safe from any goroutine.

<a name="Screen"></a>
## type Screen

Screen is a double\-buffered terminal canvas.

It maintains two cell grids \(current frame and previous frame\) and tracks which rows changed so Flush emits minimal ANSI output — only rows with at least one changed cell are repainted.

Screen is NOT safe for concurrent use from multiple goroutines. The input event loop and render loop must run on the same goroutine, or the caller must synchronise externally.

```go
type Screen struct {
    // contains filtered or unexported fields
}
```

<a name="NewScreen"></a>
### func NewScreen

```go
func NewScreen(width, height int, out io.Writer) *Screen
```

NewScreen creates a Screen of the given dimensions writing to out.

Use NewScreenStdout for the common case of writing to os.Stdout.

Example:

```
s := retui.NewScreen(80, 24, os.Stdout)
```

<a name="NewScreenStdout"></a>
### func NewScreenStdout

```go
func NewScreenStdout() *Screen
```

NewScreenStdout creates a Screen sized to the current terminal dimensions. Falls back to 80×24 if the terminal size cannot be determined.

Example:

```
s := retui.NewScreenStdout()
s.Start()
defer s.Stop()
```

<a name="StdOutScreen"></a>
### func StdOutScreen

```go
func StdOutScreen() *Screen
```

StdOutScreen returns the package\-level singleton Screen for os.Stdout. Sized from the live terminal at first call, not at package init.

Prefer NewScreenStdout\(\) when you need explicit control over lifecycle.

<a name="Screen.ApplyOverlay"></a>
### func \(\*Screen\) ApplyOverlay

```go
func (s *Screen) ApplyOverlay(region OverlayRegion, x, y int)
```

ApplyOverlay stamps an OverlayRegion onto the screen buffer at \(x, y\).

Only cells within the screen bounds are written. Out\-of\-bounds pixels are clipped silently. The overlay is written OVER existing content, so it must be called after the main layout has been painted.

Example:

```
screen.Clear()
renderMainLayout(screen)    // paint background first
screen.ApplyOverlay(windowRegion, 10, 3) // then overlay the window on top
screen.Flush()
```

<a name="Screen.Clear"></a>
### func \(\*Screen\) Clear

```go
func (s *Screen) Clear()
```

Clear resets every cell to a blank space. Only cells that differ from blank are marked dirty, so calling Clear on an already\-blank frame is effectively free.

Call this at the start of every render frame before painting new content.

<a name="Screen.EnsureRoom"></a>
### func \(\*Screen\) EnsureRoom

```go
func (s *Screen) EnsureRoom(contentH int)
```

EnsureRoom guarantees contentH rows fit within the physical viewport. When content would overflow, it writes rows inline so the terminal scrolls older content into scrollback, then adjusts anchorRow accordingly.

Call before Flush when content height may exceed terminal height.

<a name="Screen.Flush"></a>
### func \(\*Screen\) Flush

```go
func (s *Screen) Flush()
```

Flush emits only the rows that changed since the last Flush.

For each dirty row, it scans cells and emits ANSI sequences only for cells whose current value differs from the previously flushed value. Style runs are batched — one ANSI prefix per style change, not per cell. All output is collected in a 16 KB bufio.Writer and written in a single syscall at the end.

Calling Flush when nothing is dirty is a no\-op.

<a name="Screen.ForceMarkAllDirty"></a>
### func \(\*Screen\) ForceMarkAllDirty

```go
func (s *Screen) ForceMarkAllDirty()
```

ForceMarkAllDirty forces a full repaint on the next Flush. Use after resize or when recovering from a corrupted terminal state.

<a name="Screen.GetCell"></a>
### func \(\*Screen\) GetCell

```go
func (s *Screen) GetCell(x, y int) Cell
```

GetCell returns the current cell value at \(x, y\).

Example:

```
cell := screen.GetCell(10, 5)
```

<a name="Screen.HandleResize"></a>
### func \(\*Screen\) HandleResize

```go
func (s *Screen) HandleResize()
```

HandleResize responds to SIGWINCH \(terminal resize\). It queries the new terminal size, reallocates grids, and forces a full redraw.

Example \(in your event loop\):

```
case sig := <-sigwinchCh:
    _ = sig
    screen.HandleResize()
```

<a name="Screen.Height"></a>
### func \(\*Screen\) Height

```go
func (s *Screen) Height() int
```

Height returns the screen height in rows.

<a name="Screen.PaintOverlay"></a>
### func \(\*Screen\) PaintOverlay

```go
func (s *Screen) PaintOverlay(width, height int, paint func(s *Screen)) OverlayRegion
```

PaintOverlay renders a closure into an off\-screen OverlayRegion of the given dimensions. The closure receives a mini\-Screen sized to \(width, height\) and should paint into it exactly as it would a normal screen.

Use this to pre\-render a window's content into a bounded buffer.

Example:

```
region := screen.PaintOverlay(60, 20, func(s *retui.Screen) {
    // paint window chrome and content into s
    s.SetCell(0, 0, '┌', borderStyle)
    // ...
})
screen.ApplyOverlay(region, 10, 3)
```

<a name="Screen.Resize"></a>
### func \(\*Screen\) Resize

```go
func (s *Screen) Resize(width, height int)
```

Resize changes the screen dimensions. Existing content is discarded and all cells reset to blank.

Example:

```
screen.Resize(120, 40)
```

<a name="Screen.SetCell"></a>
### func \(\*Screen\) SetCell

```go
func (s *Screen) SetCell(x, y int, value rune, style Style)
```

SetCell writes a rune and style into cell \(x, y\).

Out\-of\-bounds coordinates are silently ignored. Cells that did not change \(same rune and style as current frame\) are skipped without marking dirty — this keeps the dirty region tight even when the renderer calls SetCell for every cell unconditionally.

Parameters:

- x: column \(0\-based\)
- y: row \(0\-based\)
- value: rune to render \(0 = space\)
- style: visual attributes

<a name="Screen.Start"></a>
### func \(\*Screen\) Start

```go
func (s *Screen) Start()
```

Start prepares the terminal for full\-screen rendering.

It clears the screen, enters raw mode, queries terminal dimensions, hides the cursor, and enables bracketed paste.

Always pair with defer s.Stop\(\).

Example:

```
s.Start()
defer s.Stop()
```

<a name="Screen.Stop"></a>
### func \(\*Screen\) Stop

```go
func (s *Screen) Stop()
```

Stop restores the terminal to its original state.

Safe to call if Start was never called or failed.

Example:

```
defer s.Stop()
```

<a name="Screen.Width"></a>
### func \(\*Screen\) Width

```go
func (s *Screen) Width() int
```

Width returns the screen width in columns.

<a name="ScreenParams"></a>
## type ScreenParams

ScreenParams contains navigation parameters passed to a screen.

Unlike component Props, ScreenParams belong to the navigation system and are associated with a screen while it is active.

Example:

```
retui.PushScreen("ledger_list", retui.ScreenParams{
    "groupId": 10,
})
```

```go
type ScreenParams map[string]any
```

<a name="CurrentScreenParams"></a>
### func CurrentScreenParams

```go
func CurrentScreenParams() ScreenParams
```

CurrentScreenParams returns the parameters associated with the currently active screen.

<a name="ScreenRoute"></a>
## type ScreenRoute

ScreenRoute represents one entry in the navigation stack.

Each entry stores both the screen identifier and any navigation parameters supplied when the screen was pushed.

```go
type ScreenRoute struct {
    ID     string
    Params ScreenParams
}
```

<a name="ScreenStack"></a>
## type ScreenStack

ScreenStack manages the navigation history for a RetUI application.

ScreenStack is safe for concurrent use.

```go
type ScreenStack struct {
    // contains filtered or unexported fields
}
```

<a name="NewScreenStack"></a>
### func NewScreenStack

```go
func NewScreenStack(rootScreen string) *ScreenStack
```

NewScreenStack creates a new navigation stack.

The supplied screen becomes the root screen.

Example:

```
nav := retui.NewScreenStack("home")
```

<a name="ScreenStack.CanPop"></a>
### func \(\*ScreenStack\) CanPop

```go
func (n *ScreenStack) CanPop() bool
```

CanPop reports whether the current screen can navigate back.

<a name="ScreenStack.Clear"></a>
### func \(\*ScreenStack\) Clear

```go
func (n *ScreenStack) Clear()
```

Clear removes every screen from the navigation stack.

Normally Reset should be preferred.

<a name="ScreenStack.Current"></a>
### func \(\*ScreenStack\) Current

```go
func (n *ScreenStack) Current() string
```

Current returns the current screen ID.

<a name="ScreenStack.CurrentParams"></a>
### func \(\*ScreenStack\) CurrentParams

```go
func (n *ScreenStack) CurrentParams() ScreenParams
```

CurrentParams returns the navigation parameters for the current screen.

Returns nil if no parameters were supplied.

<a name="ScreenStack.IsEmpty"></a>
### func \(\*ScreenStack\) IsEmpty

```go
func (n *ScreenStack) IsEmpty() bool
```

IsEmpty reports whether the navigation stack is empty.

<a name="ScreenStack.PopScreen"></a>
### func \(\*ScreenStack\) PopScreen

```go
func (n *ScreenStack) PopScreen() string
```

PopScreen removes the top screen.

The root screen cannot be removed.

<a name="ScreenStack.PushScreen"></a>
### func \(\*ScreenStack\) PushScreen

```go
func (n *ScreenStack) PushScreen(screenID string, params ScreenParams)
```

PushScreen pushes a new screen onto the navigation stack.

Params may be nil.

Example:

```
nav.PushScreen("ledger_list", retui.ScreenParams{
    "groupId": 10,
})
```

<a name="ScreenStack.ReplaceScreen"></a>
### func \(\*ScreenStack\) ReplaceScreen

```go
func (n *ScreenStack) ReplaceScreen(screenID string, params ScreenParams)
```

ReplaceScreen replaces the current screen.

Navigation history is preserved except for the top entry.

<a name="ScreenStack.Reset"></a>
### func \(\*ScreenStack\) Reset

```go
func (n *ScreenStack) Reset(rootScreenID string)
```

Reset clears the stack and creates a new root screen.

<a name="ScreenStack.Size"></a>
### func \(\*ScreenStack\) Size

```go
func (n *ScreenStack) Size() int
```

Size returns the number of screens currently on the stack.

<a name="ScreenStack.Stack"></a>
### func \(\*ScreenStack\) Stack

```go
func (n *ScreenStack) Stack() []string
```

Stack returns the navigation history.

A copy is returned so callers cannot modify the internal stack.

<a name="Sizing"></a>
## type Sizing

Sizing describes how a dimension \(width or height\) is computed.

Mode: The sizing strategy \(Fixed, Grow, Fit, Percent\). Value: The mode\-specific parameter:

- SizingFixed: pixel count
- SizingGrow: weight \(default 1 if 0; higher = larger share of remaining space\)
- SizingFit: unused \(always 0\)
- SizingPercent: percentage \(0\-100 of parent's resolved size\)

Examples:

```
Fixed(100)       → exactly 100px
Grow(2)          → 2x weight in remaining space (vs weight 1)
Fit()            → shrink-wrap children
Percent(50)      → 50% of parent's resolved size on this axis
```

```go
type Sizing struct {
    Mode  SizingMode
    Value int
}
```

<a name="Fit"></a>
### func Fit

```go
func Fit() Sizing
```

Fit returns a Sizing that shrink\-wraps to children's intrinsic size. Does not claim remaining space; children are measured normally and the dimension is the sum \(plus padding/gap\) of their contributions.

<a name="Fixed"></a>
### func Fixed

```go
func Fixed(n int) Sizing
```

Fixed returns a Sizing for a fixed pixel dimension.

<a name="Grow"></a>
### func Grow

```go
func Grow(n int) Sizing
```

Grow returns a Sizing that claims remaining space with the given weight. In a parent with leftover space after fixed/fit children are sized, each Grow child gets a share proportional to its weight. Weight 0 defaults to weight 1. Example: two children with Grow\(1\) and Grow\(2\) split remaining space 1:2.

<a name="Percent"></a>
### func Percent

```go
func Percent(n int) Sizing
```

Percent returns a Sizing that resolves to a percentage \(0\-100\) of the parent's resolved size along the same axis.

If the parent's size is indefinite during the measure pass \(e.g. a SizingFit parent trying to shrink\-wrap\), the percentage cannot be resolved and is treated as 0 for measure, mirroring SizingGrow. The real value is resolved later during ComputeLayout once the parent has a concrete Rect.

Value is clamped to \[0, 100\] in practice, though out\-of\-range values are not explicitly rejected.

<a name="SizingMode"></a>
## type SizingMode

SizingMode determines how a node's dimension is resolved. See Sizing for details on each mode.

```go
type SizingMode int
```

<a name="SizingFixed"></a>

```go
const (
    // SizingFixed: dimension is a fixed pixel value.
    SizingFixed SizingMode = iota
    // SizingGrow: dimension expands to claim remaining space proportionally.
    // Only valid with a concrete parent size.
    SizingGrow
    // SizingFit: dimension shrink-wraps to children's intrinsic size.
    // Does not participate in parent's remaining-space distribution.
    SizingFit
    // SizingPercent: dimension is a percentage (0-100) of parent's resolved size.
    // If parent size is indefinite (e.g. SizingFit parent), treated as 0 during
    // measure pass; resolved later during layout with actual parent Rect.
    SizingPercent
)
```

<a name="Style"></a>
## type Style



```go
type Style struct {
    // contains filtered or unexported fields
}
```

<a name="NewStyle"></a>
### func NewStyle

```go
func NewStyle() Style
```



<a name="Style.ANSIPrefix"></a>
### func \(Style\) ANSIPrefix

```go
func (s Style) ANSIPrefix() string
```



<a name="Style.Background"></a>
### func \(Style\) Background

```go
func (s Style) Background(color Color) Style
```



<a name="Style.Bold"></a>
### func \(Style\) Bold

```go
func (s Style) Bold(bold bool) Style
```



<a name="Style.Border"></a>
### func \(Style\) Border

```go
func (s Style) Border(b Border) Style
```



<a name="Style.Foreground"></a>
### func \(Style\) Foreground

```go
func (s Style) Foreground(color Color) Style
```



<a name="Style.IsBold"></a>
### func \(Style\) IsBold

```go
func (s Style) IsBold() bool
```



<a name="Style.Italic"></a>
### func \(Style\) Italic

```go
func (s Style) Italic(italic bool) Style
```



<a name="Style.Underline"></a>
### func \(Style\) Underline

```go
func (s Style) Underline(underline bool) Style
```



# components

```go
import "github.com/subhasundardass/retui/retui/components"
```

## Index

- [Constants](<#constants>)
- [func Badge\(label string, fg retui.Color, bg retui.Color\) retui.Element](<#Badge>)
- [func ClearToasts\(\)](<#ClearToasts>)
- [func ConfigureToasts\(opts ...ManagerOption\)](<#ConfigureToasts>)
- [func DismissToast\(id int64\)](<#DismissToast>)
- [func ExampleNumberUsage\(\) retui.Element](<#ExampleNumberUsage>)
- [func ProgressBar\(value float64, width int, color retui.Color\) retui.Element](<#ProgressBar>)
- [func ShowError\(msg string, opts ...ToastOption\) int64](<#ShowError>)
- [func ShowSuccess\(msg string, opts ...ToastOption\) int64](<#ShowSuccess>)
- [func ShowToast\(msg string, opts ...ToastOption\) int64](<#ShowToast>)
- [func ShowWarning\(msg string, opts ...ToastOption\) int64](<#ShowWarning>)
- [func Spinner\(label string\) retui.Element](<#Spinner>)
- [func ToastLayer\(mgr \*ToastManager\) \[\]retui.Element](<#ToastLayer>)
- [func Tree\(treeID string, nodes \[\]TreeNode, focused bool, onChange func\(id string\)\) retui.Element](<#Tree>)
- [type ButtonConfig](<#ButtonConfig>)
- [type ButtonField](<#ButtonField>)
  - [func Button\(\) \*ButtonField](<#Button>)
  - [func \(b \*ButtonField\) Active\(v bool\) \*ButtonField](<#ButtonField.Active>)
  - [func \(b \*ButtonField\) ActiveStyle\(s retui.Style\) \*ButtonField](<#ButtonField.ActiveStyle>)
  - [func \(b \*ButtonField\) Focused\(v bool\) \*ButtonField](<#ButtonField.Focused>)
  - [func \(b \*ButtonField\) HoverStyle\(s retui.Style\) \*ButtonField](<#ButtonField.HoverStyle>)
  - [func \(b \*ButtonField\) ID\(v string\) \*ButtonField](<#ButtonField.ID>)
  - [func \(b \*ButtonField\) Label\(v string\) \*ButtonField](<#ButtonField.Label>)
  - [func \(b \*ButtonField\) OnBlur\(fn func\(string\)\) \*ButtonField](<#ButtonField.OnBlur>)
  - [func \(b \*ButtonField\) OnClick\(fn func\(string\)\) \*ButtonField](<#ButtonField.OnClick>)
  - [func \(b \*ButtonField\) OnFocus\(fn func\(string\)\) \*ButtonField](<#ButtonField.OnFocus>)
  - [func \(b \*ButtonField\) OnKeyPress\(fn func\(string, retui.Key\) bool\) \*ButtonField](<#ButtonField.OnKeyPress>)
  - [func \(b \*ButtonField\) Prefix\(v string\) \*ButtonField](<#ButtonField.Prefix>)
  - [func \(b \*ButtonField\) Render\(\) retui.Element](<#ButtonField.Render>)
  - [func \(b \*ButtonField\) Style\(s retui.Style\) \*ButtonField](<#ButtonField.Style>)
  - [func \(b \*ButtonField\) Suffix\(v string\) \*ButtonField](<#ButtonField.Suffix>)
  - [func \(b \*ButtonField\) Width\(w int\) \*ButtonField](<#ButtonField.Width>)
- [type CheckboxConfig](<#CheckboxConfig>)
- [type CheckboxField](<#CheckboxField>)
  - [func Checkbox\(\) \*CheckboxField](<#Checkbox>)
  - [func \(c \*CheckboxField\) Checked\(v bool\) \*CheckboxField](<#CheckboxField.Checked>)
  - [func \(c \*CheckboxField\) CheckedStyle\(s retui.Style\) \*CheckboxField](<#CheckboxField.CheckedStyle>)
  - [func \(c \*CheckboxField\) Focused\(v bool\) \*CheckboxField](<#CheckboxField.Focused>)
  - [func \(c \*CheckboxField\) ID\(v string\) \*CheckboxField](<#CheckboxField.ID>)
  - [func \(c \*CheckboxField\) Label\(v string\) \*CheckboxField](<#CheckboxField.Label>)
  - [func \(c \*CheckboxField\) OnBlur\(fn func\(string\)\) \*CheckboxField](<#CheckboxField.OnBlur>)
  - [func \(c \*CheckboxField\) OnChange\(fn func\(string, bool\)\) \*CheckboxField](<#CheckboxField.OnChange>)
  - [func \(c \*CheckboxField\) OnFocus\(fn func\(string\)\) \*CheckboxField](<#CheckboxField.OnFocus>)
  - [func \(c \*CheckboxField\) OnKeyPress\(fn func\(string, retui.Key\) bool\) \*CheckboxField](<#CheckboxField.OnKeyPress>)
  - [func \(c \*CheckboxField\) Render\(\) retui.Element](<#CheckboxField.Render>)
  - [func \(c \*CheckboxField\) Style\(s retui.Style\) \*CheckboxField](<#CheckboxField.Style>)
  - [func \(c \*CheckboxField\) UncheckedStyle\(s retui.Style\) \*CheckboxField](<#CheckboxField.UncheckedStyle>)
  - [func \(c \*CheckboxField\) Width\(w int\) \*CheckboxField](<#CheckboxField.Width>)
- [type DateConfig](<#DateConfig>)
- [type DateInputField](<#DateInputField>)
  - [func DateInput\(\) \*DateInputField](<#DateInput>)
  - [func \(d \*DateInputField\) Focused\(v bool\) \*DateInputField](<#DateInputField.Focused>)
  - [func \(d \*DateInputField\) Format\(v string\) \*DateInputField](<#DateInputField.Format>)
  - [func \(d \*DateInputField\) ID\(v string\) \*DateInputField](<#DateInputField.ID>)
  - [func \(d \*DateInputField\) Max\(v string\) \*DateInputField](<#DateInputField.Max>)
  - [func \(d \*DateInputField\) Min\(v string\) \*DateInputField](<#DateInputField.Min>)
  - [func \(d \*DateInputField\) OnBlur\(fn func\(string\)\) \*DateInputField](<#DateInputField.OnBlur>)
  - [func \(d \*DateInputField\) OnChange\(fn func\(string, string\)\) \*DateInputField](<#DateInputField.OnChange>)
  - [func \(d \*DateInputField\) OnFocus\(fn func\(string\)\) \*DateInputField](<#DateInputField.OnFocus>)
  - [func \(d \*DateInputField\) OnKeyPress\(fn func\(string, retui.Key\) bool\) \*DateInputField](<#DateInputField.OnKeyPress>)
  - [func \(d \*DateInputField\) OnSubmit\(fn func\(string, string\)\) \*DateInputField](<#DateInputField.OnSubmit>)
  - [func \(d \*DateInputField\) Placeholder\(v string\) \*DateInputField](<#DateInputField.Placeholder>)
  - [func \(d \*DateInputField\) Prefix\(v string\) \*DateInputField](<#DateInputField.Prefix>)
  - [func \(d \*DateInputField\) Render\(\) retui.Element](<#DateInputField.Render>)
  - [func \(d \*DateInputField\) Style\(s retui.Style\) \*DateInputField](<#DateInputField.Style>)
  - [func \(d \*DateInputField\) Suffix\(v string\) \*DateInputField](<#DateInputField.Suffix>)
  - [func \(d \*DateInputField\) Value\(v string\) \*DateInputField](<#DateInputField.Value>)
  - [func \(d \*DateInputField\) Width\(w int\) \*DateInputField](<#DateInputField.Width>)
- [type InputConfig](<#InputConfig>)
- [type InputField](<#InputField>)
  - [func TextInput\(\) \*InputField](<#TextInput>)
  - [func \(i \*InputField\) Focused\(v bool\) \*InputField](<#InputField.Focused>)
  - [func \(i \*InputField\) ID\(v string\) \*InputField](<#InputField.ID>)
  - [func \(i \*InputField\) MaxLength\(v int\) \*InputField](<#InputField.MaxLength>)
  - [func \(i \*InputField\) MinLength\(v int\) \*InputField](<#InputField.MinLength>)
  - [func \(i \*InputField\) OnBlur\(fn func\(string\)\) \*InputField](<#InputField.OnBlur>)
  - [func \(i \*InputField\) OnChange\(fn func\(string, string\)\) \*InputField](<#InputField.OnChange>)
  - [func \(i \*InputField\) OnFocus\(fn func\(string\)\) \*InputField](<#InputField.OnFocus>)
  - [func \(i \*InputField\) OnKeyPress\(fn func\(string, retui.Key\) bool\) \*InputField](<#InputField.OnKeyPress>)
  - [func \(i \*InputField\) OnSubmit\(fn func\(string, string\)\) \*InputField](<#InputField.OnSubmit>)
  - [func \(i \*InputField\) Placeholder\(v string\) \*InputField](<#InputField.Placeholder>)
  - [func \(i \*InputField\) Prefix\(v string\) \*InputField](<#InputField.Prefix>)
  - [func \(i \*InputField\) Render\(\) retui.Element](<#InputField.Render>)
  - [func \(i \*InputField\) Style\(s retui.Style\) \*InputField](<#InputField.Style>)
  - [func \(i \*InputField\) Suffix\(v string\) \*InputField](<#InputField.Suffix>)
  - [func \(i \*InputField\) Value\(v string\) \*InputField](<#InputField.Value>)
  - [func \(i \*InputField\) Width\(w int\) \*InputField](<#InputField.Width>)
- [type ListConfig](<#ListConfig>)
- [type ListField](<#ListField>)
  - [func List\(\) \*ListField](<#List>)
  - [func \(l \*ListField\) Focused\(v bool\) \*ListField](<#ListField.Focused>)
  - [func \(l \*ListField\) ID\(v string\) \*ListField](<#ListField.ID>)
  - [func \(l \*ListField\) Items\(v \[\]string\) \*ListField](<#ListField.Items>)
  - [func \(l \*ListField\) OnBlur\(fn func\(string\)\) \*ListField](<#ListField.OnBlur>)
  - [func \(l \*ListField\) OnFocus\(fn func\(string\)\) \*ListField](<#ListField.OnFocus>)
  - [func \(l \*ListField\) OnKeyPress\(fn func\(string, retui.Key\) bool\) \*ListField](<#ListField.OnKeyPress>)
  - [func \(l \*ListField\) OnSelect\(fn func\(string, int, string\)\) \*ListField](<#ListField.OnSelect>)
  - [func \(l \*ListField\) OnSubmit\(fn func\(string, int, string\)\) \*ListField](<#ListField.OnSubmit>)
  - [func \(l \*ListField\) Prefix\(v string\) \*ListField](<#ListField.Prefix>)
  - [func \(l \*ListField\) Render\(\) retui.Element](<#ListField.Render>)
  - [func \(l \*ListField\) Selected\(v int\) \*ListField](<#ListField.Selected>)
  - [func \(l \*ListField\) SelectedStyle\(s retui.Style\) \*ListField](<#ListField.SelectedStyle>)
  - [func \(l \*ListField\) Style\(s retui.Style\) \*ListField](<#ListField.Style>)
  - [func \(l \*ListField\) Suffix\(v string\) \*ListField](<#ListField.Suffix>)
  - [func \(l \*ListField\) UnselectedStyle\(s retui.Style\) \*ListField](<#ListField.UnselectedStyle>)
  - [func \(l \*ListField\) Width\(w int\) \*ListField](<#ListField.Width>)
- [type ManagerOption](<#ManagerOption>)
  - [func WithDefaultDuration\(d time.Duration\) ManagerOption](<#WithDefaultDuration>)
  - [func WithDefaultPosition\(p ToastPosition\) ManagerOption](<#WithDefaultPosition>)
- [type NumberInputConfig](<#NumberInputConfig>)
- [type NumberInputField](<#NumberInputField>)
  - [func NumberInput\(\) \*NumberInputField](<#NumberInput>)
  - [func \(n \*NumberInputField\) ArrowStep\(v bool\) \*NumberInputField](<#NumberInputField.ArrowStep>)
  - [func \(n \*NumberInputField\) Decimals\(v int\) \*NumberInputField](<#NumberInputField.Decimals>)
  - [func \(n \*NumberInputField\) Empty\(v bool\) \*NumberInputField](<#NumberInputField.Empty>)
  - [func \(n \*NumberInputField\) Focused\(v bool\) \*NumberInputField](<#NumberInputField.Focused>)
  - [func \(n \*NumberInputField\) ID\(v string\) \*NumberInputField](<#NumberInputField.ID>)
  - [func \(n \*NumberInputField\) Max\(v float64\) \*NumberInputField](<#NumberInputField.Max>)
  - [func \(n \*NumberInputField\) Min\(v float64\) \*NumberInputField](<#NumberInputField.Min>)
  - [func \(n \*NumberInputField\) OnBlur\(fn func\(string\)\) \*NumberInputField](<#NumberInputField.OnBlur>)
  - [func \(n \*NumberInputField\) OnChange\(fn func\(string, float64\)\) \*NumberInputField](<#NumberInputField.OnChange>)
  - [func \(n \*NumberInputField\) OnFocus\(fn func\(string\)\) \*NumberInputField](<#NumberInputField.OnFocus>)
  - [func \(n \*NumberInputField\) OnKeyPress\(fn func\(string, retui.Key\) bool\) \*NumberInputField](<#NumberInputField.OnKeyPress>)
  - [func \(n \*NumberInputField\) OnSubmit\(fn func\(string, float64\)\) \*NumberInputField](<#NumberInputField.OnSubmit>)
  - [func \(n \*NumberInputField\) Placeholder\(v string\) \*NumberInputField](<#NumberInputField.Placeholder>)
  - [func \(n \*NumberInputField\) Prefix\(v string\) \*NumberInputField](<#NumberInputField.Prefix>)
  - [func \(n \*NumberInputField\) Render\(\) retui.Element](<#NumberInputField.Render>)
  - [func \(n \*NumberInputField\) SelectAllOnFocus\(v bool\) \*NumberInputField](<#NumberInputField.SelectAllOnFocus>)
  - [func \(n \*NumberInputField\) Step\(v float64\) \*NumberInputField](<#NumberInputField.Step>)
  - [func \(n \*NumberInputField\) Style\(s retui.Style\) \*NumberInputField](<#NumberInputField.Style>)
  - [func \(n \*NumberInputField\) Suffix\(v string\) \*NumberInputField](<#NumberInputField.Suffix>)
  - [func \(n \*NumberInputField\) Value\(v float64\) \*NumberInputField](<#NumberInputField.Value>)
  - [func \(n \*NumberInputField\) Width\(w int\) \*NumberInputField](<#NumberInputField.Width>)
- [type PasswordConfig](<#PasswordConfig>)
- [type PasswordFeild](<#PasswordFeild>)
  - [func Password\(\) \*PasswordFeild](<#Password>)
  - [func \(p \*PasswordFeild\) Focused\(v bool\) \*PasswordFeild](<#PasswordFeild.Focused>)
  - [func \(p \*PasswordFeild\) ID\(v string\) \*PasswordFeild](<#PasswordFeild.ID>)
  - [func \(p \*PasswordFeild\) MaskChar\(v string\) \*PasswordFeild](<#PasswordFeild.MaskChar>)
  - [func \(p \*PasswordFeild\) MaxLength\(v int\) \*PasswordFeild](<#PasswordFeild.MaxLength>)
  - [func \(p \*PasswordFeild\) MinLength\(v int\) \*PasswordFeild](<#PasswordFeild.MinLength>)
  - [func \(p \*PasswordFeild\) OnBlur\(fn func\(string\)\) \*PasswordFeild](<#PasswordFeild.OnBlur>)
  - [func \(p \*PasswordFeild\) OnChange\(fn func\(string, string\)\) \*PasswordFeild](<#PasswordFeild.OnChange>)
  - [func \(p \*PasswordFeild\) OnFocus\(fn func\(string\)\) \*PasswordFeild](<#PasswordFeild.OnFocus>)
  - [func \(p \*PasswordFeild\) OnKeyPress\(fn func\(string, retui.Key\) bool\) \*PasswordFeild](<#PasswordFeild.OnKeyPress>)
  - [func \(p \*PasswordFeild\) OnSubmit\(fn func\(string, string\)\) \*PasswordFeild](<#PasswordFeild.OnSubmit>)
  - [func \(p \*PasswordFeild\) Placeholder\(v string\) \*PasswordFeild](<#PasswordFeild.Placeholder>)
  - [func \(p \*PasswordFeild\) Prefix\(v string\) \*PasswordFeild](<#PasswordFeild.Prefix>)
  - [func \(p \*PasswordFeild\) Render\(\) retui.Element](<#PasswordFeild.Render>)
  - [func \(p \*PasswordFeild\) ShowLastChar\(v bool\) \*PasswordFeild](<#PasswordFeild.ShowLastChar>)
  - [func \(p \*PasswordFeild\) Style\(s retui.Style\) \*PasswordFeild](<#PasswordFeild.Style>)
  - [func \(p \*PasswordFeild\) Suffix\(v string\) \*PasswordFeild](<#PasswordFeild.Suffix>)
  - [func \(p \*PasswordFeild\) Value\(v string\) \*PasswordFeild](<#PasswordFeild.Value>)
  - [func \(p \*PasswordFeild\) Width\(w int\) \*PasswordFeild](<#PasswordFeild.Width>)
- [type SelectConfig](<#SelectConfig>)
- [type SelectField](<#SelectField>)
  - [func SelectDropdown\(\) \*SelectField](<#SelectDropdown>)
  - [func \(s \*SelectField\) Disabled\(v bool\) \*SelectField](<#SelectField.Disabled>)
  - [func \(s \*SelectField\) Focused\(v bool\) \*SelectField](<#SelectField.Focused>)
  - [func \(s \*SelectField\) Height\(h int\) \*SelectField](<#SelectField.Height>)
  - [func \(s \*SelectField\) ID\(v string\) \*SelectField](<#SelectField.ID>)
  - [func \(s \*SelectField\) Label\(v string\) \*SelectField](<#SelectField.Label>)
  - [func \(s \*SelectField\) OnBlur\(fn func\(string\)\) \*SelectField](<#SelectField.OnBlur>)
  - [func \(s \*SelectField\) OnChange\(fn func\(string, string\)\) \*SelectField](<#SelectField.OnChange>)
  - [func \(s \*SelectField\) OnFilter\(fn func\(string, string\) \[\]SelectOption\) \*SelectField](<#SelectField.OnFilter>)
  - [func \(s \*SelectField\) OnFocus\(fn func\(string\)\) \*SelectField](<#SelectField.OnFocus>)
  - [func \(s \*SelectField\) OnKeyPress\(fn func\(string, retui.Key\) bool\) \*SelectField](<#SelectField.OnKeyPress>)
  - [func \(s \*SelectField\) OnOpenChange\(fn func\(string, bool\)\) \*SelectField](<#SelectField.OnOpenChange>)
  - [func \(s \*SelectField\) Options\(v \[\]SelectOption\) \*SelectField](<#SelectField.Options>)
  - [func \(s \*SelectField\) OverlayAbsPos\(x, y int\) \*SelectField](<#SelectField.OverlayAbsPos>)
  - [func \(s \*SelectField\) Placeholder\(v string\) \*SelectField](<#SelectField.Placeholder>)
  - [func \(s \*SelectField\) Render\(\) retui.Element](<#SelectField.Render>)
  - [func \(s \*SelectField\) Style\(st retui.Style\) \*SelectField](<#SelectField.Style>)
  - [func \(s \*SelectField\) Value\(v string\) \*SelectField](<#SelectField.Value>)
  - [func \(s \*SelectField\) Width\(w int\) \*SelectField](<#SelectField.Width>)
- [type SelectOption](<#SelectOption>)
- [type SelectPickerConfig](<#SelectPickerConfig>)
- [type SelectPickerField](<#SelectPickerField>)
  - [func SelectPicker\(\) \*SelectPickerField](<#SelectPicker>)
  - [func \(s \*SelectPickerField\) Focused\(v bool\) \*SelectPickerField](<#SelectPickerField.Focused>)
  - [func \(s \*SelectPickerField\) ID\(v string\) \*SelectPickerField](<#SelectPickerField.ID>)
  - [func \(s \*SelectPickerField\) OnBlur\(fn func\(string\)\) \*SelectPickerField](<#SelectPickerField.OnBlur>)
  - [func \(s \*SelectPickerField\) OnChange\(fn func\(string, int, string\)\) \*SelectPickerField](<#SelectPickerField.OnChange>)
  - [func \(s \*SelectPickerField\) OnFocus\(fn func\(string\)\) \*SelectPickerField](<#SelectPickerField.OnFocus>)
  - [func \(s \*SelectPickerField\) OnKeyPress\(fn func\(string, retui.Key\) bool\) \*SelectPickerField](<#SelectPickerField.OnKeyPress>)
  - [func \(s \*SelectPickerField\) OnSubmit\(fn func\(string, int, string\)\) \*SelectPickerField](<#SelectPickerField.OnSubmit>)
  - [func \(s \*SelectPickerField\) Options\(v \[\]string\) \*SelectPickerField](<#SelectPickerField.Options>)
  - [func \(s \*SelectPickerField\) Prefix\(v string\) \*SelectPickerField](<#SelectPickerField.Prefix>)
  - [func \(s \*SelectPickerField\) Render\(\) retui.Element](<#SelectPickerField.Render>)
  - [func \(s \*SelectPickerField\) Selected\(v int\) \*SelectPickerField](<#SelectPickerField.Selected>)
  - [func \(s \*SelectPickerField\) Style\(style retui.Style\) \*SelectPickerField](<#SelectPickerField.Style>)
  - [func \(s \*SelectPickerField\) Suffix\(v string\) \*SelectPickerField](<#SelectPickerField.Suffix>)
  - [func \(s \*SelectPickerField\) Width\(w int\) \*SelectPickerField](<#SelectPickerField.Width>)
- [type TableField](<#TableField>)
  - [func Table\(\) \*TableField](<#Table>)
  - [func \(t \*TableField\) Alignments\(alignments \[\]string\) \*TableField](<#TableField.Alignments>)
  - [func \(t \*TableField\) BorderColor\(color retui.Color\) \*TableField](<#TableField.BorderColor>)
  - [func \(t \*TableField\) BorderStyle\(style retui.Style\) \*TableField](<#TableField.BorderStyle>)
  - [func \(t \*TableField\) CellPadding\(padding int\) \*TableField](<#TableField.CellPadding>)
  - [func \(t \*TableField\) ColumnWidths\(widths \[\]int\) \*TableField](<#TableField.ColumnWidths>)
  - [func \(t \*TableField\) Focused\(focused bool\) \*TableField](<#TableField.Focused>)
  - [func \(t \*TableField\) HeaderBackground\(color retui.Color\) \*TableField](<#TableField.HeaderBackground>)
  - [func \(t \*TableField\) HeaderColor\(color retui.Color\) \*TableField](<#TableField.HeaderColor>)
  - [func \(t \*TableField\) HeaderStyle\(style retui.Style\) \*TableField](<#TableField.HeaderStyle>)
  - [func \(t \*TableField\) Headers\(headers \[\]string\) \*TableField](<#TableField.Headers>)
  - [func \(t \*TableField\) Height\(height int\) \*TableField](<#TableField.Height>)
  - [func \(i \*TableField\) ID\(v string\) \*TableField](<#TableField.ID>)
  - [func \(t \*TableField\) MaxColumnWidth\(width int\) \*TableField](<#TableField.MaxColumnWidth>)
  - [func \(t \*TableField\) MinColumnWidth\(width int\) \*TableField](<#TableField.MinColumnWidth>)
  - [func \(t \*TableField\) OnChange\(fn func\(int\)\) \*TableField](<#TableField.OnChange>)
  - [func \(t \*TableField\) Render\(\) retui.Element](<#TableField.Render>)
  - [func \(t \*TableField\) RowBackground\(color retui.Color\) \*TableField](<#TableField.RowBackground>)
  - [func \(t \*TableField\) RowColor\(color retui.Color\) \*TableField](<#TableField.RowColor>)
  - [func \(t \*TableField\) RowStyle\(style retui.Style\) \*TableField](<#TableField.RowStyle>)
  - [func \(t \*TableField\) Rows\(rows \[\]\[\]string\) \*TableField](<#TableField.Rows>)
  - [func \(t \*TableField\) Selectable\(selectable bool\) \*TableField](<#TableField.Selectable>)
  - [func \(t \*TableField\) SelectedBackground\(color retui.Color\) \*TableField](<#TableField.SelectedBackground>)
  - [func \(t \*TableField\) SelectedForeground\(color retui.Color\) \*TableField](<#TableField.SelectedForeground>)
  - [func \(t \*TableField\) SelectedIndex\(index int\) \*TableField](<#TableField.SelectedIndex>)
  - [func \(t \*TableField\) SelectedStyle\(style retui.Style\) \*TableField](<#TableField.SelectedStyle>)
  - [func \(t \*TableField\) ShowBorders\(show bool\) \*TableField](<#TableField.ShowBorders>)
  - [func \(t \*TableField\) ShowHeaders\(show bool\) \*TableField](<#TableField.ShowHeaders>)
  - [func \(t \*TableField\) ShowSelection\(show bool\) \*TableField](<#TableField.ShowSelection>)
  - [func \(t \*TableField\) Width\(width int\) \*TableField](<#TableField.Width>)
- [type TextAreaConfig](<#TextAreaConfig>)
- [type TextAreaField](<#TextAreaField>)
  - [func TextArea\(\) \*TextAreaField](<#TextArea>)
  - [func \(t \*TextAreaField\) Focused\(v bool\) \*TextAreaField](<#TextAreaField.Focused>)
  - [func \(t \*TextAreaField\) Height\(h int\) \*TextAreaField](<#TextAreaField.Height>)
  - [func \(t \*TextAreaField\) ID\(v string\) \*TextAreaField](<#TextAreaField.ID>)
  - [func \(t \*TextAreaField\) MaxLength\(v int\) \*TextAreaField](<#TextAreaField.MaxLength>)
  - [func \(t \*TextAreaField\) MinLength\(v int\) \*TextAreaField](<#TextAreaField.MinLength>)
  - [func \(t \*TextAreaField\) OnBlur\(fn func\(string\)\) \*TextAreaField](<#TextAreaField.OnBlur>)
  - [func \(t \*TextAreaField\) OnChange\(fn func\(string, string\)\) \*TextAreaField](<#TextAreaField.OnChange>)
  - [func \(t \*TextAreaField\) OnFocus\(fn func\(string\)\) \*TextAreaField](<#TextAreaField.OnFocus>)
  - [func \(t \*TextAreaField\) OnKeyPress\(fn func\(string, retui.Key\) bool\) \*TextAreaField](<#TextAreaField.OnKeyPress>)
  - [func \(t \*TextAreaField\) OnSubmit\(fn func\(string, string\)\) \*TextAreaField](<#TextAreaField.OnSubmit>)
  - [func \(t \*TextAreaField\) Placeholder\(v string\) \*TextAreaField](<#TextAreaField.Placeholder>)
  - [func \(t \*TextAreaField\) Prefix\(v string\) \*TextAreaField](<#TextAreaField.Prefix>)
  - [func \(t \*TextAreaField\) Render\(\) retui.Element](<#TextAreaField.Render>)
  - [func \(t \*TextAreaField\) Style\(s retui.Style\) \*TextAreaField](<#TextAreaField.Style>)
  - [func \(t \*TextAreaField\) Suffix\(v string\) \*TextAreaField](<#TextAreaField.Suffix>)
  - [func \(t \*TextAreaField\) Value\(v string\) \*TextAreaField](<#TextAreaField.Value>)
  - [func \(t \*TextAreaField\) Width\(w int\) \*TextAreaField](<#TextAreaField.Width>)
- [type Toast](<#Toast>)
  - [func \(t \*Toast\) Height\(\) int](<#Toast.Height>)
  - [func \(t \*Toast\) Style\(\) retui.Style](<#Toast.Style>)
  - [func \(t \*Toast\) Title\(\) string](<#Toast.Title>)
  - [func \(t \*Toast\) Width\(\) int](<#Toast.Width>)
- [type ToastManager](<#ToastManager>)
  - [func NewToastManager\(\) \*ToastManager](<#NewToastManager>)
  - [func \(m \*ToastManager\) Clear\(\)](<#ToastManager.Clear>)
  - [func \(m \*ToastManager\) Configure\(opts ...ManagerOption\)](<#ToastManager.Configure>)
  - [func \(m \*ToastManager\) Dismiss\(id int64\)](<#ToastManager.Dismiss>)
  - [func \(m \*ToastManager\) HasToast\(\) bool](<#ToastManager.HasToast>)
  - [func \(m \*ToastManager\) OnChange\(fn func\(\)\)](<#ToastManager.OnChange>)
  - [func \(m \*ToastManager\) Show\(msg string, opts ...ToastOption\) int64](<#ToastManager.Show>)
  - [func \(m \*ToastManager\) Toasts\(\) \[\]\*Toast](<#ToastManager.Toasts>)
- [type ToastOption](<#ToastOption>)
  - [func WithDuration\(d time.Duration\) ToastOption](<#WithDuration>)
  - [func WithPosition\(p ToastPosition\) ToastOption](<#WithPosition>)
  - [func WithType\(t ToastType\) ToastOption](<#WithType>)
- [type ToastPosition](<#ToastPosition>)
- [type ToastType](<#ToastType>)
- [type TreeNode](<#TreeNode>)


## Constants

<a name="BORDER_COLOR"></a>

```go
const BORDER_COLOR = "#1d1d1d"
```

<a name="Badge"></a>
## func Badge

```go
func Badge(label string, fg retui.Color, bg retui.Color) retui.Element
```

Badge renders a short colored label with padding.

<details><summary>Example</summary>
<p>

Example usage test

```go
// Create a success badge
successBadge := Badge("Success", retui.Green, retui.Black)
_ = successBadge

// Create an error badge
errorBadge := Badge("Error", retui.Red, retui.White)
_ = errorBadge

// Create a warning badge
warningBadge := Badge("Warning", retui.Yellow, retui.Navy)
_ = warningBadge
```

</p>
</details>

<a name="ClearToasts"></a>
## func ClearToasts

```go
func ClearToasts()
```



<a name="ConfigureToasts"></a>
## func ConfigureToasts

```go
func ConfigureToasts(opts ...ManagerOption)
```

ConfigureToasts sets app\-wide defaults for the global toast manager. Call once at startup, before any ShowToast/ShowSuccess/etc. calls:

```
components.ConfigureToasts(
    components.WithDefaultPosition(components.ToastTopRight),
    components.WithDefaultDuration(4*time.Second),
)
```

<a name="DismissToast"></a>
## func DismissToast

```go
func DismissToast(id int64)
```



<a name="ExampleNumberUsage"></a>
## func ExampleNumberUsage

```go
func ExampleNumberUsage() retui.Element
```



<a name="ProgressBar"></a>
## func ProgressBar

```go
func ProgressBar(value float64, width int, color retui.Color) retui.Element
```

ProgressBar renders a filled/empty block bar representing value \(0\-1\) across width characters, including the two end caps.

<details><summary>Example</summary>
<p>

Example usage test

```go
// Create progress bars with different values
progress1 := ProgressBar(0.25, 20, retui.Green)
_ = progress1

progress2 := ProgressBar(0.50, 20, retui.Cyan)
_ = progress2

progress3 := ProgressBar(0.75, 20, retui.Yellow)
_ = progress3

progress4 := ProgressBar(1.00, 20, retui.Green)
_ = progress4

// Output:
```

#### Output

```

```

</p>
</details>

<a name="ShowError"></a>
## func ShowError

```go
func ShowError(msg string, opts ...ToastOption) int64
```



<a name="ShowSuccess"></a>
## func ShowSuccess

```go
func ShowSuccess(msg string, opts ...ToastOption) int64
```



<a name="ShowToast"></a>
## func ShowToast

```go
func ShowToast(msg string, opts ...ToastOption) int64
```



<a name="ShowWarning"></a>
## func ShowWarning

```go
func ShowWarning(msg string, opts ...ToastOption) int64
```



<a name="Spinner"></a>
## func Spinner

```go
func Spinner(label string) retui.Element
```

Spinner renders an animated braille spinner. Advances one frame per render.

<details><summary>Example</summary>
<p>

Example usage test

```go
// Create a spinner with a label
loadingSpinner := Spinner("Loading data...")
_ = loadingSpinner

// Create a spinner without a label
spinner := Spinner("")
_ = spinner

// Create a spinner with custom message
processingSpinner := Spinner("Processing your request...")
_ = processingSpinner
```

</p>
</details>

<a name="ToastLayer"></a>
## func ToastLayer

```go
func ToastLayer(mgr *ToastManager) []retui.Element
```

ToastLayer returns one Overlay element per visible toast, positioned using the current terminal dimensions tracked by the window package. Append its result to the children of your root layout element:

```
children := append([]retui.Element{final}, components.ToastLayer(nil)...)
```

<a name="Tree"></a>
## func Tree

```go
func Tree(treeID string, nodes []TreeNode, focused bool, onChange func(id string)) retui.Element
```



<details><summary>Example</summary>
<p>

Example usage test

```go
// Create a tree
nodes := []TreeNode{
	{
		Label: "Root",
		ID:    "root",
		Children: []TreeNode{
			{Label: "Child 1", ID: "child1"},
			{Label: "Child 2", ID: "child2", Children: []TreeNode{
				{Label: "Grandchild", ID: "grandchild"},
			}},
		},
	},
}

tree := Tree("example", nodes, false, func(id string) {
	// Handle selection
})
_ = tree
```

</p>
</details>

<a name="ButtonConfig"></a>
## type ButtonConfig



```go
type ButtonConfig struct {
    ID          string
    Label       string
    Width       int
    Style       retui.Style
    HoverStyle  retui.Style
    ActiveStyle retui.Style
    Prefix      string
    Suffix      string
    OnClick     func(id string)
    OnKeyPress  func(id string, key retui.Key) bool
    OnFocus     func(id string)
    OnBlur      func(id string)
}
```

<a name="ButtonField"></a>
## type ButtonField



```go
type ButtonField struct {
    // contains filtered or unexported fields
}
```

<a name="Button"></a>
### func Button

```go
func Button() *ButtonField
```



<a name="ButtonField.Active"></a>
### func \(\*ButtonField\) Active

```go
func (b *ButtonField) Active(v bool) *ButtonField
```



<a name="ButtonField.ActiveStyle"></a>
### func \(\*ButtonField\) ActiveStyle

```go
func (b *ButtonField) ActiveStyle(s retui.Style) *ButtonField
```



<a name="ButtonField.Focused"></a>
### func \(\*ButtonField\) Focused

```go
func (b *ButtonField) Focused(v bool) *ButtonField
```



<a name="ButtonField.HoverStyle"></a>
### func \(\*ButtonField\) HoverStyle

```go
func (b *ButtonField) HoverStyle(s retui.Style) *ButtonField
```



<a name="ButtonField.ID"></a>
### func \(\*ButtonField\) ID

```go
func (b *ButtonField) ID(v string) *ButtonField
```



<a name="ButtonField.Label"></a>
### func \(\*ButtonField\) Label

```go
func (b *ButtonField) Label(v string) *ButtonField
```



<a name="ButtonField.OnBlur"></a>
### func \(\*ButtonField\) OnBlur

```go
func (b *ButtonField) OnBlur(fn func(string)) *ButtonField
```



<a name="ButtonField.OnClick"></a>
### func \(\*ButtonField\) OnClick

```go
func (b *ButtonField) OnClick(fn func(string)) *ButtonField
```



<a name="ButtonField.OnFocus"></a>
### func \(\*ButtonField\) OnFocus

```go
func (b *ButtonField) OnFocus(fn func(string)) *ButtonField
```



<a name="ButtonField.OnKeyPress"></a>
### func \(\*ButtonField\) OnKeyPress

```go
func (b *ButtonField) OnKeyPress(fn func(string, retui.Key) bool) *ButtonField
```



<a name="ButtonField.Prefix"></a>
### func \(\*ButtonField\) Prefix

```go
func (b *ButtonField) Prefix(v string) *ButtonField
```



<a name="ButtonField.Render"></a>
### func \(\*ButtonField\) Render

```go
func (b *ButtonField) Render() retui.Element
```



<a name="ButtonField.Style"></a>
### func \(\*ButtonField\) Style

```go
func (b *ButtonField) Style(s retui.Style) *ButtonField
```



<a name="ButtonField.Suffix"></a>
### func \(\*ButtonField\) Suffix

```go
func (b *ButtonField) Suffix(v string) *ButtonField
```



<a name="ButtonField.Width"></a>
### func \(\*ButtonField\) Width

```go
func (b *ButtonField) Width(w int) *ButtonField
```



<a name="CheckboxConfig"></a>
## type CheckboxConfig



```go
type CheckboxConfig struct {
    ID             string
    Checked        bool
    Label          string
    Width          int
    Style          retui.Style
    CheckedStyle   retui.Style
    UncheckedStyle retui.Style
    OnChange       func(id string, checked bool)
    OnKeyPress     func(id string, key retui.Key) bool
    OnFocus        func(id string)
    OnBlur         func(id string)
}
```

<a name="CheckboxField"></a>
## type CheckboxField



```go
type CheckboxField struct {
    // contains filtered or unexported fields
}
```

<a name="Checkbox"></a>
### func Checkbox

```go
func Checkbox() *CheckboxField
```

─── Builder Methods ──────────────────────────────────────────────────────

```
func ExampleCheckboxUsage() retui.Element {
			agreeCheckbox := Checkbox().
				ID("agree").
	            Checked(c.state.IsActive).
				Label("I agree to the terms").
				OnChange(func(id string, checked bool) {
					println("Checkbox", id, "changed to:", checked)
				}).
             Render(),
```

<details><summary>Example</summary>
<p>

Example usage test

```go
// Create a checkbox
agreeCheckbox := Checkbox().
	ID("agree").
	Checked(false).
	Label("I agree to the terms").
	OnChange(func(id string, checked bool) {
		println("Checkbox", id, "changed to:", checked)
	}).
	Render()
_ = agreeCheckbox
```

</p>
</details>

<a name="CheckboxField.Checked"></a>
### func \(\*CheckboxField\) Checked

```go
func (c *CheckboxField) Checked(v bool) *CheckboxField
```



<a name="CheckboxField.CheckedStyle"></a>
### func \(\*CheckboxField\) CheckedStyle

```go
func (c *CheckboxField) CheckedStyle(s retui.Style) *CheckboxField
```



<a name="CheckboxField.Focused"></a>
### func \(\*CheckboxField\) Focused

```go
func (c *CheckboxField) Focused(v bool) *CheckboxField
```



<a name="CheckboxField.ID"></a>
### func \(\*CheckboxField\) ID

```go
func (c *CheckboxField) ID(v string) *CheckboxField
```



<a name="CheckboxField.Label"></a>
### func \(\*CheckboxField\) Label

```go
func (c *CheckboxField) Label(v string) *CheckboxField
```



<a name="CheckboxField.OnBlur"></a>
### func \(\*CheckboxField\) OnBlur

```go
func (c *CheckboxField) OnBlur(fn func(string)) *CheckboxField
```



<a name="CheckboxField.OnChange"></a>
### func \(\*CheckboxField\) OnChange

```go
func (c *CheckboxField) OnChange(fn func(string, bool)) *CheckboxField
```



<a name="CheckboxField.OnFocus"></a>
### func \(\*CheckboxField\) OnFocus

```go
func (c *CheckboxField) OnFocus(fn func(string)) *CheckboxField
```



<a name="CheckboxField.OnKeyPress"></a>
### func \(\*CheckboxField\) OnKeyPress

```go
func (c *CheckboxField) OnKeyPress(fn func(string, retui.Key) bool) *CheckboxField
```



<a name="CheckboxField.Render"></a>
### func \(\*CheckboxField\) Render

```go
func (c *CheckboxField) Render() retui.Element
```



<a name="CheckboxField.Style"></a>
### func \(\*CheckboxField\) Style

```go
func (c *CheckboxField) Style(s retui.Style) *CheckboxField
```



<a name="CheckboxField.UncheckedStyle"></a>
### func \(\*CheckboxField\) UncheckedStyle

```go
func (c *CheckboxField) UncheckedStyle(s retui.Style) *CheckboxField
```



<a name="CheckboxField.Width"></a>
### func \(\*CheckboxField\) Width

```go
func (c *CheckboxField) Width(w int) *CheckboxField
```



<a name="DateConfig"></a>
## type DateConfig

DateConfig holds the state and callbacks for a DateInputField.

Value, Min, and Max are all expected to be formatted according to Format \(e.g. "2024\-01\-15" for the default "YYYY\-MM\-DD"\). Min/Max are only enforced once the entered date is complete and calendrically valid; partial input is never rejected while typing.

```go
type DateConfig struct {
    ID          string
    Value       string
    Format      string
    Placeholder string
    Width       int
    Style       retui.Style
    Prefix      string
    Suffix      string
    Min         string // Minimum allowed date, formatted per Format ("" = no limit)
    Max         string // Maximum allowed date, formatted per Format ("" = no limit)
    OnChange    func(id string, value string)
    OnKeyPress  func(id string, key retui.Key) bool
    OnFocus     func(id string)
    OnBlur      func(id string)
    OnSubmit    func(id string, value string)
}
```

<a name="DateInputField"></a>
## type DateInputField

DateInputField is a builder for a masked date\-entry component.

```go
type DateInputField struct {
    // contains filtered or unexported fields
}
```

<a name="DateInput"></a>
### func DateInput

```go
func DateInput() *DateInputField
```

─── Builder Methods ──────────────────────────────────────────────────────

```
func ExampleDateUsage() retui.Element {
	// Date input with format and a range constraint.
	dateInput := DateInput().
		ID("birthday").
		Format("YYYY-MM-DD").
		Placeholder("Enter date").
		Width(25).
		Suffix(" ✓").
		Min("1900-01-01").
		Max("2026-12-31").
		OnChange(func(id string, value string) {
			fmt.Printf("Date changed to: %s\n", value)
		}).
		OnSubmit(func(id string, value string) {
			fmt.Printf("Date submitted: %s\n", value)
		})
```

<details><summary>Example</summary>
<p>

Example usage test

```go
// Create a date input with format and range constraint
dateInput := DateInput().
	ID("birthday").
	Format("YYYY-MM-DD").
	Placeholder("Enter date").
	Width(25).
	Suffix(" ✓").
	Min("1900-01-01").
	Max("2026-12-31").
	OnChange(func(id string, value string) {
		// Handle change
	}).
	OnSubmit(func(id string, value string) {
		// Handle submit
	}).
	Render()
_ = dateInput
```

</p>
</details>

<a name="DateInputField.Focused"></a>
### func \(\*DateInputField\) Focused

```go
func (d *DateInputField) Focused(v bool) *DateInputField
```



<a name="DateInputField.Format"></a>
### func \(\*DateInputField\) Format

```go
func (d *DateInputField) Format(v string) *DateInputField
```



<a name="DateInputField.ID"></a>
### func \(\*DateInputField\) ID

```go
func (d *DateInputField) ID(v string) *DateInputField
```



<a name="DateInputField.Max"></a>
### func \(\*DateInputField\) Max

```go
func (d *DateInputField) Max(v string) *DateInputField
```

Max sets the latest allowed date, formatted per Format.

<a name="DateInputField.Min"></a>
### func \(\*DateInputField\) Min

```go
func (d *DateInputField) Min(v string) *DateInputField
```

Min sets the earliest allowed date, formatted per Format.

<a name="DateInputField.OnBlur"></a>
### func \(\*DateInputField\) OnBlur

```go
func (d *DateInputField) OnBlur(fn func(string)) *DateInputField
```



<a name="DateInputField.OnChange"></a>
### func \(\*DateInputField\) OnChange

```go
func (d *DateInputField) OnChange(fn func(string, string)) *DateInputField
```



<a name="DateInputField.OnFocus"></a>
### func \(\*DateInputField\) OnFocus

```go
func (d *DateInputField) OnFocus(fn func(string)) *DateInputField
```



<a name="DateInputField.OnKeyPress"></a>
### func \(\*DateInputField\) OnKeyPress

```go
func (d *DateInputField) OnKeyPress(fn func(string, retui.Key) bool) *DateInputField
```



<a name="DateInputField.OnSubmit"></a>
### func \(\*DateInputField\) OnSubmit

```go
func (d *DateInputField) OnSubmit(fn func(string, string)) *DateInputField
```



<a name="DateInputField.Placeholder"></a>
### func \(\*DateInputField\) Placeholder

```go
func (d *DateInputField) Placeholder(v string) *DateInputField
```



<a name="DateInputField.Prefix"></a>
### func \(\*DateInputField\) Prefix

```go
func (d *DateInputField) Prefix(v string) *DateInputField
```



<a name="DateInputField.Render"></a>
### func \(\*DateInputField\) Render

```go
func (d *DateInputField) Render() retui.Element
```



<a name="DateInputField.Style"></a>
### func \(\*DateInputField\) Style

```go
func (d *DateInputField) Style(s retui.Style) *DateInputField
```



<a name="DateInputField.Suffix"></a>
### func \(\*DateInputField\) Suffix

```go
func (d *DateInputField) Suffix(v string) *DateInputField
```



<a name="DateInputField.Value"></a>
### func \(\*DateInputField\) Value

```go
func (d *DateInputField) Value(v string) *DateInputField
```



<a name="DateInputField.Width"></a>
### func \(\*DateInputField\) Width

```go
func (d *DateInputField) Width(w int) *DateInputField
```



<a name="InputConfig"></a>
## type InputConfig



```go
type InputConfig struct {
    ID          string
    Value       string
    Placeholder string
    Width       int
    Style       retui.Style
    Prefix      string
    Suffix      string
    MinLength   int // Minimum length (0 = no limit)
    MaxLength   int // Maximum length (0 = no limit)
    OnChange    func(id string, value string)
    OnKeyPress  func(id string, key retui.Key) bool
    OnFocus     func(id string)
    OnBlur      func(id string)
    OnSubmit    func(id string, value string)
}
```

<a name="InputField"></a>
## type InputField



```go
type InputField struct {
    // contains filtered or unexported fields
}
```

<a name="TextInput"></a>
### func TextInput

```go
func TextInput() *InputField
```



<details><summary>Example</summary>
<p>

Example usage test

```go
// Simple text input
textInput := TextInput().
	ID("name").
	Placeholder("Enter your name").
	Width(30).
	MinLength(2).
	MaxLength(50).
	Prefix("👤 ").
	Suffix(" ✏️").
	OnChange(func(id string, value string) {
		// Handle change
	}).
	OnSubmit(func(id string, value string) {
		// Handle submit
	}).
	Render()
_ = textInput
```

</p>
</details>

<a name="InputField.Focused"></a>
### func \(\*InputField\) Focused

```go
func (i *InputField) Focused(v bool) *InputField
```



<a name="InputField.ID"></a>
### func \(\*InputField\) ID

```go
func (i *InputField) ID(v string) *InputField
```



<a name="InputField.MaxLength"></a>
### func \(\*InputField\) MaxLength

```go
func (i *InputField) MaxLength(v int) *InputField
```



<a name="InputField.MinLength"></a>
### func \(\*InputField\) MinLength

```go
func (i *InputField) MinLength(v int) *InputField
```



<a name="InputField.OnBlur"></a>
### func \(\*InputField\) OnBlur

```go
func (i *InputField) OnBlur(fn func(string)) *InputField
```



<a name="InputField.OnChange"></a>
### func \(\*InputField\) OnChange

```go
func (i *InputField) OnChange(fn func(string, string)) *InputField
```



<a name="InputField.OnFocus"></a>
### func \(\*InputField\) OnFocus

```go
func (i *InputField) OnFocus(fn func(string)) *InputField
```



<a name="InputField.OnKeyPress"></a>
### func \(\*InputField\) OnKeyPress

```go
func (i *InputField) OnKeyPress(fn func(string, retui.Key) bool) *InputField
```



<a name="InputField.OnSubmit"></a>
### func \(\*InputField\) OnSubmit

```go
func (i *InputField) OnSubmit(fn func(string, string)) *InputField
```



<a name="InputField.Placeholder"></a>
### func \(\*InputField\) Placeholder

```go
func (i *InputField) Placeholder(v string) *InputField
```



<a name="InputField.Prefix"></a>
### func \(\*InputField\) Prefix

```go
func (i *InputField) Prefix(v string) *InputField
```



<a name="InputField.Render"></a>
### func \(\*InputField\) Render

```go
func (i *InputField) Render() retui.Element
```



<a name="InputField.Style"></a>
### func \(\*InputField\) Style

```go
func (i *InputField) Style(s retui.Style) *InputField
```



<a name="InputField.Suffix"></a>
### func \(\*InputField\) Suffix

```go
func (i *InputField) Suffix(v string) *InputField
```



<a name="InputField.Value"></a>
### func \(\*InputField\) Value

```go
func (i *InputField) Value(v string) *InputField
```



<a name="InputField.Width"></a>
### func \(\*InputField\) Width

```go
func (i *InputField) Width(w int) *InputField
```



<a name="ListConfig"></a>
## type ListConfig



```go
type ListConfig struct {
    ID              string
    Items           []string
    Selected        int
    Width           int
    Style           retui.Style
    SelectedStyle   retui.Style
    UnselectedStyle retui.Style
    Prefix          string
    Suffix          string
    OnSelect        func(id string, index int, value string)
    OnKeyPress      func(id string, key retui.Key) bool
    OnFocus         func(id string)
    OnBlur          func(id string)
    OnSubmit        func(id string, index int, value string)
}
```

<a name="ListField"></a>
## type ListField



```go
type ListField struct {
    // contains filtered or unexported fields
}
```

<a name="List"></a>
### func List

```go
func List() *ListField
```

─── Builder Methods ──────────────────────────────────────────────────────

```
func ExampleListUsage() retui.Element {
	// Simple list
	fruitList := List().
		ID("fruits").
		Items([]string{"Apple", "Banana", "Orange", "Grape", "Mango"}).
		Selected(0).
		Width(20).
		Suffix(" ✓").
		OnSelect(func(id string, index int, value string) {
			println("Selected:", value, "at index:", index)
		}).
		OnSubmit(func(id string, index int, value string) {
			println("Submitted:", value)
		})
```

<details><summary>Example</summary>
<p>

Example usage test

```go
// Create a list
fruitList := List().
	ID("fruits").
	Items([]string{"Apple", "Banana", "Orange", "Grape", "Mango"}).
	Selected(0).
	Width(20).
	Prefix("📋 ").
	Suffix(" ✓").
	OnSelect(func(id string, index int, value string) {
		println("Selected:", value, "at index:", index)
	}).
	OnSubmit(func(id string, index int, value string) {
		println("Submitted:", value)
	}).
	Render()
_ = fruitList
```

</p>
</details>

<a name="ListField.Focused"></a>
### func \(\*ListField\) Focused

```go
func (l *ListField) Focused(v bool) *ListField
```



<a name="ListField.ID"></a>
### func \(\*ListField\) ID

```go
func (l *ListField) ID(v string) *ListField
```



<a name="ListField.Items"></a>
### func \(\*ListField\) Items

```go
func (l *ListField) Items(v []string) *ListField
```



<a name="ListField.OnBlur"></a>
### func \(\*ListField\) OnBlur

```go
func (l *ListField) OnBlur(fn func(string)) *ListField
```



<a name="ListField.OnFocus"></a>
### func \(\*ListField\) OnFocus

```go
func (l *ListField) OnFocus(fn func(string)) *ListField
```



<a name="ListField.OnKeyPress"></a>
### func \(\*ListField\) OnKeyPress

```go
func (l *ListField) OnKeyPress(fn func(string, retui.Key) bool) *ListField
```



<a name="ListField.OnSelect"></a>
### func \(\*ListField\) OnSelect

```go
func (l *ListField) OnSelect(fn func(string, int, string)) *ListField
```



<a name="ListField.OnSubmit"></a>
### func \(\*ListField\) OnSubmit

```go
func (l *ListField) OnSubmit(fn func(string, int, string)) *ListField
```



<a name="ListField.Prefix"></a>
### func \(\*ListField\) Prefix

```go
func (l *ListField) Prefix(v string) *ListField
```



<a name="ListField.Render"></a>
### func \(\*ListField\) Render

```go
func (l *ListField) Render() retui.Element
```



<a name="ListField.Selected"></a>
### func \(\*ListField\) Selected

```go
func (l *ListField) Selected(v int) *ListField
```



<a name="ListField.SelectedStyle"></a>
### func \(\*ListField\) SelectedStyle

```go
func (l *ListField) SelectedStyle(s retui.Style) *ListField
```



<a name="ListField.Style"></a>
### func \(\*ListField\) Style

```go
func (l *ListField) Style(s retui.Style) *ListField
```



<a name="ListField.Suffix"></a>
### func \(\*ListField\) Suffix

```go
func (l *ListField) Suffix(v string) *ListField
```



<a name="ListField.UnselectedStyle"></a>
### func \(\*ListField\) UnselectedStyle

```go
func (l *ListField) UnselectedStyle(s retui.Style) *ListField
```



<a name="ListField.Width"></a>
### func \(\*ListField\) Width

```go
func (l *ListField) Width(w int) *ListField
```



<a name="ManagerOption"></a>
## type ManagerOption



```go
type ManagerOption func(*ToastManager)
```

<a name="WithDefaultDuration"></a>
### func WithDefaultDuration

```go
func WithDefaultDuration(d time.Duration) ManagerOption
```

WithDefaultDuration sets how long new toasts stay visible by default. 0 means toasts persist until Dismiss\(\) is called explicitly.

<a name="WithDefaultPosition"></a>
### func WithDefaultPosition

```go
func WithDefaultPosition(p ToastPosition) ManagerOption
```

WithDefaultPosition sets the corner new toasts appear in by default.

<a name="NumberInputConfig"></a>
## type NumberInputConfig

NumberInputConfig holds the state and callbacks for a NumberInputField.

Bounds: HasMin/HasMax gate whether Min/Max are enforced, so a legitimate bound of exactly 0 \(e.g. Min\(0\) for an age field, or Max\(0\) for a field that must be non\-positive\) is respected. Setting Min/Max via the builder methods always sets the corresponding HasMin/HasMax flag.

Emptiness: Empty distinguishes "no value entered" from "value is 0". It starts true and is cleared automatically the first time Value\(\) is called on the builder, or the first time the user commits a parseable number. When Empty is true, the placeholder is shown instead of "0".

Select\-all\-on\-focus: like a typical web \<input\>, gaining focus selects the entire current value. While selected, typing a character replaces the whole value instead of inserting at the cursor, and Backspace/ Delete clear it entirely. Left/Right/Home/End instead just collapse the selection to that boundary, matching standard browser behavior. Set SelectAllOnFocus to false to disable this and keep the cursor wherever it was, like a plain text field.

```go
type NumberInputConfig struct {
    ID          string
    Value       float64
    Empty       bool
    Placeholder string
    Width       int
    Style       retui.Style
    Prefix      string
    Suffix      string
    HasMin      bool
    Min         float64
    HasMax      bool
    Max         float64
    Step        float64 // Increment/decrement step (default: 1)
    Decimals    int     // Number of decimal places allowed (0 = integer only)
    ArrowStep   bool    // If false, Up/Down do not change the value (default: true).

    // SelectAllOnFocus enables web-style select-all behavior on focus.
    // Defaults to true.
    SelectAllOnFocus bool

    OnChange   func(id string, value float64)
    OnKeyPress func(id string, key retui.Key) bool
    OnFocus    func(id string)
    OnBlur     func(id string)
    OnSubmit   func(id string, value float64)
}
```

<a name="NumberInputField"></a>
## type NumberInputField

NumberInputField is a builder for a numeric text input component.

```go
type NumberInputField struct {
    // contains filtered or unexported fields
}
```

<a name="NumberInput"></a>
### func NumberInput

```go
func NumberInput() *NumberInputField
```

NumberInput creates a new NumberInputField with sensible defaults. The field starts Empty \(no value\) and displays Placeholder until the user types something or Value\(\) is called explicitly.

<details><summary>Example</summary>
<p>

Example usage test

```go
// Integer input
ageInput := NumberInput().
	ID("age").
	Placeholder("Enter age").
	Width(20).
	Min(0).
	Max(150).
	Step(1).
	Decimals(0).
	Prefix("👤 ").
	Suffix(" years").
	OnChange(func(id string, value float64) {
		// Handle change
	}).
	OnSubmit(func(id string, value float64) {
		// Handle submit
	}).
	Render()
_ = ageInput

// Decimal input
priceInput := NumberInput().
	ID("price").
	Placeholder("0.00").
	Width(25).
	Min(0).
	Max(9999.99).
	Step(0.01).
	Decimals(2).
	Prefix("$ ").
	Suffix(" USD").
	Render()
_ = priceInput
```

</p>
</details>

<a name="NumberInputField.ArrowStep"></a>
### func \(\*NumberInputField\) ArrowStep

```go
func (n *NumberInputField) ArrowStep(v bool) *NumberInputField
```

ArrowStep controls whether Up/Down change the value. Disable this when the surrounding form/list also uses Up/Down to move focus between fields, so the same keypress can't both step the value and blur the field at once.

<a name="NumberInputField.Decimals"></a>
### func \(\*NumberInputField\) Decimals

```go
func (n *NumberInputField) Decimals(v int) *NumberInputField
```

Decimals sets how many digits after the decimal point are allowed. 0 means the field only accepts integers \(no decimal point at all\).

<a name="NumberInputField.Empty"></a>
### func \(\*NumberInputField\) Empty

```go
func (n *NumberInputField) Empty(v bool) *NumberInputField
```

Empty explicitly marks the field as having no value, showing the placeholder regardless of the current Value. Useful for reset/clear.

<a name="NumberInputField.Focused"></a>
### func \(\*NumberInputField\) Focused

```go
func (n *NumberInputField) Focused(v bool) *NumberInputField
```



<a name="NumberInputField.ID"></a>
### func \(\*NumberInputField\) ID

```go
func (n *NumberInputField) ID(v string) *NumberInputField
```



<a name="NumberInputField.Max"></a>
### func \(\*NumberInputField\) Max

```go
func (n *NumberInputField) Max(v float64) *NumberInputField
```

Max sets a maximum bound and enables its enforcement \(including a legitimate maximum of exactly 0\).

<a name="NumberInputField.Min"></a>
### func \(\*NumberInputField\) Min

```go
func (n *NumberInputField) Min(v float64) *NumberInputField
```

Min sets a minimum bound and enables its enforcement \(including a legitimate minimum of exactly 0\).

<a name="NumberInputField.OnBlur"></a>
### func \(\*NumberInputField\) OnBlur

```go
func (n *NumberInputField) OnBlur(fn func(string)) *NumberInputField
```



<a name="NumberInputField.OnChange"></a>
### func \(\*NumberInputField\) OnChange

```go
func (n *NumberInputField) OnChange(fn func(string, float64)) *NumberInputField
```



<a name="NumberInputField.OnFocus"></a>
### func \(\*NumberInputField\) OnFocus

```go
func (n *NumberInputField) OnFocus(fn func(string)) *NumberInputField
```



<a name="NumberInputField.OnKeyPress"></a>
### func \(\*NumberInputField\) OnKeyPress

```go
func (n *NumberInputField) OnKeyPress(fn func(string, retui.Key) bool) *NumberInputField
```



<a name="NumberInputField.OnSubmit"></a>
### func \(\*NumberInputField\) OnSubmit

```go
func (n *NumberInputField) OnSubmit(fn func(string, float64)) *NumberInputField
```



<a name="NumberInputField.Placeholder"></a>
### func \(\*NumberInputField\) Placeholder

```go
func (n *NumberInputField) Placeholder(v string) *NumberInputField
```



<a name="NumberInputField.Prefix"></a>
### func \(\*NumberInputField\) Prefix

```go
func (n *NumberInputField) Prefix(v string) *NumberInputField
```



<a name="NumberInputField.Render"></a>
### func \(\*NumberInputField\) Render

```go
func (n *NumberInputField) Render() retui.Element
```



<a name="NumberInputField.SelectAllOnFocus"></a>
### func \(\*NumberInputField\) SelectAllOnFocus

```go
func (n *NumberInputField) SelectAllOnFocus(v bool) *NumberInputField
```

SelectAllOnFocus toggles web\-style select\-all\-on\-focus behavior. Enabled by default; pass false to keep the cursor position instead.

<a name="NumberInputField.Step"></a>
### func \(\*NumberInputField\) Step

```go
func (n *NumberInputField) Step(v float64) *NumberInputField
```

Step sets the increment/decrement applied by the Up/Down keys. Non\-positive values are ignored and fall back to the default of 1.

<a name="NumberInputField.Style"></a>
### func \(\*NumberInputField\) Style

```go
func (n *NumberInputField) Style(s retui.Style) *NumberInputField
```



<a name="NumberInputField.Suffix"></a>
### func \(\*NumberInputField\) Suffix

```go
func (n *NumberInputField) Suffix(v string) *NumberInputField
```



<a name="NumberInputField.Value"></a>
### func \(\*NumberInputField\) Value

```go
func (n *NumberInputField) Value(v float64) *NumberInputField
```

Value sets an explicit starting value and marks the field as non\-empty, even if v is 0. Use this to pre\-fill a field with a real zero value.

<a name="NumberInputField.Width"></a>
### func \(\*NumberInputField\) Width

```go
func (n *NumberInputField) Width(w int) *NumberInputField
```



<a name="PasswordConfig"></a>
## type PasswordConfig



```go
type PasswordConfig struct {
    ID           string
    Value        string
    Placeholder  string
    Width        int
    Style        retui.Style
    Prefix       string
    Suffix       string
    MinLength    int    // Minimum length (0 = no limit)
    MaxLength    int    // Maximum length (0 = no limit)
    MaskChar     string // Character to mask input (default: "•")
    ShowLastChar bool   // Show last character unmasked (default: true)
    OnChange     func(id string, value string)
    OnKeyPress   func(id string, key retui.Key) bool
    OnFocus      func(id string)
    OnBlur       func(id string)
    OnSubmit     func(id string, value string)
}
```

<a name="PasswordFeild"></a>
## type PasswordFeild



```go
type PasswordFeild struct {
    // contains filtered or unexported fields
}
```

<a name="Password"></a>
### func Password

```go
func Password() *PasswordFeild
```



<details><summary>Example</summary>
<p>

Example usage test

```go
// Simple password input
passwordInput := Password().
	ID("password").
	Placeholder("Enter password").
	Width(30).
	MinLength(8).
	MaxLength(20).
	Prefix("🔒 ").
	Suffix(" ✓").
	OnChange(func(id string, value string) {
		// Handle change
	}).
	OnSubmit(func(id string, value string) {
		// Handle submit
	}).
	Render()
_ = passwordInput

// Password with custom mask
customMask := Password().
	ID("pin").
	Placeholder("Enter PIN").
	Width(20).
	MaskChar("*").
	ShowLastChar(false).
	MaxLength(4).
	Prefix("PIN: ").
	Render()
_ = customMask
```

</p>
</details>

<a name="PasswordFeild.Focused"></a>
### func \(\*PasswordFeild\) Focused

```go
func (p *PasswordFeild) Focused(v bool) *PasswordFeild
```



<a name="PasswordFeild.ID"></a>
### func \(\*PasswordFeild\) ID

```go
func (p *PasswordFeild) ID(v string) *PasswordFeild
```



<a name="PasswordFeild.MaskChar"></a>
### func \(\*PasswordFeild\) MaskChar

```go
func (p *PasswordFeild) MaskChar(v string) *PasswordFeild
```



<a name="PasswordFeild.MaxLength"></a>
### func \(\*PasswordFeild\) MaxLength

```go
func (p *PasswordFeild) MaxLength(v int) *PasswordFeild
```



<a name="PasswordFeild.MinLength"></a>
### func \(\*PasswordFeild\) MinLength

```go
func (p *PasswordFeild) MinLength(v int) *PasswordFeild
```



<a name="PasswordFeild.OnBlur"></a>
### func \(\*PasswordFeild\) OnBlur

```go
func (p *PasswordFeild) OnBlur(fn func(string)) *PasswordFeild
```



<a name="PasswordFeild.OnChange"></a>
### func \(\*PasswordFeild\) OnChange

```go
func (p *PasswordFeild) OnChange(fn func(string, string)) *PasswordFeild
```



<a name="PasswordFeild.OnFocus"></a>
### func \(\*PasswordFeild\) OnFocus

```go
func (p *PasswordFeild) OnFocus(fn func(string)) *PasswordFeild
```



<a name="PasswordFeild.OnKeyPress"></a>
### func \(\*PasswordFeild\) OnKeyPress

```go
func (p *PasswordFeild) OnKeyPress(fn func(string, retui.Key) bool) *PasswordFeild
```



<a name="PasswordFeild.OnSubmit"></a>
### func \(\*PasswordFeild\) OnSubmit

```go
func (p *PasswordFeild) OnSubmit(fn func(string, string)) *PasswordFeild
```



<a name="PasswordFeild.Placeholder"></a>
### func \(\*PasswordFeild\) Placeholder

```go
func (p *PasswordFeild) Placeholder(v string) *PasswordFeild
```



<a name="PasswordFeild.Prefix"></a>
### func \(\*PasswordFeild\) Prefix

```go
func (p *PasswordFeild) Prefix(v string) *PasswordFeild
```



<a name="PasswordFeild.Render"></a>
### func \(\*PasswordFeild\) Render

```go
func (p *PasswordFeild) Render() retui.Element
```



<a name="PasswordFeild.ShowLastChar"></a>
### func \(\*PasswordFeild\) ShowLastChar

```go
func (p *PasswordFeild) ShowLastChar(v bool) *PasswordFeild
```



<a name="PasswordFeild.Style"></a>
### func \(\*PasswordFeild\) Style

```go
func (p *PasswordFeild) Style(s retui.Style) *PasswordFeild
```



<a name="PasswordFeild.Suffix"></a>
### func \(\*PasswordFeild\) Suffix

```go
func (p *PasswordFeild) Suffix(v string) *PasswordFeild
```



<a name="PasswordFeild.Value"></a>
### func \(\*PasswordFeild\) Value

```go
func (p *PasswordFeild) Value(v string) *PasswordFeild
```



<a name="PasswordFeild.Width"></a>
### func \(\*PasswordFeild\) Width

```go
func (p *PasswordFeild) Width(w int) *PasswordFeild
```



<a name="SelectConfig"></a>
## type SelectConfig



```go
type SelectConfig struct {
    ID           string
    Label        string
    Options      []SelectOption
    Value        string
    Placeholder  string
    Width        int
    Height       int
    Style        retui.Style
    Disabled     bool
    OnChange     func(id string, value string)
    OnKeyPress   func(id string, key retui.Key) bool
    OnFocus      func(id string)
    OnBlur       func(id string)
    OnOpenChange func(id string, isOpen bool)

    // OnFilter, when set, is the single source of truth for what options
    // are shown — including the empty query. This means:
    //
    //   - Options can be left empty entirely for a server/DB-backed select.
    //     Opening the dropdown calls OnFilter(id, "") to load the default
    //     page, exactly like typing calls it to load filtered results.
    //   - Static Options is ignored once OnFilter is set (it's never mixed
    //     with the local substring matcher).
    //
    // OnFilter is called exactly once per distinct query — on open, and
    // once per keystroke that actually changes the search text. It is
    // never called again just because the user pressed an arrow key, and
    // it is never called more than once for the same query. It's called
    // synchronously, no debounce — debounce it yourself if the source is
    // slow (e.g. wrap it and only call through after a timer).
    OnFilter func(id string, query string) []SelectOption

    OverlayAbsX int
    OverlayAbsY int
}
```

<a name="SelectField"></a>
## type SelectField



```go
type SelectField struct {
    // contains filtered or unexported fields
}
```

<a name="SelectDropdown"></a>
### func SelectDropdown

```go
func SelectDropdown() *SelectField
```



<details><summary>Example</summary>
<p>

Example usage test

```go
// Create a select dropdown with options
selectDropdown := SelectDropdown().
	ID("color").
	Label("Choose Color").
	Options([]SelectOption{
		{Label: "Red", Value: "red"},
		{Label: "Green", Value: "green"},
		{Label: "Blue", Value: "blue"},
		{Label: "Yellow", Value: "yellow", Disabled: true},
	}).
	Value("red").
	Placeholder("Select a color...").
	Width(30).
	Height(5).
	OnChange(func(id string, value string) {
		// Handle selection change
	}).
	Render()
_ = selectDropdown
```

</p>
</details>

<a name="SelectField.Disabled"></a>
### func \(\*SelectField\) Disabled

```go
func (s *SelectField) Disabled(v bool) *SelectField
```



<a name="SelectField.Focused"></a>
### func \(\*SelectField\) Focused

```go
func (s *SelectField) Focused(v bool) *SelectField
```



<a name="SelectField.Height"></a>
### func \(\*SelectField\) Height

```go
func (s *SelectField) Height(h int) *SelectField
```



<a name="SelectField.ID"></a>
### func \(\*SelectField\) ID

```go
func (s *SelectField) ID(v string) *SelectField
```



<a name="SelectField.Label"></a>
### func \(\*SelectField\) Label

```go
func (s *SelectField) Label(v string) *SelectField
```



<a name="SelectField.OnBlur"></a>
### func \(\*SelectField\) OnBlur

```go
func (s *SelectField) OnBlur(fn func(string)) *SelectField
```



<a name="SelectField.OnChange"></a>
### func \(\*SelectField\) OnChange

```go
func (s *SelectField) OnChange(fn func(string, string)) *SelectField
```



<a name="SelectField.OnFilter"></a>
### func \(\*SelectField\) OnFilter

```go
func (s *SelectField) OnFilter(fn func(string, string) []SelectOption) *SelectField
```

OnFilter registers a data source for the dropdown. Called with the current search text — including "" when the dropdown first opens, so it also doubles as "load default options". See SelectConfig.OnFilter for the full contract.

<a name="SelectField.OnFocus"></a>
### func \(\*SelectField\) OnFocus

```go
func (s *SelectField) OnFocus(fn func(string)) *SelectField
```



<a name="SelectField.OnKeyPress"></a>
### func \(\*SelectField\) OnKeyPress

```go
func (s *SelectField) OnKeyPress(fn func(string, retui.Key) bool) *SelectField
```



<a name="SelectField.OnOpenChange"></a>
### func \(\*SelectField\) OnOpenChange

```go
func (s *SelectField) OnOpenChange(fn func(string, bool)) *SelectField
```



<a name="SelectField.Options"></a>
### func \(\*SelectField\) Options

```go
func (s *SelectField) Options(v []SelectOption) *SelectField
```



<a name="SelectField.OverlayAbsPos"></a>
### func \(\*SelectField\) OverlayAbsPos

```go
func (s *SelectField) OverlayAbsPos(x, y int) *SelectField
```



<a name="SelectField.Placeholder"></a>
### func \(\*SelectField\) Placeholder

```go
func (s *SelectField) Placeholder(v string) *SelectField
```



<a name="SelectField.Render"></a>
### func \(\*SelectField\) Render

```go
func (s *SelectField) Render() retui.Element
```



<a name="SelectField.Style"></a>
### func \(\*SelectField\) Style

```go
func (s *SelectField) Style(st retui.Style) *SelectField
```



<a name="SelectField.Value"></a>
### func \(\*SelectField\) Value

```go
func (s *SelectField) Value(v string) *SelectField
```



<a name="SelectField.Width"></a>
### func \(\*SelectField\) Width

```go
func (s *SelectField) Width(w int) *SelectField
```



<a name="SelectOption"></a>
## type SelectOption



```go
type SelectOption struct {
    Label    string
    Value    string
    Disabled bool
}
```

<a name="SelectPickerConfig"></a>
## type SelectPickerConfig



```go
type SelectPickerConfig struct {
    ID         string
    Options    []string
    Selected   int // Index of selected option
    Width      int
    Style      retui.Style
    Prefix     string
    Suffix     string
    OnChange   func(id string, selected int, value string)
    OnKeyPress func(id string, key retui.Key) bool
    OnFocus    func(id string)
    OnBlur     func(id string)
    OnSubmit   func(id string, selected int, value string)
}
```

<a name="SelectPickerField"></a>
## type SelectPickerField



```go
type SelectPickerField struct {
    // contains filtered or unexported fields
}
```

<a name="SelectPicker"></a>
### func SelectPicker

```go
func SelectPicker() *SelectPickerField
```

─── Builder Methods ────────────────────────────────────────────────────── colorPicker := SelectPicker\(\).

```
ID("color").
Options([]string{"Red", "Green", "Blue", "Yellow"}).
Selected(0).
Width(20).
Prefix("🎨 ").
Suffix(" ✓").
OnChange(func(id string, selected int, value string) {
	println("Selected:", value, "at index:", selected)
}).
OnSubmit(func(id string, selected int, value string) {
	println("Submitted:", value)
})
```

<a name="SelectPickerField.Focused"></a>
### func \(\*SelectPickerField\) Focused

```go
func (s *SelectPickerField) Focused(v bool) *SelectPickerField
```



<a name="SelectPickerField.ID"></a>
### func \(\*SelectPickerField\) ID

```go
func (s *SelectPickerField) ID(v string) *SelectPickerField
```



<a name="SelectPickerField.OnBlur"></a>
### func \(\*SelectPickerField\) OnBlur

```go
func (s *SelectPickerField) OnBlur(fn func(string)) *SelectPickerField
```



<a name="SelectPickerField.OnChange"></a>
### func \(\*SelectPickerField\) OnChange

```go
func (s *SelectPickerField) OnChange(fn func(string, int, string)) *SelectPickerField
```



<a name="SelectPickerField.OnFocus"></a>
### func \(\*SelectPickerField\) OnFocus

```go
func (s *SelectPickerField) OnFocus(fn func(string)) *SelectPickerField
```



<a name="SelectPickerField.OnKeyPress"></a>
### func \(\*SelectPickerField\) OnKeyPress

```go
func (s *SelectPickerField) OnKeyPress(fn func(string, retui.Key) bool) *SelectPickerField
```



<a name="SelectPickerField.OnSubmit"></a>
### func \(\*SelectPickerField\) OnSubmit

```go
func (s *SelectPickerField) OnSubmit(fn func(string, int, string)) *SelectPickerField
```



<a name="SelectPickerField.Options"></a>
### func \(\*SelectPickerField\) Options

```go
func (s *SelectPickerField) Options(v []string) *SelectPickerField
```



<a name="SelectPickerField.Prefix"></a>
### func \(\*SelectPickerField\) Prefix

```go
func (s *SelectPickerField) Prefix(v string) *SelectPickerField
```



<a name="SelectPickerField.Render"></a>
### func \(\*SelectPickerField\) Render

```go
func (s *SelectPickerField) Render() retui.Element
```



<a name="SelectPickerField.Selected"></a>
### func \(\*SelectPickerField\) Selected

```go
func (s *SelectPickerField) Selected(v int) *SelectPickerField
```



<a name="SelectPickerField.Style"></a>
### func \(\*SelectPickerField\) Style

```go
func (s *SelectPickerField) Style(style retui.Style) *SelectPickerField
```



<a name="SelectPickerField.Suffix"></a>
### func \(\*SelectPickerField\) Suffix

```go
func (s *SelectPickerField) Suffix(v string) *SelectPickerField
```



<a name="SelectPickerField.Width"></a>
### func \(\*SelectPickerField\) Width

```go
func (s *SelectPickerField) Width(w int) *SelectPickerField
```



<a name="TableField"></a>
## type TableField

Table represents a table component with fluent API

```go
type TableField struct {
    // contains filtered or unexported fields
}
```

<a name="Table"></a>
### func Table

```go
func Table() *TableField
```

NewTable creates a new table with default configuration

<details><summary>Example</summary>
<p>

Example usage test

```go
// Create a table
table := Table().
	ID("users").
	Headers([]string{"Name", "Age", "City"}).
	Rows([][]string{
		{"Alice", "30", "NYC"},
		{"Bob", "25", "LA"},
		{"Charlie", "35", "Chicago"},
	}).
	Width(50).
	Height(10).
	SelectedIndex(0).
	OnChange(func(index int) {
		// Handle selection change
	}).
	Render()
_ = table
```

</p>
</details>

<a name="TableField.Alignments"></a>
### func \(\*TableField\) Alignments

```go
func (t *TableField) Alignments(alignments []string) *TableField
```

Alignments sets column alignments \(left, center, right\)

<a name="TableField.BorderColor"></a>
### func \(\*TableField\) BorderColor

```go
func (t *TableField) BorderColor(color retui.Color) *TableField
```

BorderColor sets the border color

<a name="TableField.BorderStyle"></a>
### func \(\*TableField\) BorderStyle

```go
func (t *TableField) BorderStyle(style retui.Style) *TableField
```

BorderStyle sets the border style

<a name="TableField.CellPadding"></a>
### func \(\*TableField\) CellPadding

```go
func (t *TableField) CellPadding(padding int) *TableField
```

CellPadding sets the cell padding

<a name="TableField.ColumnWidths"></a>
### func \(\*TableField\) ColumnWidths

```go
func (t *TableField) ColumnWidths(widths []int) *TableField
```

ColumnWidths sets explicit column widths

<a name="TableField.Focused"></a>
### func \(\*TableField\) Focused

```go
func (t *TableField) Focused(focused bool) *TableField
```

Focused sets whether the table is focused

<a name="TableField.HeaderBackground"></a>
### func \(\*TableField\) HeaderBackground

```go
func (t *TableField) HeaderBackground(color retui.Color) *TableField
```

HeaderBackground sets the header background color

<a name="TableField.HeaderColor"></a>
### func \(\*TableField\) HeaderColor

```go
func (t *TableField) HeaderColor(color retui.Color) *TableField
```

HeaderColor sets the header text color

<a name="TableField.HeaderStyle"></a>
### func \(\*TableField\) HeaderStyle

```go
func (t *TableField) HeaderStyle(style retui.Style) *TableField
```

HeaderStyle sets the header text style

<a name="TableField.Headers"></a>
### func \(\*TableField\) Headers

```go
func (t *TableField) Headers(headers []string) *TableField
```

Headers sets the table headers

<a name="TableField.Height"></a>
### func \(\*TableField\) Height

```go
func (t *TableField) Height(height int) *TableField
```

Height sets an explicit, hard\-coded table height \(enables row scrolling/truncation to fit\). Optional — if you don't call this, the table shows all rows and sizes itself naturally to fit its content.

<a name="TableField.ID"></a>
### func \(\*TableField\) ID

```go
func (i *TableField) ID(v string) *TableField
```

ID of table

<a name="TableField.MaxColumnWidth"></a>
### func \(\*TableField\) MaxColumnWidth

```go
func (t *TableField) MaxColumnWidth(width int) *TableField
```

MaxColumnWidth sets the maximum column width

<a name="TableField.MinColumnWidth"></a>
### func \(\*TableField\) MinColumnWidth

```go
func (t *TableField) MinColumnWidth(width int) *TableField
```

MinColumnWidth sets the minimum column width

<a name="TableField.OnChange"></a>
### func \(\*TableField\) OnChange

```go
func (t *TableField) OnChange(fn func(int)) *TableField
```

OnChange sets the callback for selection changes

<a name="TableField.Render"></a>
### func \(\*TableField\) Render

```go
func (t *TableField) Render() retui.Element
```

Render renders the table as a retui.Element.

It does NOT build the table's rows/columns immediately. Column widths depend on the space actually available, which isn't known yet at this point — it depends on whatever parent \(e.g. Box\) this element ends up inside, and that's only resolved during the layout pass. So Render returns a placeholder Element carrying a ContentBuilder: the renderer calls it back with the real resolved width/height once layout knows them, and \*that's\* when t.build\(\) actually runs.

<a name="TableField.RowBackground"></a>
### func \(\*TableField\) RowBackground

```go
func (t *TableField) RowBackground(color retui.Color) *TableField
```

RowBackground sets the row background color

<a name="TableField.RowColor"></a>
### func \(\*TableField\) RowColor

```go
func (t *TableField) RowColor(color retui.Color) *TableField
```

RowColor sets the row text color

<a name="TableField.RowStyle"></a>
### func \(\*TableField\) RowStyle

```go
func (t *TableField) RowStyle(style retui.Style) *TableField
```

RowStyle sets the row text style

<a name="TableField.Rows"></a>
### func \(\*TableField\) Rows

```go
func (t *TableField) Rows(rows [][]string) *TableField
```

Rows sets the table rows

<a name="TableField.Selectable"></a>
### func \(\*TableField\) Selectable

```go
func (t *TableField) Selectable(selectable bool) *TableField
```

Selectable sets whether the table is selectable

<a name="TableField.SelectedBackground"></a>
### func \(\*TableField\) SelectedBackground

```go
func (t *TableField) SelectedBackground(color retui.Color) *TableField
```

SelectedBackground sets the selected row background color

<a name="TableField.SelectedForeground"></a>
### func \(\*TableField\) SelectedForeground

```go
func (t *TableField) SelectedForeground(color retui.Color) *TableField
```

SelectedForeground sets the selected row text color

<a name="TableField.SelectedIndex"></a>
### func \(\*TableField\) SelectedIndex

```go
func (t *TableField) SelectedIndex(index int) *TableField
```

SelectedIndex sets the selected row index

<a name="TableField.SelectedStyle"></a>
### func \(\*TableField\) SelectedStyle

```go
func (t *TableField) SelectedStyle(style retui.Style) *TableField
```

SelectedStyle sets the selected row text style

<a name="TableField.ShowBorders"></a>
### func \(\*TableField\) ShowBorders

```go
func (t *TableField) ShowBorders(show bool) *TableField
```

ShowBorders sets whether to show borders

<a name="TableField.ShowHeaders"></a>
### func \(\*TableField\) ShowHeaders

```go
func (t *TableField) ShowHeaders(show bool) *TableField
```

ShowHeaders sets whether to show headers

<a name="TableField.ShowSelection"></a>
### func \(\*TableField\) ShowSelection

```go
func (t *TableField) ShowSelection(show bool) *TableField
```

ShowSelection sets whether to show selection

<a name="TableField.Width"></a>
### func \(\*TableField\) Width

```go
func (t *TableField) Width(width int) *TableField
```

Width sets an explicit, hard\-coded table width. Optional — if you don't call this, the table automatically fills whatever width its parent \(e.g. a Box\) assigns it.

<a name="TextAreaConfig"></a>
## type TextAreaConfig



```go
type TextAreaConfig struct {
    ID          string
    Value       string
    Placeholder string
    Width       int
    Height      int
    Style       retui.Style
    Prefix      string
    Suffix      string
    MinLength   int
    MaxLength   int
    OnChange    func(id string, value string)
    OnKeyPress  func(id string, key retui.Key) bool
    OnFocus     func(id string)
    OnBlur      func(id string)
    OnSubmit    func(id string, value string)
}
```

<a name="TextAreaField"></a>
## type TextAreaField



```go
type TextAreaField struct {
    // contains filtered or unexported fields
}
```

<a name="TextArea"></a>
### func TextArea

```go
func TextArea() *TextAreaField
```



<details><summary>Example</summary>
<p>

Example usage test

```go
// Simple text area
textArea := TextArea().
	ID("bio").
	Placeholder("Enter your bio").
	Width(40).
	Height(5).
	MinLength(10).
	MaxLength(500).
	Prefix("📝 ").
	Suffix(" ✏️").
	OnChange(func(id string, value string) {
		// Handle change
	}).
	OnSubmit(func(id string, value string) {
		// Handle submit
	}).
	Render()
_ = textArea
```

</p>
</details>

<a name="TextAreaField.Focused"></a>
### func \(\*TextAreaField\) Focused

```go
func (t *TextAreaField) Focused(v bool) *TextAreaField
```



<a name="TextAreaField.Height"></a>
### func \(\*TextAreaField\) Height

```go
func (t *TextAreaField) Height(h int) *TextAreaField
```



<a name="TextAreaField.ID"></a>
### func \(\*TextAreaField\) ID

```go
func (t *TextAreaField) ID(v string) *TextAreaField
```



<a name="TextAreaField.MaxLength"></a>
### func \(\*TextAreaField\) MaxLength

```go
func (t *TextAreaField) MaxLength(v int) *TextAreaField
```



<a name="TextAreaField.MinLength"></a>
### func \(\*TextAreaField\) MinLength

```go
func (t *TextAreaField) MinLength(v int) *TextAreaField
```



<a name="TextAreaField.OnBlur"></a>
### func \(\*TextAreaField\) OnBlur

```go
func (t *TextAreaField) OnBlur(fn func(string)) *TextAreaField
```



<a name="TextAreaField.OnChange"></a>
### func \(\*TextAreaField\) OnChange

```go
func (t *TextAreaField) OnChange(fn func(string, string)) *TextAreaField
```



<a name="TextAreaField.OnFocus"></a>
### func \(\*TextAreaField\) OnFocus

```go
func (t *TextAreaField) OnFocus(fn func(string)) *TextAreaField
```



<a name="TextAreaField.OnKeyPress"></a>
### func \(\*TextAreaField\) OnKeyPress

```go
func (t *TextAreaField) OnKeyPress(fn func(string, retui.Key) bool) *TextAreaField
```



<a name="TextAreaField.OnSubmit"></a>
### func \(\*TextAreaField\) OnSubmit

```go
func (t *TextAreaField) OnSubmit(fn func(string, string)) *TextAreaField
```



<a name="TextAreaField.Placeholder"></a>
### func \(\*TextAreaField\) Placeholder

```go
func (t *TextAreaField) Placeholder(v string) *TextAreaField
```



<a name="TextAreaField.Prefix"></a>
### func \(\*TextAreaField\) Prefix

```go
func (t *TextAreaField) Prefix(v string) *TextAreaField
```



<a name="TextAreaField.Render"></a>
### func \(\*TextAreaField\) Render

```go
func (t *TextAreaField) Render() retui.Element
```



<a name="TextAreaField.Style"></a>
### func \(\*TextAreaField\) Style

```go
func (t *TextAreaField) Style(s retui.Style) *TextAreaField
```



<a name="TextAreaField.Suffix"></a>
### func \(\*TextAreaField\) Suffix

```go
func (t *TextAreaField) Suffix(v string) *TextAreaField
```



<a name="TextAreaField.Value"></a>
### func \(\*TextAreaField\) Value

```go
func (t *TextAreaField) Value(v string) *TextAreaField
```



<a name="TextAreaField.Width"></a>
### func \(\*TextAreaField\) Width

```go
func (t *TextAreaField) Width(w int) *TextAreaField
```



<a name="Toast"></a>
## type Toast



```go
type Toast struct {
    ID       int64
    Message  string
    Type     ToastType
    Duration time.Duration // 0 = persists until Dismiss()
    Created  time.Time
    Position ToastPosition
}
```

<a name="Toast.Height"></a>
### func \(\*Toast\) Height

```go
func (t *Toast) Height() int
```

Height is fixed at 1 row \(flat bar, no border\).

<a name="Toast.Style"></a>
### func \(\*Toast\) Style

```go
func (t *Toast) Style() retui.Style
```

Style returns the color styling for this toast's type.

<a name="Toast.Title"></a>
### func \(\*Toast\) Title

```go
func (t *Toast) Title() string
```

Title returns the label shown before the message, based on type.

<a name="Toast.Width"></a>
### func \(\*Toast\) Width

```go
func (t *Toast) Width() int
```

Width returns the rendered width: title \+ message \+ horizontal padding.

<a name="ToastManager"></a>
## type ToastManager



```go
type ToastManager struct {
    // contains filtered or unexported fields
}
```

<a name="NewToastManager"></a>
### func NewToastManager

```go
func NewToastManager() *ToastManager
```



<a name="ToastManager.Clear"></a>
### func \(\*ToastManager\) Clear

```go
func (m *ToastManager) Clear()
```



<a name="ToastManager.Configure"></a>
### func \(\*ToastManager\) Configure

```go
func (m *ToastManager) Configure(opts ...ManagerOption)
```

Configure sets this manager's defaults \(position, duration\) for all future Show\(\)/ShowX\(\) calls that don't explicitly override them via a ToastOption. Typically called once at app startup.

<a name="ToastManager.Dismiss"></a>
### func \(\*ToastManager\) Dismiss

```go
func (m *ToastManager) Dismiss(id int64)
```



<a name="ToastManager.HasToast"></a>
### func \(\*ToastManager\) HasToast

```go
func (m *ToastManager) HasToast() bool
```



<a name="ToastManager.OnChange"></a>
### func \(\*ToastManager\) OnChange

```go
func (m *ToastManager) OnChange(fn func())
```

OnChange registers a callback fired whenever the toast set changes \(added, dismissed, or expired\-on\-sweep\). Wire this to your app's redraw/tick mechanism so toasts appear and disappear promptly instead of waiting for an unrelated input event to trigger the next render.

<a name="ToastManager.Show"></a>
### func \(\*ToastManager\) Show

```go
func (m *ToastManager) Show(msg string, opts ...ToastOption) int64
```



<a name="ToastManager.Toasts"></a>
### func \(\*ToastManager\) Toasts

```go
func (m *ToastManager) Toasts() []*Toast
```



<a name="ToastOption"></a>
## type ToastOption



```go
type ToastOption func(*Toast)
```

<a name="WithDuration"></a>
### func WithDuration

```go
func WithDuration(d time.Duration) ToastOption
```



<a name="WithPosition"></a>
### func WithPosition

```go
func WithPosition(p ToastPosition) ToastOption
```



<a name="WithType"></a>
### func WithType

```go
func WithType(t ToastType) ToastOption
```



<a name="ToastPosition"></a>
## type ToastPosition



```go
type ToastPosition int
```

<a name="ToastBottomRight"></a>

```go
const (
    ToastBottomRight ToastPosition = iota
    ToastBottomLeft
    ToastTopRight
    ToastTopLeft
)
```

<a name="ToastType"></a>
## type ToastType



```go
type ToastType int
```

<a name="ToastInfo"></a>

```go
const (
    ToastInfo ToastType = iota
    ToastSuccess
    ToastWarning
    ToastError
)
```

<a name="TreeNode"></a>
## type TreeNode



```go
type TreeNode struct {
    Label    string
    ID       string
    Children []TreeNode
}
```

# window

```go
import "github.com/subhasundardass/retui/retui/window"
```

## Index

- [Variables](<#variables>)
- [func CloseAll\(\)](<#CloseAll>)
- [func CloseFocusedWindow\(\)](<#CloseFocusedWindow>)
- [func Count\(\) int](<#Count>)
- [func CountVisible\(\) int](<#CountVisible>)
- [func GetActiveModal\(\) string](<#GetActiveModal>)
- [func GetFocused\(\) string](<#GetFocused>)
- [func GetFocusedID\(\) string](<#GetFocusedID>)
- [func GetScreenSize\(\) \(int, int\)](<#GetScreenSize>)
- [func IsAnyModalOpen\(\) bool](<#IsAnyModalOpen>)
- [func IsWindowBlocked\(id string\) bool](<#IsWindowBlocked>)
- [func RenderWindowsOverlay\(mainContent retui.Element\) retui.Element](<#RenderWindowsOverlay>)
- [func ResetGlobalManager\(\)](<#ResetGlobalManager>)
- [func SetDefaultScreenSize\(width, height int\)](<#SetDefaultScreenSize>)
- [func SetRenderTrigger\(trigger func\(\)\)](<#SetRenderTrigger>)
- [func SetScreenSize\(width, height int\)](<#SetScreenSize>)
- [type OverlayRenderer](<#OverlayRenderer>)
  - [func NewOverlayRenderer\(\) \*OverlayRenderer](<#NewOverlayRenderer>)
  - [func \(or \*OverlayRenderer\) Render\(mainContent retui.Element\) retui.Element](<#OverlayRenderer.Render>)
- [type Window](<#Window>)
  - [func AlertError\(title string, content retui.Element\) \*Window](<#AlertError>)
  - [func GetVisible\(\) \[\]\*Window](<#GetVisible>)
  - [func GetWindowByID\(id string\) \*Window](<#GetWindowByID>)
  - [func NewWindow\(\) \*Window](<#NewWindow>)
  - [func \(w \*Window\) Center\(\) \*Window](<#Window.Center>)
  - [func \(w \*Window\) CenterHorizontally\(screenWidth int\) \*Window](<#Window.CenterHorizontally>)
  - [func \(w \*Window\) CenterVertically\(screenHeight int\) \*Window](<#Window.CenterVertically>)
  - [func \(w \*Window\) Clone\(\) \*Window](<#Window.Clone>)
  - [func \(w \*Window\) Close\(\)](<#Window.Close>)
  - [func \(w \*Window\) Focus\(\)](<#Window.Focus>)
  - [func \(w \*Window\) GetBodyBgColor\(\) retui.Color](<#Window.GetBodyBgColor>)
  - [func \(w \*Window\) GetBounds\(\) \[4\]int](<#Window.GetBounds>)
  - [func \(w \*Window\) GetContent\(\) retui.Element](<#Window.GetContent>)
  - [func \(w \*Window\) GetPosition\(\) \[2\]int](<#Window.GetPosition>)
  - [func \(w \*Window\) GetSize\(\) \[2\]int](<#Window.GetSize>)
  - [func \(w \*Window\) GetTitle\(\) string](<#Window.GetTitle>)
  - [func \(w \*Window\) GetTitleBarBgColor\(\) retui.Color](<#Window.GetTitleBarBgColor>)
  - [func \(w \*Window\) HandleKey\(key retui.Key\) bool](<#Window.HandleKey>)
  - [func \(w \*Window\) Hide\(\) \*Window](<#Window.Hide>)
  - [func \(w \*Window\) IsActive\(\) bool](<#Window.IsActive>)
  - [func \(w \*Window\) IsFocused\(\) bool](<#Window.IsFocused>)
  - [func \(w \*Window\) IsModal\(\) bool](<#Window.IsModal>)
  - [func \(w \*Window\) IsVisible\(\) bool](<#Window.IsVisible>)
  - [func \(w \*Window\) MoveBy\(dx, dy int\) \*Window](<#Window.MoveBy>)
  - [func \(w \*Window\) MoveTo\(x, y int\) \*Window](<#Window.MoveTo>)
  - [func \(w \*Window\) OnKeyPress\(fn func\(key retui.Key\) bool\) \*Window](<#Window.OnKeyPress>)
  - [func \(w \*Window\) Render\(\) retui.Element](<#Window.Render>)
  - [func \(w \*Window\) ResizeTo\(width, height int\) \*Window](<#Window.ResizeTo>)
  - [func \(w \*Window\) SetBodyBgColor\(color retui.Color\) \*Window](<#Window.SetBodyBgColor>)
  - [func \(w \*Window\) SetContent\(content retui.Element\) \*Window](<#Window.SetContent>)
  - [func \(w \*Window\) SetModal\(modal bool\) \*Window](<#Window.SetModal>)
  - [func \(w \*Window\) SetPosition\(x, y int\) \*Window](<#Window.SetPosition>)
  - [func \(w \*Window\) SetRenderFn\(fn func\(\) retui.Element\) \*Window](<#Window.SetRenderFn>)
  - [func \(w \*Window\) SetSize\(width, height int\) \*Window](<#Window.SetSize>)
  - [func \(w \*Window\) SetTitle\(title string\) \*Window](<#Window.SetTitle>)
  - [func \(w \*Window\) SetTitleBarBgColor\(color retui.Color\) \*Window](<#Window.SetTitleBarBgColor>)
  - [func \(w \*Window\) Show\(\) \*Window](<#Window.Show>)
  - [func \(w \*Window\) ShowTitleBar\(\) bool](<#Window.ShowTitleBar>)
  - [func \(w \*Window\) String\(\) string](<#Window.String>)
  - [func \(w \*Window\) ToggleVisibility\(\) bool](<#Window.ToggleVisibility>)
- [type WindowManager](<#WindowManager>)
  - [func GetManager\(\) \*WindowManager](<#GetManager>)
  - [func NewWindowManager\(\) \*WindowManager](<#NewWindowManager>)
  - [func \(wm \*WindowManager\) AddWindow\(w \*Window\)](<#WindowManager.AddWindow>)
  - [func \(wm \*WindowManager\) BringToFront\(id string\)](<#WindowManager.BringToFront>)
  - [func \(wm \*WindowManager\) Count\(\) int](<#WindowManager.Count>)
  - [func \(wm \*WindowManager\) CountVisible\(\) int](<#WindowManager.CountVisible>)
  - [func \(wm \*WindowManager\) FocusNext\(\)](<#WindowManager.FocusNext>)
  - [func \(wm \*WindowManager\) GetActiveModal\(\) string](<#WindowManager.GetActiveModal>)
  - [func \(wm \*WindowManager\) GetAll\(\) \[\]\*Window](<#WindowManager.GetAll>)
  - [func \(wm \*WindowManager\) GetFocused\(\) string](<#WindowManager.GetFocused>)
  - [func \(wm \*WindowManager\) GetScreenSize\(\) \(int, int\)](<#WindowManager.GetScreenSize>)
  - [func \(wm \*WindowManager\) GetTopVisibleModal\(\) \*Window](<#WindowManager.GetTopVisibleModal>)
  - [func \(wm \*WindowManager\) GetVisible\(\) \[\]\*Window](<#WindowManager.GetVisible>)
  - [func \(wm \*WindowManager\) GetWindow\(id string\) \*Window](<#WindowManager.GetWindow>)
  - [func \(wm \*WindowManager\) GetZOrder\(\) \[\]string](<#WindowManager.GetZOrder>)
  - [func \(wm \*WindowManager\) HasWindow\(id string\) bool](<#WindowManager.HasWindow>)
  - [func \(wm \*WindowManager\) IsAnyModalOpen\(\) bool](<#WindowManager.IsAnyModalOpen>)
  - [func \(wm \*WindowManager\) IsFocused\(id string\) bool](<#WindowManager.IsFocused>)
  - [func \(wm \*WindowManager\) IsWindowBlocked\(id string\) bool](<#WindowManager.IsWindowBlocked>)
  - [func \(wm \*WindowManager\) RemoveWindow\(id string\)](<#WindowManager.RemoveWindow>)
  - [func \(wm \*WindowManager\) SetFocus\(id string\)](<#WindowManager.SetFocus>)
  - [func \(wm \*WindowManager\) SetRenderTrigger\(trigger func\(\)\)](<#WindowManager.SetRenderTrigger>)
  - [func \(wm \*WindowManager\) SetScreenSize\(width, height int\)](<#WindowManager.SetScreenSize>)


## Variables

<a name="DefaultScreenWidth"></a>

```go
var (
    DefaultScreenWidth  = 140
    DefaultScreenHeight = 40
)
```

<a name="CloseAll"></a>
## func CloseAll

```go
func CloseAll()
```

CloseAll closes all open windows.

<a name="CloseFocusedWindow"></a>
## func CloseFocusedWindow

```go
func CloseFocusedWindow()
```

closeFocusedWindow closes the currently focused window

<a name="Count"></a>
## func Count

```go
func Count() int
```

Count returns the number of open windows globally.

<a name="CountVisible"></a>
## func CountVisible

```go
func CountVisible() int
```

CountVisible returns the number of visible windows globally.

<a name="GetActiveModal"></a>
## func GetActiveModal

```go
func GetActiveModal() string
```

GetActiveModal returns the topmost modal ID.

<a name="GetFocused"></a>
## func GetFocused

```go
func GetFocused() string
```

GetFocused returns the globally focused window ID.

<a name="GetFocusedID"></a>
## func GetFocusedID

```go
func GetFocusedID() string
```



<a name="GetScreenSize"></a>
## func GetScreenSize

```go
func GetScreenSize() (int, int)
```

GetScreenSize returns the current global screen dimensions

<a name="IsAnyModalOpen"></a>
## func IsAnyModalOpen

```go
func IsAnyModalOpen() bool
```

IsAnyModalOpen returns true if any modal is currently open.

<a name="IsWindowBlocked"></a>
## func IsWindowBlocked

```go
func IsWindowBlocked(id string) bool
```

IsWindowBlocked returns true if window is blocked by modal.

<a name="RenderWindowsOverlay"></a>
## func RenderWindowsOverlay

```go
func RenderWindowsOverlay(mainContent retui.Element) retui.Element
```

RenderWindowsOverlay is a convenience function that renders windows as overlays. This is the recommended way to integrate window management into your app.

Usage:

```
func App(props retui.Props) retui.Element {
    mainContent := buildYourMainUI()
    return window.RenderWindowsOverlay(140, 40, mainContent)
}
```

<a name="ResetGlobalManager"></a>
## func ResetGlobalManager

```go
func ResetGlobalManager()
```

ResetGlobalManager resets the global manager state \(for testing\).

<a name="SetDefaultScreenSize"></a>
## func SetDefaultScreenSize

```go
func SetDefaultScreenSize(width, height int)
```

SetDefaultScreenSize sets the default screen size for auto\-centering

<a name="SetRenderTrigger"></a>
## func SetRenderTrigger

```go
func SetRenderTrigger(trigger func())
```

SetRenderTrigger sets the global render trigger function. This connects the window system to the retui render loop.

<a name="SetScreenSize"></a>
## func SetScreenSize

```go
func SetScreenSize(width, height int)
```

======================================== QUERY METHODS ======================================== SetScreenSize sets the screen dimensions globally

<a name="OverlayRenderer"></a>
## type OverlayRenderer

OverlayRenderer handles rendering windows as overlays on top of main content. It uses retui.Overlay\(\) for absolute positioning of windows.

```go
type OverlayRenderer struct{}
```

<a name="NewOverlayRenderer"></a>
### func NewOverlayRenderer

```go
func NewOverlayRenderer() *OverlayRenderer
```

NewOverlayRenderer creates a new overlay renderer with the given screen dimensions.

<a name="OverlayRenderer.Render"></a>
### func \(\*OverlayRenderer\) Render

```go
func (or *OverlayRenderer) Render(mainContent retui.Element) retui.Element
```

Render returns a full\-screen element with windows properly overlaid on main content. The render order is: Main Content → Windows \(in Z\-order\) Windows are rendered on top of main content using absolute positioning.

<a name="Window"></a>
## type Window

Window represents a single window in the application. It holds state, content, and rendering information.

```go
type Window struct {
    ID            string
    Title         string
    Width         int
    Height        int
    X             int           // Position on screen
    Y             int           // Position on screen
    Modal         bool          // Whether this window blocks background interaction
    StaticContent retui.Element // The content to display inside the window

    RenderFn func() retui.Element

    TitleBarBgColor retui.Color // Background color for the title bar
    BodyBgColor     retui.Color // Background color for the window body
    // contains filtered or unexported fields
}
```

<a name="AlertError"></a>
### func AlertError

```go
func AlertError(title string, content retui.Element) *Window
```

Alert shows a simple modal window with content

<a name="GetVisible"></a>
### func GetVisible

```go
func GetVisible() []*Window
```

GetVisible returns all visible windows globally.

<a name="GetWindowByID"></a>
### func GetWindowByID

```go
func GetWindowByID(id string) *Window
```



<a name="NewWindow"></a>
### func NewWindow

```go
func NewWindow() *Window
```

NewWindow creates a new window with given content. Window starts hidden \- you must call Show\(\) to display it.

<a name="Window.Center"></a>
### func \(\*Window\) Center

```go
func (w *Window) Center() *Window
```

======================================== POSITIONING HELPERS ======================================== Center automatically centers the window using the manager's screen dimensions

<a name="Window.CenterHorizontally"></a>
### func \(\*Window\) CenterHorizontally

```go
func (w *Window) CenterHorizontally(screenWidth int) *Window
```

CenterHorizontally centers the window horizontally on screen.

<a name="Window.CenterVertically"></a>
### func \(\*Window\) CenterVertically

```go
func (w *Window) CenterVertically(screenHeight int) *Window
```

CenterVertically centers the window vertically on screen.

<a name="Window.Clone"></a>
### func \(\*Window\) Clone

```go
func (w *Window) Clone() *Window
```

Clone creates a copy of the window with a new ID. Useful for duplicating windows.

<a name="Window.Close"></a>
### func \(\*Window\) Close

```go
func (w *Window) Close()
```

Close removes the window from the manager and hides it. The window cannot be shown again after closing.

<a name="Window.Focus"></a>
### func \(\*Window\) Focus

```go
func (w *Window) Focus()
```

Focus brings this window to the front \(top of Z\-order\).

<a name="Window.GetBodyBgColor"></a>
### func \(\*Window\) GetBodyBgColor

```go
func (w *Window) GetBodyBgColor() retui.Color
```

GetBodyBgColor returns the window body's background color.

<a name="Window.GetBounds"></a>
### func \(\*Window\) GetBounds

```go
func (w *Window) GetBounds() [4]int
```

GetBounds returns the window's position and size as \[x, y, width, height\].

<a name="Window.GetContent"></a>
### func \(\*Window\) GetContent

```go
func (w *Window) GetContent() retui.Element
```

GetContent returns the window's current content.

<a name="Window.GetPosition"></a>
### func \(\*Window\) GetPosition

```go
func (w *Window) GetPosition() [2]int
```

GetPosition returns the window position as \[x, y\].

<a name="Window.GetSize"></a>
### func \(\*Window\) GetSize

```go
func (w *Window) GetSize() [2]int
```

GetSize returns the window dimensions as \[width, height\].

<a name="Window.GetTitle"></a>
### func \(\*Window\) GetTitle

```go
func (w *Window) GetTitle() string
```

GetTitle returns the window title.

<a name="Window.GetTitleBarBgColor"></a>
### func \(\*Window\) GetTitleBarBgColor

```go
func (w *Window) GetTitleBarBgColor() retui.Color
```

GetTitleBarBgColor returns the title bar's background color.

<a name="Window.HandleKey"></a>
### func \(\*Window\) HandleKey

```go
func (w *Window) HandleKey(key retui.Key) bool
```

HandleKey is called by the framework's key\-dispatch loop when this

<a name="Window.Hide"></a>
### func \(\*Window\) Hide

```go
func (w *Window) Hide() *Window
```

Hide makes the window invisible but keeps it in the registry. The window can be shown again later with Show\(\).

<a name="Window.IsActive"></a>
### func \(\*Window\) IsActive

```go
func (w *Window) IsActive() bool
```

IsActive returns whether this window is focused \(alias for IsFocused\).

<a name="Window.IsFocused"></a>
### func \(\*Window\) IsFocused

```go
func (w *Window) IsFocused() bool
```

IsFocused returns whether this window is currently focused.

<a name="Window.IsModal"></a>
### func \(\*Window\) IsModal

```go
func (w *Window) IsModal() bool
```

IsModal returns whether this window is modal.

<a name="Window.IsVisible"></a>
### func \(\*Window\) IsVisible

```go
func (w *Window) IsVisible() bool
```

IsVisible returns whether the window is currently displayed.

<a name="Window.MoveBy"></a>
### func \(\*Window\) MoveBy

```go
func (w *Window) MoveBy(dx, dy int) *Window
```

MoveBy moves the window by the specified offset.

<a name="Window.MoveTo"></a>
### func \(\*Window\) MoveTo

```go
func (w *Window) MoveTo(x, y int) *Window
```

MoveTo moves the window to the specified position.

<a name="Window.OnKeyPress"></a>
### func \(\*Window\) OnKeyPress

```go
func (w *Window) OnKeyPress(fn func(key retui.Key) bool) *Window
```

OnKeyPress registers a callback invoked whenever this window has focus and receives a key event. Returns the window for chaining.

<a name="Window.Render"></a>
### func \(\*Window\) Render

```go
func (w *Window) Render() retui.Element
```

Render returns the retui.Element representing this window's UI. Called by the OverlayRenderer during render cycle.

<a name="Window.ResizeTo"></a>
### func \(\*Window\) ResizeTo

```go
func (w *Window) ResizeTo(width, height int) *Window
```

ResizeTo resizes the window to the specified dimensions.

<a name="Window.SetBodyBgColor"></a>
### func \(\*Window\) SetBodyBgColor

```go
func (w *Window) SetBodyBgColor(color retui.Color) *Window
```

SetBodyBgColor sets the background color of the window body.

<a name="Window.SetContent"></a>
### func \(\*Window\) SetContent

```go
func (w *Window) SetContent(content retui.Element) *Window
```

SetContent updates the window's content. Triggers a re\-render automatically.

<a name="Window.SetModal"></a>
### func \(\*Window\) SetModal

```go
func (w *Window) SetModal(modal bool) *Window
```

SetModal marks this window as modal \(blocks background input\).

<a name="Window.SetPosition"></a>
### func \(\*Window\) SetPosition

```go
func (w *Window) SetPosition(x, y int) *Window
```

SetPosition sets window coordinates on screen.

<a name="Window.SetRenderFn"></a>
### func \(\*Window\) SetRenderFn

```go
func (w *Window) SetRenderFn(fn func() retui.Element) *Window
```



<a name="Window.SetSize"></a>
### func \(\*Window\) SetSize

```go
func (w *Window) SetSize(width, height int) *Window
```

SetSize sets window dimensions.

<a name="Window.SetTitle"></a>
### func \(\*Window\) SetTitle

```go
func (w *Window) SetTitle(title string) *Window
```

SetTitle sets the window title.

<a name="Window.SetTitleBarBgColor"></a>
### func \(\*Window\) SetTitleBarBgColor

```go
func (w *Window) SetTitleBarBgColor(color retui.Color) *Window
```

SetTitleBarBgColor sets the background color of the title bar.

<a name="Window.Show"></a>
### func \(\*Window\) Show

```go
func (w *Window) Show() *Window
```

Show makes the window visible and adds it to the manager. Triggers a re\-render automatically.

<a name="Window.ShowTitleBar"></a>
### func \(\*Window\) ShowTitleBar

```go
func (w *Window) ShowTitleBar() bool
```

ShowTitleBar reports whether the title bar should be rendered. It's hidden automatically when no title has been set.

<a name="Window.String"></a>
### func \(\*Window\) String

```go
func (w *Window) String() string
```

String returns a string representation of the window.

<a name="Window.ToggleVisibility"></a>
### func \(\*Window\) ToggleVisibility

```go
func (w *Window) ToggleVisibility() bool
```

ToggleVisibility toggles the window between visible and hidden. Returns the new visibility state.

<a name="WindowManager"></a>
## type WindowManager

WindowManager manages all active windows in the application. It maintains the registry, Z\-order \(stacking\), and focus state. All operations are thread\-safe using a mutex.

```go
type WindowManager struct {
    // contains filtered or unexported fields
}
```

<a name="GetManager"></a>
### func GetManager

```go
func GetManager() *WindowManager
```

GetManager returns the global window manager instance.

<a name="NewWindowManager"></a>
### func NewWindowManager

```go
func NewWindowManager() *WindowManager
```

NewWindowManager creates a new window manager instance.

<a name="WindowManager.AddWindow"></a>
### func \(\*WindowManager\) AddWindow

```go
func (wm *WindowManager) AddWindow(w *Window)
```

AddWindow adds a window to the registry and Z\-order stack. Called when window.Show\(\) is invoked.

<a name="WindowManager.BringToFront"></a>
### func \(\*WindowManager\) BringToFront

```go
func (wm *WindowManager) BringToFront(id string)
```

BringToFront moves a window to the top of the Z\-order \(front\). Also updates focus to this window.

<a name="WindowManager.Count"></a>
### func \(\*WindowManager\) Count

```go
func (wm *WindowManager) Count() int
```

Count returns the number of open windows.

<a name="WindowManager.CountVisible"></a>
### func \(\*WindowManager\) CountVisible

```go
func (wm *WindowManager) CountVisible() int
```

CountVisible returns the number of visible windows.

<a name="WindowManager.FocusNext"></a>
### func \(\*WindowManager\) FocusNext

```go
func (wm *WindowManager) FocusNext()
```

FocusNext cycles focus to the next window in Z\-order. Useful for keyboard navigation \(e.g., Tab key\).

<a name="WindowManager.GetActiveModal"></a>
### func \(\*WindowManager\) GetActiveModal

```go
func (wm *WindowManager) GetActiveModal() string
```

GetActiveModal returns the topmost modal ID \(or empty\).

<a name="WindowManager.GetAll"></a>
### func \(\*WindowManager\) GetAll

```go
func (wm *WindowManager) GetAll() []*Window
```

GetAll returns all active windows.

<a name="WindowManager.GetFocused"></a>
### func \(\*WindowManager\) GetFocused

```go
func (wm *WindowManager) GetFocused() string
```

GetFocused returns the ID of the currently focused window.

<a name="WindowManager.GetScreenSize"></a>
### func \(\*WindowManager\) GetScreenSize

```go
func (wm *WindowManager) GetScreenSize() (int, int)
```

GetScreenSize returns current screen dimensions

<a name="WindowManager.GetTopVisibleModal"></a>
### func \(\*WindowManager\) GetTopVisibleModal

```go
func (wm *WindowManager) GetTopVisibleModal() *Window
```

GetTopVisibleModal returns the topmost visible modal window \(if any\).

<a name="WindowManager.GetVisible"></a>
### func \(\*WindowManager\) GetVisible

```go
func (wm *WindowManager) GetVisible() []*Window
```

GetVisible returns all visible windows in Z\-order \(bottom to top\).

<a name="WindowManager.GetWindow"></a>
### func \(\*WindowManager\) GetWindow

```go
func (wm *WindowManager) GetWindow(id string) *Window
```

GetWindow retrieves a window by ID.

<a name="WindowManager.GetZOrder"></a>
### func \(\*WindowManager\) GetZOrder

```go
func (wm *WindowManager) GetZOrder() []string
```

GetZOrder returns the current Z\-order stack \(bottom to top\). Returns a copy to prevent external modification.

<a name="WindowManager.HasWindow"></a>
### func \(\*WindowManager\) HasWindow

```go
func (wm *WindowManager) HasWindow(id string) bool
```

HasWindow checks if a window with the given ID exists.

<a name="WindowManager.IsAnyModalOpen"></a>
### func \(\*WindowManager\) IsAnyModalOpen

```go
func (wm *WindowManager) IsAnyModalOpen() bool
```

IsAnyModalOpen returns true if any modal window is currently visible.

<a name="WindowManager.IsFocused"></a>
### func \(\*WindowManager\) IsFocused

```go
func (wm *WindowManager) IsFocused(id string) bool
```

IsFocused checks if a window is currently focused.

<a name="WindowManager.IsWindowBlocked"></a>
### func \(\*WindowManager\) IsWindowBlocked

```go
func (wm *WindowManager) IsWindowBlocked(id string) bool
```

IsWindowBlocked returns true if the window is blocked by a modal.

<a name="WindowManager.RemoveWindow"></a>
### func \(\*WindowManager\) RemoveWindow

```go
func (wm *WindowManager) RemoveWindow(id string)
```

RemoveWindow removes a window from the registry and Z\-order. Called when window.Close\(\) is invoked.

<a name="WindowManager.SetFocus"></a>
### func \(\*WindowManager\) SetFocus

```go
func (wm *WindowManager) SetFocus(id string)
```

SetFocus sets which window should receive keyboard input.

<a name="WindowManager.SetRenderTrigger"></a>
### func \(\*WindowManager\) SetRenderTrigger

```go
func (wm *WindowManager) SetRenderTrigger(trigger func())
```

SetRenderTrigger sets the callback function that triggers re\-renders. This should be called from your main App to connect the window system with the retui render loop.

<a name="WindowManager.SetScreenSize"></a>
### func \(\*WindowManager\) SetScreenSize

```go
func (wm *WindowManager) SetScreenSize(width, height int)
```

======================================== RENDER TRIGGER ======================================== In WindowManager \- SetScreenSize updates screen dimensions

Generated by [gomarkdoc](<https://github.com/princjef/gomarkdoc>)
